/**
 * Home — placeholder until task_00_13 ships the actual login + dashboard
 * screens. Visible at http://localhost:3000/ when `npm run dev` is up.
 */
export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 px-6 text-center">
      <h1 className="text-4xl font-semibold tracking-tight">agentic-platform</h1>
      <p className="text-muted-foreground max-w-xl">System Admin panel · phase 0 skeleton.</p>
      <p className="text-muted-foreground text-sm">Login + dashboard arrive in task_00_13.</p>
    </main>
  );
}
