# =============================================================================
# Python
# =============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtualenvs
.venv/
venv/
env/
ENV/
.python-version

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg
wheels/
pip-wheel-metadata/

# Test / type-check / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.hypothesis/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# Tooling
.ipynb_checkpoints/

# =============================================================================
# Node / JavaScript / TypeScript
# =============================================================================
node_modules/
.pnpm-store/
.yarn/cache
.yarn/install-state.gz

# Build outputs
.next/
out/
.turbo/
.svelte-kit/
.nuxt/
.cache/
.parcel-cache/

# TypeScript
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Playwright artifacts (traces, screenshots, video, error-context.md)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# e2e runner logs from scripts/dev/run-e2e.ps1
.e2e-api-server.log
.e2e-api-server.err.log

# Detached dev stack state (scripts/dev/up.{ps1,sh} + down.{ps1,sh})
.dev/

# Estado del setup_demo_project.py (ids del proyecto y agente compartidos
# entre los demos de los tests humanos — local, no versionar). Los escribe
# `Path(__file__).parent`, o sea JUNTO al script: viven en scripts/demos/
# desde prod-15 task_gov_higiene_10.
scripts/demos/.demo_state.json

# =============================================================================
# Docker / Compose
# =============================================================================
# Local override (developers may keep their own)
docker-compose.override.yml

# =============================================================================
# Environment / Secrets
# =============================================================================
.env
.env.*
!.env.example
*.pem
*.key
*.crt
vault-unseal-keys.txt
vault-root-token.txt
vault-init-output/

# =============================================================================
# Data (mounted volumes, local stacks)
# =============================================================================
data/
/data/
*.sqlite
*.sqlite3
*.db

# =============================================================================
# IDE / Editor
# =============================================================================
.vscode/
!.vscode/extensions.json
!.vscode/settings.example.json
.idea/
*.swp
*.swo
*~
.project
.classpath

# =============================================================================
# OS
# =============================================================================
.DS_Store
Thumbs.db
desktop.ini

# =============================================================================
# Logs / Temp
# =============================================================================
*.log
logs/
tmp/
temp/

# =============================================================================
# Claude Code / local agent state
# =============================================================================
# .claude/settings.local.json is tracked when desired; keep this rule explicit
# to avoid accidentally committing local-only overrides if the convention changes.
# (currently tracked — uncomment to ignore again)
# .claude/settings.local.json
scripts/demos/.demo_state_05.json
scripts/demos/.demo_state_06.json
scripts/demos/.demo_06/
scripts/demos/.demo_state_06_8.json
scripts/demos/.demo_state_06_9.json

# Claude Code internal lockfile (per-session / per-machine).
.claude/scheduled_tasks.lock

# Bundles de backup locales (bind de workers-aux; durabilidad 2026-07-03)
.backups/

# Informe JSON de pip-audit (lo escribe el job security-scan y quien reproduzca
# el SCA en local con el comando del runbook). Es un artefacto, no una fuente.
pip-audit.json
