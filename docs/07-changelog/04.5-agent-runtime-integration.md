---
plan_id: 04.5-agent-runtime-integration
title: Integración del agent-runtime con la pila de memoria y RAG
started_at: 2026-05-26
completed_at: 2026-05-26
status: completed
tasks_done: 6
tasks_total: 6
tasks_pending_local: []
docs_language: es
---

> **Estado:** plan **`completed`** (cerrado el 2026-05-26). Las 6
> tareas (`task_04_5_01`..`task_04_5_06`) en `done` con sus tests
> automáticos en verde. Plan 04.5 cierra la brecha que Plan 04
> dejó explícita en su changelog: el agent-runtime ahora invoca de
> verdad los cinco tools que el placeholder 501 de
> Plan 02 task_02_19 mantenía dormidos, y el Memorizer se dispara
> automáticamente al finalizar cada Execution `done`.

# Changelog — Plan 04.5 · Integración agent-runtime ↔ memoria/RAG

Plan corto e intercalado entre Plan 04 (Memoria + RAG + KBs) y
Plan 05 (Multi-agente y coordinación avanzada). Su único propósito:
**cablear**. Plan 04 construyó la pila completa en el api-server;
Plan 04.5 la conecta al sandbox donde el agente realmente corre.

## Resultado

Al cierre del plan, la plataforma puede:

1. **Memorizar ejecuciones sin intervención humana.** Cuando una
   `Execution` cierra como `done`, el worker dispara
   `workers.memorize_execution` (Celery, fire-and-forget). El
   Memorizer aplica `should_memorize`, destila con Ollama y
   persiste `MemoryEntry` con el `scope` y owner pointer correctos
   (team_id desde el proyecto, project_id desde la task, global sin
   owner; el caso `private` se skipea con log para agentes IA).

2. **Tools de memoria reales en el agente.** `memory_recall` y
   `memory_store` ya no devuelven 501. Llaman a
   `/internal/agent/memory-recall` y `/internal/agent/memory-store`
   vía httpx, con el bearer `AGENTIC_INTERNAL_TOKEN` minteado por
   el worker. Owner pointers (team_id / project_id) se resuelven
   en el servidor desde el `Agent` row — el sandbox no puede
   widening de scope.

3. **RAG en el agente.** `rag_search` es un tool nuevo (no había
   placeholder). El sandbox llama a `/internal/agent/rag-search`
   y reusa el motor `api_server.rag.tool.rag_search` (BM25,
   vector y reranker). El `project_id` se pin del agente: cero
   forma de escapar al KB de otro proyecto.

4. **Tools Docling en el agente.** `document_convert` deja de ser
   501 y devuelve los chunks estructurados de un Document
   existente (lectura rápida sobre `chunks`). `promote_to_kb` es
   un tool nuevo que duplica un Document + sus chunks en otra KB
   del mismo proyecto. Ambos comprueban grants en
   `kb_projects` para lectura **y** escritura.

5. **Auth dedicado para el sandbox.** `/internal/agent/*` vive
   bajo un único `get_agent_principal` que valida un JWT con
   `kind: "agent"`. Tokens humanos sin ese claim se rechazan
   con 401; tokens de agente borrado entre mint y uso → 403.

## Tareas (6/6)

```
Fase A — Infraestructura
[x] task_04_5_01  Auth /internal/agent/* + token de sandbox + middleware    5h
Fase B — Memorizer trigger automático
[x] task_04_5_02  Celery task workers.memorize_execution + trigger          3h
Fase C — Endpoints + clientes en agent-runtime
[x] task_04_5_03  memory-recall + memory-store + adapters                   3h
[x] task_04_5_04  rag-search + adapter (tool nuevo)                         2h
[x] task_04_5_05  document-convert + promote-to-kb + adapters               3h
Fase D — Cierre
[x] task_04_5_06  ADR 0024 + changelog 04.5                                 2h
                                                                  ─────────
                                                              total 18h efectivas
```

## Decisiones clave (ADR 0024)

Las cinco tools del sandbox hablan con el api-server por
`/internal/agent/*` (HTTP+JSON). Alternativas descartadas: opción B
(pre-inyectar contexto en `AGENT_TASK_SPEC`) y opción C (sink para
writes + HTTP para reads). HTTP puro gana por **read-after-write
dentro de la misma run** y **menos piezas móviles**. Ver
`docs/05-architecture-decisions/0024-internal-agent-http-api.md`.

## Tests automáticos (50 nuevos)

| Suite                             | Tests  | Cobertura                            |
| --------------------------------- | ------ | ------------------------------------ |
| `test_internal_agent_auth.py`     | 10     | Mint / decode / kind-claim / smoke   |
| `test_memorizer_trigger.py`       | 7      | Memorizer Celery + trigger predicate |
| `test_memory_tools_wireup.py`     | 10     | Endpoints + adapters + validación    |
| `test_rag_search_wireup.py`       | 7      | Endpoint + cross-project boundary    |
| `test_docling_tools_wireup.py`    | 10     | document_convert + promote_to_kb     |
| `test_placeholders.py` (refactor) | 3      | Catálogo vacío, factory dormida      |
| **Total**                         | **47** | —                                    |

## Tests humanos (2/2)

Las dos pruebas humanas que Plan 04 dejó pendientes a la espera
del wire-up del agent-runtime ya son ejecutables end-to-end:

1. **`human_04_5_01`** — _Memory replay._ Arrancar una ejecución
   `done` con un agente `memory_scope=team_shared`, comprobar en
   `/admin/memories` que aparece la(s) entrada(s) destilada(s).
   Lanzar una segunda ejecución que pida algo afín; los pasos
   `recall` del nuevo agente deben referenciar la memoria
   almacenada.

2. **`human_04_5_02`** — _RAG con citas._ Subir un PDF a una KB de
   un proyecto (UI Plan 04 task_04_19). Esperar a que se indexe.
   Pedirle al agente algo cuya respuesta esté en ese PDF; el
   `steps_log` debe incluir un `tool_call` a `rag_search` con
   hits cuyo `document_id` apunte al PDF subido.

Ejecutar sobre la rama `plan/04.5-agent-runtime-integration` antes
de mergear a `main`. Los dos tests humanos se exponen en vivo con
los scripts `scripts/setup_demo_04_5.py` +
`scripts/demo_human_04_5_0{1,2}.py`; la guía paso a paso (orden,
variables de entorno, troubleshooting) vive en
[docs/03-guides/run-demo-human-tests.md](../03-guides/run-demo-human-tests.md).

## Cambios principales

| Capa                        | Cambios                                                        |
| --------------------------- | -------------------------------------------------------------- |
| Auth `/internal/agent/*`    | `api_server/auth/internal_agent.py` (nuevo)                    |
| Router `/internal/agent/*`  | 5 endpoints + smoke `_health`                                  |
| Worker trigger              | `workers/memorizer.py` + hook en `execution.py`                |
| Agent-runtime HTTP client   | `agent_runtime/internal_api.py` (nuevo)                        |
| Agent-runtime tool adapters | `memory_tools.py`, `rag_tools.py`, `docling_tools.py` (nuevos) |
| Placeholder catalogue       | Vaciado (los 3 placeholders originales ya son tools reales)    |
| ADR                         | 0024 — HTTP `/internal/agent/*`                                |
| Pre-commit                  | mypy exclude añade `workers/memorizer.py`                      |

## Pre-condiciones cumplidas

- Plan 04 cerrado (status: `completed`).
- ADR 0012 (aislamiento sandbox) intacto: el sandbox NO tiene
  credenciales BD, ni socket Docker, ni acceso al fs del host. Sólo
  HTTP al api-server + un bearer token.
- El embedder real (Plan 04 task_04_14) sigue fuera de la hot-path:
  `/internal/agent/memory-recall` y `/internal/agent/rag-search`
  corren BM25-only por defecto. Activar el embedder es un cambio
  de configuración, no de código.

## Métricas

| Métrica                         | Valor                                               |
| ------------------------------- | --------------------------------------------------- |
| Tareas técnicas                 | 6                                                   |
| Tests automáticos en el plan    | 47 nuevos (+13 regresión Plan 04 ok)                |
| Líneas Python netas             | +1.8k (api-server 0.6k + sandbox 0.4k + tests 0.8k) |
| Nuevos servicios docker-compose | 0                                                   |
| Migraciones                     | 0                                                   |
| Cobertura humana                | 2/2 pruebas humanas ejecutables                     |

## Addendum post-merge (2026-05-26)

Tras cerrar el plan, una sesión de validación humana en vivo destapó
ruido alrededor del Plan 04.5 que se cerró en commits aparte sobre la
misma rama:

- **Launcher one-shot** (`scripts/dev/run-human-tests.ps1`): un comando
  arranca el stack si no está + corre los 2 setups idempotentes + los
  7 demos (Plan 02 ×5 + Plan 04.5 ×2) + imprime PASS/FAIL.
- **Guía detallada por test** (`docs/03-guides/run-demo-human-tests.md`):
  para cada uno de los 12 tests humanos del repo (Plan 02 + Plan 04 + Plan
  04.5) — qué prueba, comando exacto, output esperado literal, pass
  criteria, UI dónde mirar y falla típica.
- **Dashboard `/admin/system-health`**: ampliado de 5 a 8 servicios
  (añadidos `docling-serve` HTTP, `ollama` HTTP, `egress-proxy` TCP).
  Timeout del probe 2 s → 5 s + `rollback` defensivo en `_check_postgres`
  para evitar `PendingRollbackError` en cold pool. Puertos `5001` y
  `8888` expuestos al host en `docker-compose.dev.yml`.
- **Demo seed mejorado**:
  - `demo_human_04_5_01.py` siembra el `steps_log` con shape canónico
    (`index` + `node` + `kind` + `status` + `summary` por paso) para
    que `/admin/executions/<id>` pinte la Timeline con 5 nodos.
  - `setup_demo_04_5.py` siembra el Document como `application/pdf`
    con 2 páginas + bboxes por chunk para que `/citations` pinte 4
    rectángulos azules sobre 2 hojas A4 placeholder.
  - Demos imprimen URLs absolutas (`http://localhost:3000/...`) con
    UUIDs reales sustituidos — Ctrl+click directo desde la terminal.
- **Admin-panel UX**:
  - `/admin/documents/<id>/ingestion` ahora hace fetch del Document
    al cargar (antes sólo escuchaba el WebSocket → "Pendiente" falso
    cuando el documento estaba ya `indexed` sin pipeline). Reutiliza
    `/documents/<id>/citations` para sacar el `document`; añade
    `error_message` a esa respuesta.
  - Mensaje contextual en la card "Eventos" según el estado real:
    `indexed` → "Documento ya indexado, no llegarán eventos…",
    `failed` → motivo del fallo.
  - `suppressHydrationWarning` en `<body>` del root layout para
    silenciar el ruido que extensiones del navegador (ColorZilla,
    Grammarly, LastPass) inyectan en `<body>` tras el SSR.
  - `app/icon.svg` + `app/favicon.ico` (16×16 generado a mano con
    `struct`) — fin del `GET /favicon.ico 404`.
- **Compose dev**:
  - `docling-mcp` comentado del compose (upstream no publica imagen
    en GHCR; documentación en
    `docs/03-guides/gotchas/docling-mcp-no-public-image.md`).

Ninguno de estos cambios altera el contrato del plan ni los ADR
asociados; son **mejoras de DX** sobre la pila que el plan dejó
funcionando.

## Siguiente fase

Tras cerrar este plan, el siguiente activo es **Plan 05 — MCP &
Tools avanzadas**. `blocking_plan` de Plan 05 incluye 04.5, así que
queda desbloqueado. Plan 05 ya no arrastra deuda de wire-up: empieza
sobre un agent-runtime que de verdad puede leer y escribir memoria,
buscar en KBs y promocionar documentos.
