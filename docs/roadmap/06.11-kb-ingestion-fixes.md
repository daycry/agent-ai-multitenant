---
plan_id: 06.11-kb-ingestion-fixes
title: KB ingestion wiring + arreglos críticos del subsistema de Knowledge Bases
status: in_progress
blocking_plan: [06.10-kb-categories]
started_at: 2026-05-29
completed_at: null
estimated_duration_calendar: 3-4 días
estimated_effort_person_days: 3
estimated_cost_human_eur: 1.200 € – 2.000 €
estimated_cost_ai_eur: 20 € – 40 €
created_by: system_architect
spec_sections_referenced: [11, 13]
docs_language: es
---

# Plan 06.11 — KB ingestion wiring + arreglos críticos

## Cabecera

| Campo                            | Valor                           |
| -------------------------------- | ------------------------------- |
| **ID del Plan**                  | `06.11-kb-ingestion-fixes`      |
| **Estado**                       | `in_progress`                   |
| **Bloqueado por**                | `06.10-kb-categories`           |
| **Tiempo estimado (calendario)** | 3-4 días                        |
| **Rama git**                     | `plan/06.11-kb-ingestion-fixes` |

---

## Resumen

Una auditoría end-to-end del subsistema de KB (backend, frontend, modelo
de datos, RLS) destapó que **el flujo principal está roto**: los
documentos que un usuario sube se quedan en `status='pending'` para
siempre porque **nadie encola la ingestión**. El pipeline
`ingest_document` existe y está testeado, pero no tiene llamador de
producción, no hay módulo worker (`apps/workers/src/workers/ingestion.py`
nunca se creó) y `upload_document` solo escribe en MinIO + inserta la
fila y devuelve 201.

Además la auditoría encontró dos fallos de correctitud y varios huecos
de UX que hacían el subsistema confuso e incompleto. Este plan cierra el
bloque crítico.

## Alcance

**Entra**:

### Backend — correctitud y wiring

- **Cablear la ingestión** (fix #1): crear el módulo worker
  `apps/workers/src/workers/ingestion.py` con la task Celery
  `workers.ingest_document` (adaptador fino sobre `ingest_document`),
  encolarla desde `upload_document` tras el flush (tolerante a broker
  caído, patrón `trigger_memorize`), y un beat de respaldo
  `workers.sweep_pending_documents` que reencola documentos atascados en
  `pending`.
- **Arreglar el drift de soft-delete en el RAG**: `visibility_filter_clause`
  (el filtro real de chunks) no comprueba `deleted_at`, así que los
  chunks de una KB borrada siguen siendo recuperables. Añadir
  `knowledge_bases.deleted_at IS NULL` + `documents.deleted_at IS NULL`.
- **Endpoint de re-indexar**: `POST /knowledge-bases/{kb}/documents/{doc}/reindex`
  que resetea a `pending`, limpia `error_message` y reencola. Permite
  recuperarse de `failed`.
- **Built-ins alcanzables**: hoy las KB built-in sembradas bajo
  `PLATFORM_TENANT_ID` son invisibles para los tenants (sin política RLS
  `_builtin_read`, y el resolver filtra por `tenant_id`). Migrar a
  `tenant_id IS NULL` + política `knowledge_bases_builtin_read` + rama
  global en el resolver y en el filtro de chunks (mismo patrón que
  `kb_categories`, decisión del humano).

### Frontend — descubribilidad

- **Subida + lista de documentos en la página general de KB**
  (`/admin/knowledge-bases`): por cada KB, expandir para ver sus
  documentos (`GET /knowledge-bases/{id}/documents`) y un botón
  "Subir documento" que reusa el `UploadDialog`. El endpoint ya es
  KB-scoped — no requiere grant a proyecto. Resuelve la queja directa
  del usuario.
- **Corregir copys que misdirigen** + convertir prosa en enlaces reales
  en los diálogos de crear/conceder/asignaciones.

### Documentación

- Nota explícita en la guía de KB: **los equipos no tienen KB**; el
  conocimiento se ancla a agente (rol) y proyecto (stack); `team_shared`
  es solo para memoria. (Decisión del humano: no implementar KB de team.)
- Entrada de changelog.

**Queda fuera** (follow-ups):

- `team_knowledge_bases` (rechazado por ADR 0026; solo documentamos la
  ausencia).
- Cablear `apply_template_kb_grants` en la creación de proyecto (queda
  para un plan que toque el wizard de proyectos; depende del fix de
  built-ins de éste).
- Bulk upload, drag-drop, descarga/preview del original, vista de "KBs
  efectivas", embedder configurable por KB (Plan 12).

## Decisiones clave

- **Worker BYPASSRLS**: la task de ingestión usa el engine
  `migrations_user` (BYPASSRLS) como el memorizer; el pipeline escribe
  `tenant_id` explícito en cada chunk, así que es seguro sin setear
  `app.tenant_id`.
- **Enqueue tolerante a fallos**: si el broker está caído, el upload no
  falla (devuelve 201); el beat de respaldo recoge el `pending`.
- **Built-ins con `tenant_id NULL`** (no policy ligada a
  `PLATFORM_TENANT_ID`): consistente con `kb_categories` y
  `approval_policy_templates`.

## Tareas

### Fase A — Backend

#### `task_06_11_01` — Cablear ingestión (worker + enqueue + beat)

- [x] **Título**: `workers/ingestion.py` (task `workers.ingest_document`) + `trigger_ingestion` desde `upload_document` + beat
      `sweep_pending_documents`
- **Tiempo**: 4 h · **Complejidad**: m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_11_01_a
    runtime: python-pytest
    command: "pytest tests/integration/test_ingestion_worker.py -v"
  ```

#### `task_06_11_02` — Fix drift de soft-delete en visibility_filter_clause

- [x] **Título**: `deleted_at IS NULL` en ambas ramas + test de regresión
- **Tiempo**: 45 min · **Complejidad**: s

#### `task_06_11_03` — Endpoint de re-indexar

- [x] **Título**: `POST /knowledge-bases/{kb}/documents/{doc}/reindex`
      (resetea a `pending` + reencola)
- **Tiempo**: 1 h · **Complejidad**: s

#### `task_06_11_04` — Built-ins alcanzables (tenant_id NULL + RLS + resolver)

- [ ] **Título**: Migración (column nullable + builtin rows a NULL +
      índice COALESCE + policy `_builtin_read`) + rama global en resolver
      y filtro de chunks + seed
- **Tiempo**: 3 h · **Complejidad**: m
- **Estado**: DIFERIDA. La auditoría la marcó 🟠 (no bloqueante) y al
  diseñarla en detalle resultó tener mucho más ripple/riesgo del
  estimado: cambia la nulabilidad de `tenant_id` en una tabla core,
  toca la RLS, **el listado de KB de todos los tenants** (de golpe verían
  el catálogo built-in), y ripplea a ORM/schema/`to_kb_response`/resolver/
  filtro de chunks/seed/tipos del frontend + ocultar edit-delete de
  built-ins. Además **las KB built-in hoy están vacías** (sin chunks),
  así que el payoff inmediato es bajo. Merece su propio PR con test
  humano dedicado. Pendiente de confirmación del humano para abordarla
  por separado.

### Fase B — Frontend

#### `task_06_11_05` — Subida + lista de documentos en `/admin/knowledge-bases`

- [x] **Título**: Expander por KB con docs + botón "Subir documento"
      reusando `UploadDialog`
- **Tiempo**: 3 h · **Complejidad**: m

#### `task_06_11_06` — Corregir copys + enlaces reales

- [x] **Título**: Empty-states y diálogos con `Link` clicable en vez de
      prosa que misdirige
- **Tiempo**: 1 h · **Complejidad**: s

### Fase C — Documentación

#### `task_06_11_07` — Documentar ausencia de KB de team + changelog

- [x] **Título**: Nota en la guía rol-vs-stack + entrada de changelog
- **Tiempo**: 45 min · **Complejidad**: s

## Tests humanos del Plan

```yaml
- id: human_06_11_01
  description: "Subir un documento e indexarlo end-to-end"
  hint: "Con el stack arriba (docling-serve, clamav, ollama, minio)"
  checklist:
    - "Subir un .md/.pdf desde /admin/knowledge-bases (botón 'Subir documento')"
    - "El documento pasa de 'pending' a 'indexed' (badge + página de progreso)"
    - "El RAG recupera chunks de ese documento en un chat del proyecto/agente con grant"

- id: human_06_11_02
  description: "Re-indexar un documento fallido"
  checklist:
    - "Un documento en 'failed' muestra botón 'Reintentar'"
    - "Tras reintentar vuelve a 'pending' y se reprocesa"

- id: human_06_11_03
  description: "KB built-in visible y usable"
  checklist:
    - "Las KB built-in del catálogo aparecen y se pueden conceder a un proyecto/agente"
    - "Un agente con grant a una built-in recupera sus chunks"

- id: human_06_11_04
  description: "KB borrada no contamina el RAG"
  checklist:
    - "Tras borrar (soft-delete) una KB, sus chunks dejan de aparecer en resultados de búsqueda"
```

## Criterios de cierre

1. Las 7 tareas con `[x]` y sus tests automáticos verdes.
2. Los 4 tests humanos pass (requieren stack arriba).
3. Entrada de changelog en `docs/07-changelog/06.11-kb-ingestion-fixes.md`.
4. PR mergeado a `master`.
