---
plan_id: 06.14-hardening-auditoria
title: Hardening de seguridad y robustez — remediación de auditoría
status: pending_approval
blocking_plan: []
started_at: null
completed_at: null
estimated_duration_calendar: 8-12 días
estimated_effort_person_days: 8
estimated_cost_human_eur: 3.200 € – 5.000 €
estimated_cost_ai_eur: 60 € – 120 €
created_by: system_architect
spec_sections_referenced: [3, 4, 7, 10, 16]
docs_language: es
---

# Plan 06.14 — Hardening de seguridad y robustez (remediación de auditoría)

> **Origen:** auditoría multi-agente de 16 dimensiones con verificación adversarial sobre el
> código _ya implementado_ (`apps/api-server`, `apps/workers`, `apps/orchestrator`,
> `apps/watchdog`, `packages/shared-*`, `apps/admin-panel`). 98 hallazgos brutos → **68
> confirmados** (5 críticos, 18 altos, 19 medios, 26 bajos), 30 refutados. El roadmap aún no
> construido queda **fuera de alcance**: este plan solo endurece lo que ya existe.
>
> 📋 El **catálogo completo de los 68 hallazgos confirmados** (cada uno con `gid`, severidad,
> `fichero:línea`, problema, fix y la tarea que lo cubre) está en el **Anexo A** al final de
> este documento; los **30 refutados** por verificación adversarial, en el **Anexo B**.

## Cabecera

| Campo                              | Valor                               |
| ---------------------------------- | ----------------------------------- |
| **ID del Plan**                    | `06.14-hardening-auditoria`         |
| **Estado**                         | `pending_approval`                  |
| **Bloqueado por**                  | — (hardening de código ya mergeado) |
| **Tiempo estimado (calendario)**   | 8-12 días                           |
| **Tiempo estimado (persona-días)** | 8                                   |
| **Rama git sugerida**              | `plan/06.14-hardening-auditoria`    |

---

## Resumen

El núcleo de aislamiento multi-tenant está **bien construido** (RLS con rol `app_user`
NOBYPASSRLS + rol admin BYPASSRLS, `set_config('app.tenant_id', …)` por transacción vía
`get_tenant_session`, gates de rol centralizados del plan 06.8, gotchas documentados). La
verificación adversarial lo confirma: **los 8 hallazgos de async-correctness fueron refutados**
y varios supuestos "bypass de tenant" en orchestrator/RAG también. Los problemas reales se
concentran en los **bordes**: WebSocket sin autorización por tenant, workers que corren como
BYPASSRLS sin validar propiedad, secretos dev sin validación en arranque, y robustez operativa
de Celery/orchestrator. Este plan **cierra esos gaps** sobre el código ya implementado, sin
tocar funcionalidad de fases futuras.

Por severidad: **5 críticos** (fuga cross-tenant en WS, workers sin contexto de tenant, secretos
sin validar), **18 altos**, **19 medios**, **26 bajos**. Se abordan todos.

## Alcance

**Entra** (workstreams → Fases A-E):

- **Seguridad crítica (P0):** autorización por tenant en los 4 endpoints WebSocket; validación
  tenant↔recurso en el borde de workers/orchestrator; validación fail-fast de secretos al arranque.
- **Correctness/robustez (P1):** robustez de Celery (retries/idempotencia/timeout/DLQ); transacción
  de dispatch + DLQ del consumer; manejo de errores de streaming en 3 providers LLM + retry 401
  Copilot + clasificación 401/403; enforcement (o corrección del claim) de `allowed_tools`; check de
  owner-pointer en merge de memorias; check de proyecto borrado en token de agente; middleware de
  request-context + exception handler global + saneo de `IntegrityError`.
- **Hardening (P2):** límites de seguridad en contenedores aux/proxy + pin de imagen MinIO;
  validación de args MCP + cap de tamaño de output; paginación en listados; Error Boundary frontend;
  acotar CORS.
- **Cleanups (P3):** índices FK faltantes (migración reversible), gaps de validación de inputs,
  exposición de detalles de error en health-check, fallos menores de frontend, locking de worktrees,
  pin de docling, TOCTOU en promoción del primer admin.
- **Tests & CI:** gate CI cross-tenant; tests de regresión por cada fix P0/P1; cerrar gaps de test.

**Queda fuera** (es roadmap NO implementado — NO son fallos):

- Motor de guardrails **pre_llm / post_llm / post_tool** y capa de config platform/tenant/project
  → **Plan 11** (hallazgos guardrails-2/4/5; guardrails-3 fue refutado). Aquí solo se corrige el
  _claim_ de enforcement de `allowed_tools` (guardrails-1).
- Migración de JWT a **httpOnly cookies** (quitar token de localStorage y del query de WS)
  → **Plan 15** (frontend-admin-panel-1/5). Aquí solo se cierra la autorización del WS.

## Decisiones clave

- **WS:** los handlers WebSocket no usan `Depends(get_tenant_session)` con la ergonomía de REST, así
  que se replica el patrón manualmente (decode JWT → validar sesión Redis → abrir sesión con
  `set_config` → cargar recurso bajo RLS → cerrar `1008` si `None`). Fuente de verdad: el patrón de
  `get_principal`/`get_tenant_session` en `auth/deps.py`.
- **Workers BYPASSRLS:** NO se cambia el rol a NOBYPASSRLS — el worker opera legítimamente
  cross-tenant (escribe `executions` de varios tenants). El fix es **validación de propiedad en el
  borde** (`row.tenant_id == request.tenant_id`) + `set_config` defensivo. Confirmado:
  `workers/config.py:30-35` documenta el rol como BYPASSRLS.
- **`llm-providers-1` (Protocol `def stream`→`async def stream`): NO aplicar el fix sugerido a
  ciegas.** `base.py:43` ya usa `def stream(...) -> AsyncIterator`, que es la forma **correcta** de
  tipar un async-generator; cambiarlo a `async def` rompería los call sites (forzaría `await`). La
  tarea solo investiga si mypy strict realmente se queja y, si no, lo deja como está.
- **Secretos:** `@model_validator(mode="after")` en las 3 `config.py`; falla solo en
  `environment in {staging, prod}`, no en dev. Plan 15 sustituirá por Vault (no se adelanta).
- **Frontend no es fuente de verdad** (igual que 06.8): los fixes de UI son UX; la seguridad la
  garantiza el backend.

## Tareas

### Fase A — P0 Seguridad crítica

#### `task_06_14_01` — Autorización por tenant en WebSocket

- [x] **Título**: `_authenticate` devuelve `AuthPrincipal` + valida sesión Redis; cada handler carga
      el recurso bajo RLS y cierra `1008` si no pertenece al tenant; `kanban` filtra también por `tenant_id`
- **Archivos**: `apps/api-server/src/api_server/routers/ws.py`
- **Cubre (gids)**: ws-sse-realtime-1/2/3/5, auth-rbac-casbin-3, auth-rbac-casbin-1, ws-sse-realtime-4
- **Tiempo**: 6 h · **Complejidad**: m · **Rol**: backend_dev
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_01_a
    runtime: python-pytest
    command: "pytest tests/integration/test_ws_tenant_isolation.py -v"
  ```

#### `task_06_14_02` — Validación tenant↔recurso en workers/orchestrator

- [x] **Título**: Validar `task.tenant_id == request.tenant_id` en el borde de `conduct_execution`
      (`CrossTenantExecutionError`). Reconciliación: `ingestion`/`memorizer` derivan el tenant del
      propio row (solo reciben `document_id`/`execution_id`, sin tenant a contrastar) → ya seguros;
      los sweeps de `maintenance` son cross-tenant **por diseño**. Solo `conduct_execution` recibía
      `tenant_id` y `task_id` por separado sin contrastarlos (el camino realmente explotable).
- **Archivos**: `apps/workers/src/workers/execution.py`
- **Cubre (gids)**: multi-tenancy-rls-1 (crit), multi-tenancy-rls-3 (reconciliado), multi-tenancy-rls-5
- **Tiempo**: 5 h · **Complejidad**: m · **Rol**: backend_dev
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_02_a
    runtime: python-pytest
    command: "pytest tests/integration/test_worker_tenant_boundary.py -v"
  ```

#### `task_06_14_03` — Validación fail-fast de secretos al arranque

- [x] **Título**: `@model_validator` en `Settings` (api/workers/orchestrator) que rechaza defaults dev (markers `changeme`/`dev-only`/`minioadmin`) en staging/prod
- **Archivos**: `apps/{api-server,workers,orchestrator}/.../config.py`
- **Cubre (gids)**: secrets-config-1 (crit), secrets-config-2/3/5/7
- **Tiempo**: 3 h · **Complejidad**: s · **Rol**: backend_dev
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_03_a
    runtime: python-pytest
    command: "pytest tests/unit/test_settings_prod_validation.py -v"
  ```

### Fase B — P1 Correctness / robustez

#### `task_06_14_04` — Robustez de tareas Celery (retry/idempotencia/timeout/DLQ)

- [ ] **Título**: retry config + `soft_time_limit`/`time_limit` + idempotencia de `run_execution` + dead-letter en `on_failure` + `asyncio.wait_for` sobre `to_thread`
- **Archivos**: `apps/workers/src/workers/{celery_app,tasks,execution}.py`
- **Cubre (gids)**: workers-orchestrator-1/2/10
- **Tiempo**: 6 h · **Complejidad**: m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_04_a
    runtime: python-pytest
    command: "pytest tests/integration/test_celery_idempotency.py -v"
  ```

#### `task_06_14_05` — Transacción de dispatch + DLQ del consumer

- [ ] **Título**: Evitar lost-update (mover `send_task` dentro de la transacción o patrón outbox); rutar errores del handler a dead-letter
- **Archivos**: `apps/orchestrator/src/orchestrator/{dispatch,consumer}.py`
- **Cubre (gids)**: workers-orchestrator-4/8
- **Tiempo**: 5 h · **Complejidad**: m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_05_a
    runtime: python-pytest
    command: "pytest tests/integration/test_orchestrator_dispatch_atomicity.py -v"
  ```

#### `task_06_14_06` — Manejo de errores de streaming LLM + retry/clasificación

- [ ] **Título**: try/except→`ProviderError` en `aiter_lines` de copilot/azure/ollama; retry 401 en `Copilot.stream()`; separar 401/403 en `_openai_compat`. **Investigar** mypy para llm-providers-1
- **Archivos**: `packages/shared-llm/src/shared_llm/providers/{copilot,azure_foundry,ollama,_openai_compat}.py`, `base.py`
- **Cubre (gids)**: llm-providers-2/3/5 (+ investigar llm-providers-1)
- **Tiempo**: 4 h · **Complejidad**: m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_06_a
    runtime: python-pytest
    command: "pytest tests/integration/test_model_clients.py tests/unit/test_openai_compat_helpers.py -v"
  ```

#### `task_06_14_07` — `allowed_tools`: enforcement o corrección del claim

- [ ] **Título**: Implementar el check de allowlist en tiempo de tool-call **o** corregir el comentario que afirma un enforcement inexistente
- **Archivos**: `apps/api-server/src/api_server/chat/modes.py`, `db/custom_chat_mode.py`, `docker/agent-runtimes/agent-runtime/agent_runtime/tools.py`
- **Cubre (gids)**: guardrails-1
- **Tiempo**: 3 h · **Complejidad**: s/m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_07_a
    runtime: python-pytest
    command: "pytest tests/integration/test_chat_modes_allowed_tools.py -v"
  ```

#### `task_06_14_08` — Owner-pointer en merge/listado de memorias

- [ ] **Título**: Validar mismo owner pointer (project/team/user) además de scope antes de fusionar (→422); filtro de owner en `list_similar_memories`
- **Archivos**: `apps/api-server/src/api_server/routers/memories.py`
- **Cubre (gids)**: rag-memory-ingestion-2
- **Tiempo**: 2 h · **Complejidad**: s
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_08_a
    runtime: python-pytest
    command: "pytest tests/integration/test_memory_merge_scope.py -v"
  ```

#### `task_06_14_09` — Token de agente revoca con proyecto borrado

- [ ] **Título**: En `get_agent_principal`/`_agent_exists` verificar `Project.deleted_at IS NULL` cuando hay `project_id`
- **Archivos**: `apps/api-server/src/api_server/auth/internal_agent.py`
- **Cubre (gids)**: auth-rbac-casbin-5
- **Tiempo**: 1 h · **Complejidad**: s
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_09_a
    runtime: python-pytest
    command: "pytest tests/integration/test_agent_token_project_deleted.py -v"
  ```

#### `task_06_14_10` — Request-context + exception handler global + saneo IntegrityError

- [ ] **Título**: Middleware que llama `bind_request_context()`/`clear_request_context()`; `@app.exception_handler(Exception)` en `main.py`; saneo proactivo de `IntegrityError` en KB
- **Archivos**: `apps/api-server/src/api_server/{main.py,logging/context.py,routers/knowledge_bases.py}`
- **Cubre (gids)**: error-obs-logging-1/3/5
- **Tiempo**: 4 h · **Complejidad**: m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_10_a
    runtime: python-pytest
    command: "pytest tests/integration/test_error_handling_observability.py -v"
  ```

### Fase C — P2 Hardening

#### `task_06_14_11` — Hardening de contenedores aux/proxy + pin MinIO

- [ ] **Título**: `cap_drop=['ALL']`, `no-new-privileges`, `mem_limit`/`pids_limit` en aux services y DinD proxy; pinear `minio/minio` a versión fija
- **Archivos**: `apps/workers/src/workers/test_runtime.py`, `docker/docker-compose.yml`
- **Cubre (gids)**: container-isolation-1/2/3
- **Tiempo**: 3 h · **Complejidad**: s/m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_11_a
    runtime: python-pytest
    command: "pytest tests/integration/test_test_runtime_hardening.py -v"
  ```

#### `task_06_14_12` — Validación de args MCP + cap de output

- [ ] **Título**: Validar args contra `input_schema` (jsonschema) antes de `tools.call`; limitar tamaño de output del tool (reusar `_truncate`)
- **Archivos**: `docker/agent-runtimes/agent-runtime/agent_runtime/{graph,mcp_tools}.py`
- **Cubre (gids)**: mcp-tools-1/2
- **Tiempo**: 3 h · **Complejidad**: s
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_12_a
    runtime: python-pytest
    command: "pytest tests/integration/test_mcp_tool_validation.py -v"
  ```

#### `task_06_14_13` — Paginación en endpoints de listado

- [ ] **Título**: `limit`/`offset` (con `ge/le`) en `list_agents`/`list_agent_kbs` y réplica en tasks/skills/tools/plans
- **Archivos**: `apps/api-server/src/api_server/routers/{agents,tasks,skills,tools,plans}.py`
- **Cubre (gids)**: api-routers-validation-3
- **Tiempo**: 3 h · **Complejidad**: s
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_13_a
    runtime: python-pytest
    command: "pytest tests/integration/test_list_pagination.py -v"
  ```

#### `task_06_14_14` — Error Boundary frontend + acotar CORS

- [ ] **Título**: Error Boundary en `admin/layout.tsx` + `QueryErrorResetBoundary`; restringir `allow_methods`/`allow_headers` a lo usado (no `["*"]`)
- **Archivos**: `apps/admin-panel/app/admin/layout.tsx`, `apps/api-server/src/api_server/main.py`
- **Cubre (gids)**: frontend-admin-panel-8, secrets-config-4
- **Tiempo**: 3 h · **Complejidad**: s
- **Tests automáticos**: `npm run typecheck && npm run lint && npm run build` (admin-panel); test integration CORS preflight

### Fase D — P3 Cleanups (bajos)

#### `task_06_14_15` — Bundle de cleanups de severidad baja

- [ ] **Título**: Índices FK faltantes (migración reversible) + gaps de validación de inputs + health-check error + frontend menores + locking worktrees + pin docling + TOCTOU primer admin + Copilot JWT margin
- **Archivos**: nueva migración en `apps/api-server/migrations/versions/`; routers/schemas varios; admin-panel varios; `workers/{git_repos,memorizer}.py`; `docker/docker-compose.yml`; `shared-llm/.../copilot.py`
- **Cubre (gids)**: db-models-migrations-3/4, api-routers-validation-2/4/6/7, error-obs-logging-6, frontend-admin-panel-2/4/7, workers-orchestrator-6/9, container-isolation-4/5, auth-rbac-casbin-6, llm-providers-4/6/7, mcp-tools-3
- **Tiempo**: 8 h · **Complejidad**: m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_15_a
    runtime: python-pytest
    command: "pytest tests/integration/test_migrations.py -v"
  ```

### Fase E — Tests & CI

#### `task_06_14_16` — Gate CI cross-tenant + cierre de gaps de test

- [ ] **Título**: Marcar tests cross-tenant con `@pytest.mark.cross_tenant` + step CI que los exige; cerrar gaps; evaluar `--cov-fail-under`
- **Archivos**: `.github/workflows/ci.yml`, `pyproject.toml`, `tests/integration/test_{kb_endpoints,migrations,auth_role_helpers}.py`
- **Cubre (gids)**: tests-quality-1/2/5/6
- **Tiempo**: 4 h · **Complejidad**: m
- **Tests automáticos**:
  ```yaml
  - id: auto_06_14_16_a
    runtime: python-pytest
    command: "pytest tests/integration -m cross_tenant -v"
  ```

## Tests humanos del Plan

```yaml
- id: human_06_14_01
  description: "WebSocket rechaza suscripción cross-tenant"
  checklist:
    - "Con JWT de tenant A, abrir /ws/executions/{id} de una execution de tenant B → socket cierra 1008"
    - "Repetir con /ws/kanban/{project_id}, /ws/conversation/{id}, /ws/documents/{id} de tenant B → 1008"
    - "Hacer logout (revoca sesión Redis) con un WS abierto → el socket se cierra y no recibe más eventos"
    - "Caso feliz: WS del propio tenant sigue funcionando (backlog + tail en vivo)"

- id: human_06_14_02
  description: "Arranque seguro y validación de secretos"
  checklist:
    - "Levantar api-server con API_SERVER_ENVIRONMENT=prod y JWT/MinIO/DB en default dev → arranque FALLA con error claro"
    - "Con secretos reales seteados → arranca normal"
    - "Repetir para workers y orchestrator"

- id: human_06_14_03
  description: "Robustez operativa de ejecuciones"
  checklist:
    - "Matar el worker a mitad de una execution → al reintentar NO se duplica la fila de executions"
    - "Forzar fallo del broker en dispatch → la task NO queda 'in_progress' huérfana (vuelve a ready o va a DLQ)"
    - "Cortar la red a mitad de un stream LLM → el error se registra tipado, no rompe el runtime"

- id: human_06_14_04
  description: "Aislamiento de memorias y endpoints"
  checklist:
    - "Intentar fusionar dos memorias project_shared de proyectos distintos → 422"
    - "Borrar (soft-delete) un proyecto y usar un token de agente de ese proyecto → 403/denegado"
    - "Listar agentes con ?limit=10 → devuelve como máximo 10"
```

## Criterios de cierre

1. Todas las tareas con `[x]` y su test automático en verde.
2. Suite completa verde: `pytest tests/unit tests/integration -v` (incluye los nuevos tests de regresión).
3. `pre-commit run --all-files` (black + ruff + mypy strict) y `npm run typecheck && lint && build` en admin-panel.
4. Migración nueva reversible (`alembic upgrade head && alembic downgrade -1`).
5. Step CI cross-tenant activo y exigido.
6. Los 4 tests humanos validados por un humano.
7. Entrada de changelog en `docs/07-changelog/06.14-hardening-auditoria.md`.
8. PR mergeado a `master`.

## Próximo Plan / nota de proceso

- Tras este hardening, las fases de roadmap pendientes (07+, 11, 15) pueden construirse sobre una base
  segura. El motor de guardrails (Plan 11) y httpOnly cookies (Plan 15) recogen lo que aquí queda fuera.
- **Nota de gobernanza (fuera de alcance de código):** al redactar este plan, `docs/roadmap/` tenía
  **3 fases en `status: in_progress` simultáneamente** (06.10, 06.11, 06.12), lo que viola la regla
  dura de `CLAUDE.md` "NUNCA tener dos fases en in_progress a la vez". Por eso este plan se crea en
  `pending_approval` y NO se activa automáticamente: conviene dejar una sola fase activa antes de
  poner 06.14 en `in_progress`.

---

## Anexo A — Catálogo completo de hallazgos confirmados (68)

Cada hallazgo lleva su `gid` (trazable al run de auditoría), severidad corregida tras verificación adversarial, `fichero:línea`, problema, fix propuesto y la **tarea** que lo cubre. Entradas 🚫 = reales pero fuera del alcance de ejecución de este plan (se documentan por completitud). Entrada ℹ️ = confirmación positiva (el código está bien), incluida para trazabilidad.

### CRÍTICO (5)

#### `auth-rbac-casbin-3` · CRÍTICO · multi-tenancy → **task_06_14_01**

- **Qué**: WebSocket endpoints do not validate tenant membership; users can subscribe to any execution/project/conversation
- **Dónde**: `apps/api-server/src/api_server/routers/ws.py:122-187`
- **Problema**: The four WebSocket endpoints (/ws/executions/{id}, /ws/kanban/{project_id}, /ws/conversation/{id}, /ws/documents/{id}) accept only a JWT and do not verify that the authenticated user is a member of the requested resource's tenant or has access to that specific resource. The `_authenticate` function validates only the JWT signature and expiry, not the user's rights. For example, a user from Tenant A could connect to `/ws/executions/{some-id}` where the execution belongs to Tenant B, as long as they have a valid JWT.
- **Fix**: Modify `_authenticate` to decode the JWT and extract tenant_id and user_id from claims. In each WebSocket handler, before calling `_pump()`, decode the JWT token to extract claims, then load the resource (Execution, Project, Conversation, Document) from the database using a tenant-scoped session (or a direct query that filters by tenant_id and the path parameter ID). If the resource is not found or doesn't match the authenticated user's tenant_id, close the socket with 403 (forbidden). Implement this pattern: (1) decode JWT in \_authenticate and return both the boolean and the claims dict, or create a separate \_get_user_tenant function; (2) us…
- **Verificación (skeptic)**: The WebSocket endpoints in ws.py (lines 122-187) have a critical multi-tenancy flaw. The `_authenticate` function (lines 62-73) calls `decode_jwt(token)` but does not capture or use the returned claims, meaning it never extracts or validates the `tenant_id` fr…

#### `multi-tenancy-rls-1` · CRÍTICO · multi-tenancy → **task_06_14_02**

- **Qué**: Worker execution tasks bypass RLS without setting tenant context
- **Dónde**: `apps/workers/src/workers/execution.py:218-227, 282-303`
- **Problema**: The conduct_execution function creates AsyncSessions with session.begin() but never calls set_config('app.tenant_id', ...) before executing database queries. The worker uses a BYPASSRLS role (migrations_user from DATABASE_URL). This means when \_load_project() and subsequent queries execute on lines 226, 290-291, they will bypass RLS policies entirely and can read rows from any tenant. While explicit tenant_id is passed in ExecutionRequest, there's no validation that the provided task_id actually belongs to the stated tenant_id.
- **Fix**: After line 218 (and again at line 282) in `apps/workers/src/workers/execution.py`, add: `await session.execute(text("SELECT set_config('app.tenant_id', :tid, true)"), {'tid': str(request.tenant_id)})`. Additionally, add explicit validation after loading the task: verify `task.tenant_id == UUID(request.tenant_id)` before proceeding, raising an exception if they mismatch. For defense-in-depth, consider signing the ExecutionRequest payload in the orchestrator and verifying it in the worker to prevent Celery message tampering. Switch to the non-BYPASSRLS role if possible, or apply FORCE ROW LEVEL SECURITY at the role level so BYPASSRLS cannot be…
- **Verificación (skeptic)**: The worker and orchestrator both connect using the `migrations_user` BYPASSRLS role (workers/config.py line 31, orchestrator/config.py line 47-51). Neither sets `app.tenant_id` before database operations. RLS policies (e.g., executions table in migration 20260…

#### `secrets-config-1` · CRÍTICO · security → **task_06_14_03**

- **Qué**: Dev-only JWT secret not validated at startup for production environments
- **Dónde**: `apps/api-server/src/api_server/config.py:37-40`
- **Problema**: The JWT_SECRET has a dev-only default value 'dev-only-jwt-secret-change-me'. There is no validation at startup to prevent production deployments from using this weak default secret. If an operator forgets to set API_SERVER_JWT_SECRET in production, all JWTs will be signed with a publicly known key, allowing trivial token forgery.
- **Fix**: Add @model_validator(mode='after') to Settings class that raises ValueError if environment != 'dev' and any of [jwt_secret, review_url_signing_secret, minio_secret_key] match their hardcoded dev defaults. Example: if self.environment != 'dev' and self.jwt_secret.get_secret_value() == 'dev-only-jwt-secret-change-me': raise ValueError('In production, API_SERVER_JWT_SECRET must not use the dev default'). Alternatively, make these fields required (default=None) when environment != 'dev' and raise if None is passed.
- **Verificación (skeptic)**: The code at config.py lines 37-40 defines jwt_secret with a hardcoded dev-only default. No @model_validator, field_validator, or post-init enforcement exists in the Settings class (read full file lines 17-162). The comment at lines 48-50 explicitly claims "the…

#### `ws-sse-realtime-1` · CRÍTICO · multi-tenancy → **task_06_14_01**

- **Qué**: Multi-tenancy authorization bypass: WebSocket endpoints do not validate tenant ownership of resources
- **Dónde**: `apps/api-server/src/api_server/routers/ws.py:62-73, 122-168`
- **Problema**: All four WebSocket endpoints (/ws/executions/{execution_id}, /ws/conversation/{conversation_id}, /ws/kanban/{project_id}, /ws/documents/{document_id}) authenticate the JWT token for signature validity but do NOT verify that the requested resource belongs to the authenticated user's tenant. The \_authenticate function decodes the JWT, discards all claims including tenant_id, and returns a boolean. A user from Tenant A can subscribe to any execution, conversation, project, or document from Tenant B if they know the ID, receiving all real-time even…
- **Fix**: Modify \_authenticate to return both the boolean and the decoded claims (or at least tenant_id). Then modify each WebSocket endpoint handler to: (1) Extract tenant_id from the decoded JWT claims returned by \_authenticate. (2) Load the resource from the database using get_tenant_session (which applies RLS filtering). For example, in execution_stream: await session.get(Execution, execution_id) will return None if the execution doesn't belong to the authenticated user's tenant (RLS enforces this). (3) If the resource is None, close the WebSocket with code 1008 and message "resource not found" or "access denied". (4) Only after confirming resource…
- **Verificación (skeptic)**: I read the actual ws.py code (lines 62-73, 122-186) and confirmed: (1) \_authenticate() calls decode_jwt(token) on line 69 but does NOT capture the return value—the JWT claims (including tenant_id/tid) are discarded. (2) All four endpoint handlers accept resour…

#### `ws-sse-realtime-3` · CRÍTICO · multi-tenancy → **task_06_14_01**

- **Qué**: No database authorization checks for resource ownership before streaming
- **Dónde**: `apps/api-server/src/api_server/routers/ws.py:122-187`
- **Problema**: All WebSocket endpoint handlers (execution_stream, conversation_stream, kanban_stream, document_stream) delegate to \_pump without performing any database lookups to verify that the resource belongs to the authenticated user's tenant. Unlike REST endpoints (e.g., GET /conversations/{id} in conversations.py:176-184) which use get_tenant_session for RLS-enforced lookups, WebSocket handlers skip the database entirely and trust only the path parameter. This creates an authorization gap.
- **Fix**: Add database verification in each WebSocket endpoint before calling \_pump. For execution_stream (lines 122-134): After \_authenticate succeeds, decode JWT to extract claims, get tenant_id from claims["tid"], create sessionmaker() context with set_config("app.user_id") and set_config("app.tenant_id"), then query: execution = await session.get(Execution, UUID(execution_id)). If None, close with 1008. Same pattern for conversation_stream (query Conversation), document_stream (query Document), and kanban_stream (query Project). This ensures RLS-filtered cross-tenant access is rejected before Redis stream is accessed.
- **Verificación (skeptic)**: The WebSocket endpoints (execution_stream, conversation_stream, document_stream, kanban_stream) in apps/api-server/src/api_server/routers/ws.py at lines 122-187 authenticate the JWT via \_authenticate() but then proceed directly to \_pump() which reads from Redi…

### ALTO (18)

#### `auth-rbac-casbin-1` · ALTO · correctness → **task_06_14_01**

- **Qué**: WebSocket connections do not validate Redis session revocation
- **Dónde**: `apps/api-server/src/api_server/routers/ws.py:62-73`
- **Problema**: The `_authenticate` function in WebSocket endpoints only calls `decode_jwt(token)` but never validates that the JWT's session_id still exists in Redis. In contrast, the REST `get_principal` dependency (used by all other endpoints) validates the session at line 142 of deps.py with `if not await sessions.get(session_id)`. A revoked session (via logout) is therefore still valid for WebSocket connections until the JWT expires, allowing the client to continue streaming events even after explicit logout.
- **Fix**: Modify `_authenticate` to accept a SessionStore parameter and validate session revocation before returning True. After decoding JWT, extract the `sid` claim, call `await sessions.get(session_id)`, and close with 1008 if the session is not found. Alternatively, refactor WebSocket endpoints to use FastAPI's dependency injection with `get_principal` (or a WebSocket-adapted variant) to ensure every authentication point includes Redis session validation. For the long-term pattern, consider: (a) extract session_id from the decoded JWT token in \_authenticate, (b) pass SessionStore to \_authenticate via Depends(), (c) validate `if not await sessions.g…
- **Verificación (skeptic)**: The finding is legitimate and represents a real security vulnerability. Code inspection confirms: (1) ws.py `_authenticate` (lines 62-73) calls only `decode_jwt(token)` with NO Redis session validation; (2) deps.py `get_principal` (line 142) explicitly validat…

#### `auth-rbac-casbin-5` · ALTO · correctness → **task_06_14_09**

- **Qué**: Agent token (internal_agent.py) does not validate agent still has access to its project
- **Dónde**: `apps/api-server/src/api_server/auth/internal_agent.py:106-189`
- **Problema**: The `get_agent_principal` dependency validates that the agent exists and is not soft-deleted (via `_agent_exists` at line 172-189), but does not verify that the agent still has a valid relationship to its project or that the project itself is not deleted. If a project is soft-deleted while an agent token is still valid (24-hour TTL), the agent can continue to call internal endpoints and may access resources through the deleted project.
- **Fix**: In `_agent_exists` or `get_agent_principal`, additionally verify that if the agent has a project*id, the corresponding Project row exists and is not soft-deleted: `Project.deleted_at.is*(None)`. This ensures a deleted project revokes agent access immediately.
- **Verificación (skeptic)**: The finding is accurate. In apps/api-server/src/api_server/auth/internal_agent.py, the `_agent_exists` function (lines 181-189) validates that an agent exists and is not soft-deleted, but does NOT verify that the agent's associated project (if any) exists and…

#### `error-obs-logging-1` · ALTO · robustness → **task_06_14_10**

- **Qué**: Request context never bound to logs despite middleware infrastructure
- **Dónde**: `apps/api-server/src/api_server/logging/context.py:1-38`
- **Problema**: The logging module provides bind_request_context() and clear_request_context() functions and the docstring (line 3) states they are called by FastAPI middleware. However, no middleware exists in the codebase - bind_request_context is never invoked anywhere. This means request_id, user_id, tenant_id, and project_id are never bound to structured logs via contextvars, breaking request traceability and making it impossible to correlate logs across async boundaries.
- **Fix**: Create a FastAPI middleware that calls bind_request_context() at request start with the principal's user_id/tenant_id/project_id (extracted from get_principal dependency), and clear_request_context() on request exit. Ensure the middleware runs AFTER routing but BEFORE user code executes, so all downstream logs inherit the context.
- **Verificación (skeptic)**: After auditing the codebase, I confirmed that bind_request_context() and clear_request_context() functions are defined in apps/api-server/src/api_server/logging/context.py and re-exported in logging/**init**.py, but they are NEVER invoked anywhere in the codeb…

#### `guardrails-1` · ALTO · correctness → **task_06_14_07**

- **Qué**: Missing pre_tool hook for allowed_tools enforcement in chat modes
- **Dónde**: `docker/agent-runtimes/agent-runtime/agent_runtime/tools.py:42-51`
- **Problema**: The ChatModeConfig in api_server declares allowed_tools that 'are enforced in the pre_tool guardrail layer' (apps/api-server/src/api_server/chat/modes.py line 59-60 and apps/api-server/src/api_server/db/custom_chat_mode.py line 71), but the ToolRegistry.call() method has NO guardrail hook that validates tool calls against an allowed list. The agent loop bypasses this enforcement entirely.
- **Fix**: 1. Add allowed_tools to the AGENT_TASK_SPEC pipeline: pass current_mode.allowed_tools through ExecutionRequest -> \_agent_spec(), then through **main**.py to AgentDeps or AgentState. 2. Store allowed_tools on AgentTask (or a new field on AgentState) so the loop has access to the whitelist. 3. In ToolRegistry.call(), before invoking the tool function, check if name is in the allowed list (if set). Return ToolResult(ok=False, error=f"tool '{name}' not allowed in this mode") if blocked. 4. Fix the comment in modes.py line 59 from "enforces" (present) to "will enforce" or remove it until Plan 11 is complete. 5. Add a test that verifies a tool not…
- **Verificación (skeptic)**: The finding is confirmed with one important clarification: allowed_tools ARE defined in ChatModeConfig and stored in custom_chat_mode rows, but they are NEVER passed to the agent-runtime task spec and NEVER enforced by ToolRegistry.call(). Evidence: 1. modes.p…

#### `guardrails-2` · ALTO · correctness → **🚫 fuera de alcance → Plan 11**

- **Qué**: Declared pre_llm/post_llm hook points are completely unimplemented
- **Dónde**: `CLAUDE.md:31`
- **Problema**: The project specification (CLAUDE.md §10) explicitly declares 'Guardrails declarativos por capas ... en cuatro puntos del ciclo: pre_llm, post_llm, pre_tool, post_tool.' However, the agent-runtime code has NO implementation of pre_llm or post_llm hooks. This means input validation (prompt injection, jailbreak) and output validation (content safety, hallucination) are completely absent from the execution path.
- **Fix**: Design and implement pre_llm and post_llm hook architecture in the agent-runtime. Pre_llm should sanitize/validate the state dict before passing to the model. Post_llm should validate ModelResponse against rules (e.g., no leaked secrets, no disallowed output structure). The hooks should fail-closed: any validation error aborts the execution with a guardrail-specific abort code.
- **Verificación (skeptic)**: The finding is CONFIRMED. The pre_llm and post_llm hook points are declared as a NON-NEGOTIABLE principle in CLAUDE.md (line 31) and detailed in the guardrails roadmap (Plan 11). However, the agent-runtime code shows NO implementation of these hooks at any poi…

#### `multi-tenancy-rls-3` · ALTO · multi-tenancy → **task_06_14_02**

- **Qué**: Worker maintenance and memorizer tasks create sessions without tenant context
- **Dónde**: `apps/workers/src/workers/maintenance.py, apps/workers/src/workers/memorizer.py, apps/workers/src/workers/ingestion.py:maintenance.py line 88-90, memorizer.py line 103-108, ingestion.py line 127-137`
- **Problema**: Multiple Celery tasks (maintenance sweeps, memorizer LLM distillation, document ingestion) create AsyncSessions from create_async_engine(settings.database_url) which use the BYPASSRLS migrations_user role, but none of them call set_config to establish tenant context before querying. These background tasks may read/write across tenants without realizing it, especially if they accept tenant_id as a parameter but don't explicitly set the GUC.
- **Fix**: For each background task that receives tenant_id context, set the GUC at session start: `await session.execute(text("SELECT set_config('app.tenant_id', :tid, true)"), {'tid': str(tenant_id)})`. For tasks that don't have tenant context, either: (a) require tenant_id as a parameter, (b) restrict operations to a single well-known tenant, or (c) switch to the NOBYPASSRLS role which forces RLS filtering.
- **Verificación (skeptic)**: I read all three worker files and traced the actual code paths. The finding is technically confirmed but requires important context: WHAT I FOUND: 1. Memorizer (memorizer.py lines 103-107): Creates a BYPASSRLS engine and calls `get_execution()` which executes…

#### `multi-tenancy-rls-5` · ALTO · correctness → **task_06_14_02**

- **Qué**: No validation that background task parameters match database row ownership
- **Dónde**: `apps/workers/src/workers/execution.py:71-99, 217-224`
- **Problema**: ExecutionRequest carries tenant_id, task_id, and agent_id as strings from the orchestrator (via Celery). The conduct_execution function never validates that the task_id actually belongs to the stated tenant_id before using it. If the Celery broker is compromised or an internal component mints a malicious request, a cross-tenant task could be executed. While the orchestrator creates ExecutionRequest correctly, there's no defensive check at the worker boundary.
- **Fix**: In conduct_execution (after line 217), add explicit tenant validation before proceeding: `python async def conduct_execution( request: ExecutionRequest, *, settings: Settings, sessionmaker: async_sessionmaker[AsyncSession], redis: Redis, ) -> ExecutionOutcome: """Run one task end to end: container → Redis stream → `executions` row.""" task_id = UUID(request.task_id) tenant_id = UUID(request.tenant_id) agent_id = UUID(request.agent_id) if request.agent_id else None async with sessionmaker() as session, session.begin(): # CRITICAL: Validate that task_id belongs to tenant_id before proceeding. # The worker uses BYPASSRLS so RLS cannot protect us…
- **Verificación (skeptic)**: The finding is confirmed. The worker's conduct_execution function accepts an ExecutionRequest from Celery (an external, potentially compromisable message broker) and never validates that the task_id actually belongs to the stated tenant_id before operating on…

#### `rag-memory-ingestion-2` · ALTO · multi-tenancy → **task_06_14_08**

- **Qué**: Memory merge allows cross-owner-pointer merge within same scope
- **Dónde**: `apps/api-server/src/api_server/routers/memories.py:407-411`
- **Problema**: The merge_memory_into endpoint checks that source and target have the same scope (line 407) but does NOT verify that they have the same owner pointer (user_id, team_id, or project_id). This violates memory scope isolation. For example, two 'project_shared' memories with different project_ids can be merged together, causing content from one project's memory to leak into another project's memory. The database CHECK constraint only validates scope-to-pointer consistency, not uniqueness of the owner pointer within a scope.
- **Fix**: Add explicit owner-pointer validation in the merge endpoint after loading src and tgt (after line 406). Example fix: `python # After line 406, before line 407 if src.scope == 'project_shared' and src.project_id != tgt.project_id: raise HTTPException( status_code=status.HTTP_422_UNPROCESSABLE_ENTITY, detail="cannot merge project_shared memories across different projects" ) if src.scope == 'team_shared' and src.team_id != tgt.team_id: raise HTTPException( status_code=status.HTTP_422_UNPROCESSABLE_ENTITY, detail="cannot merge team_shared memories across different teams" ) if src.scope == 'private' and src.user_id != tgt.user_id: raise HTTPExcept…
- **Verificación (skeptic)**: The finding is confirmed. Here's the precise vulnerability: 1. **RLS scope is tenant-only**: Migration 0020 line 181-186 establishes an RLS policy that filters memory_entries by `tenant_id` ONLY. This means any row from the same tenant passes RLS, regardless o…

#### `secrets-config-3` · ALTO · security → **task_06_14_03**

- **Qué**: MinIO credentials use weak dev default without production validation
- **Dónde**: `apps/api-server/src/api_server/config.py:91-98`
- **Problema**: MinIO credentials default to 'minioadmin' / 'changeme-dev-only'. There is no validation at startup to ensure these are overridden in production. If an operator forgets to set MINIO_ROOT_PASSWORD in production, all uploaded documents (user data) are accessible with the default credentials.
- **Fix**: Add a field_validator to the Settings class in apps/api-server/src/api_server/config.py: `python from pydantic import field_validator class Settings(BaseSettings): # ... existing fields ... @field_validator('minio_secret_key', mode='after') @classmethod def validate_minio_secret_production(cls, v: SecretStr, info) -> SecretStr: """Reject weak dev defaults in non-dev environments.""" if info.data.get('environment') != 'dev': secret_value = v.get_secret_value() if secret_value == 'changeme-dev-only': raise ValueError( 'MinIO secret must be overridden in production. ' 'Set API_SERVER_MINIO_SECRET_KEY to a strong secret.' ) return v ` Alternative…
- **Verificación (skeptic)**: The finding is confirmed. Code inspection shows: 1. Line 91-98 of apps/api-server/src/api_server/config.py contains exactly what was reported: `minio_access_key: str = Field(default="minioadmin", ...)` and `minio_secret_key: SecretStr = Field(default=SecretStr…

#### `secrets-config-7` · ALTO · security → **task_06_14_03**

- **Qué**: No environment-specific secret validation at application startup
- **Dónde**: `apps/api-server/src/api_server/config.py:17-156`
- **Problema**: The Settings class has no @model_validator that enforces production-safe values when environment != 'dev'. This means a misconfigured production deployment could silently use all dev defaults (weak JWT secret, default MinIO credentials, default DB password) without any warning.
- **Fix**: Add a @model_validator(mode='after') method to Settings class in apps/api-server/src/api_server/config.py (and equivalent in apps/orchestrator/src/orchestrator/config.py and apps/workers/src/workers/config.py). The validator should check: if self.environment in ['staging', 'prod'], then raise ValueError with specific messages for each secret if it matches the dev default pattern. Example for api-server: `python from pydantic import model_validator class Settings(BaseSettings): # ... fields ... @model_validator(mode='after') def validate_production_secrets(self) -> Settings: """Prevent production deployments from using dev defaults.""" if self…
- **Verificación (skeptic)**: I read the complete Settings class in apps/api-server/src/api_server/config.py (lines 17-156) and confirmed there are NO validators (@model_validator or @field_validator decorators) that enforce environment-specific secret validation. The comment on line 49-50…

#### `workers-orchestrator-1` · ALTO · correctness → **task_06_14_04**

- **Qué**: run_execution task lacks idempotency protection on retry
- **Dónde**: `apps/workers/src/workers/execution.py:209-225`
- **Problema**: The run_execution Celery task calls create_running_execution() unconditionally every time it runs. If the task is retried (worker crash, timeout, or visibility timeout before ACK), a new executions row is created for the same task_id, creating duplicate execution records. The task lacks acks_late idempotency check or deduplication logic. Since task_acks_late=True is configured, a worker crash during conduct_execution() will cause the task to be re-queued, leading to duplicate rows.
- **Fix**: Before calling create_running_execution(), query for an existing execution row with the same tenant_id + task_id. If one exists in RUNNING state, reuse it instead of creating a new one. Alternatively, add a unique constraint (tenant_id, task_id, status) and use upsert semantics, or add a Celery task ID to make the task truly idempotent.
- **Verificación (skeptic)**: The `run_execution` Celery task in apps/workers/src/workers/tasks.py (line 51) calls `conduct_execution()` which unconditionally calls `create_running_execution()` (apps/workers/src/workers/execution.py lines 219-225) every time it runs. With `task_acks_late=T…

#### `workers-orchestrator-10` · ALTO · robustness → **task_06_14_04**

- **Qué**: No soft_time_limit or task timeout defined for long-running agent tasks
- **Dónde**: `apps/workers/src/workers/tasks.py:51-60`
- **Problema**: The run_execution task has no Celery task timeout or soft_time_limit configured. Without a timeout, if conduct_execution() hangs (e.g., container startup failure not detected, network hang), the task will block the worker indefinitely. The container_run_timeout_s (600s default, workers/config.py line 88-92) only applies to the container, not the task's Celery wrapper. A hung task will prevent the worker from processing other tasks.
- **Fix**: 1) Add timeout to task decorator: @app.task(name="workers.run_execution", bind=True, soft_time_limit=900, time_limit=1200) on tasks.py:51. 2) Wrap asyncio.to_thread: container_result = await asyncio.wait_for(asyncio.to_thread(runner.run_streamed, container_spec, on_line), timeout=900) on execution.py:269. 3) Add Docker timeout: docker.from_env(timeout=30) on container.py:96. Apply same pattern to other tasks (run_agent_container, run_test_runtime, compose_review_runtime).</parameter> </StructuredOutput>
- **Verificación (skeptic)**: The finding is accurate and real. The run_execution Celery task in tasks.py (lines 51-60) has no time_limit or soft_time_limit configured. The Celery app in celery_app.py (lines 45-89) has no global timeout defaults. The container_run_timeout_s applies only to…

#### `workers-orchestrator-2` · ALTO · robustness → **task_06_14_04**

- **Qué**: No task retry configuration or dead-letter queue for Celery tasks
- **Dónde**: `apps/workers/src/workers/celery_app.py:56-88`
- **Problema**: The Celery app configuration sets task_acks_late=True but does not configure any retry policy (no default_retry_delay, max_retries, autoretry_for, or retry_backoff). Failed tasks will be ACKed and lost without automatic retry. Heavy/long-running agent tasks that timeout or encounter transient broker issues will simply fail and disappear. There is also no dead-letter queue or error handler defined for tasks that exhaust retries.
- **Fix**: Add Celery task retry configuration at app level in celery_app.py lines 56-88: add `task_default_retry_delay=60, task_max_retries=3` (or values matched to SLA). For the critical `run_execution` task specifically, add task-level retry with exponential backoff: `@app.task(name="workers.run_execution", autoretry_for=(Exception,), retry_backoff=True, retry_backoff_max=600, retry_jitter=True, max_retries=3)`. Additionally: (1) add an `on_failure` callback or signal handler to log failed task details to a dead-letter channel (Redis stream or DB row) for observability; (2) document the retry strategy in comments explaining the balance between re-exe…
- **Verificación (skeptic)**: The finding accurately identifies a real gap in task resilience. Code inspection confirms: (1) celery_app.py lines 56-88 set task_acks_late=True but lack any retry configuration (no task_default_retry_delay, task_max_retries, autoretry_for, or retry_backoff);…

#### `workers-orchestrator-4` · ALTO · robustness → **task_06_14_05**

- **Qué**: Orchestrator consumer swallows handler errors without dead-letter queue
- **Dónde**: `apps/orchestrator/src/orchestrator/consumer.py:145-165`
- **Problema**: The StreamConsumer.\_dispatch() method catches ALL exceptions from the event handler (line 154-164). When a handler raises, the event is ACKed anyway (line 133), meaning the event is lost forever. If TaskDispatcher fails to dispatch a task (e.g., agent assignment fails, broker is down, DB is unavailable), the task event is silently ACKed and never retried. There is no dead-letter stream or alert mechanism.
- **Fix**: Implement a dead-letter mechanism: (1) Before ACKing a failed event, push it to a `events:tasks:deadletter` stream with error reason and timestamp; (2) Create a new Celery task `orchestrator.replay_dead_letter` that operators can invoke to replay a dead-letter entry after the underlying issue (e.g., broker restored) is fixed; (3) Add a GET `/orchestrator/dead-letters` endpoint to list failed events with their error details; (4) Optionally: add automatic replay attempts with exponential backoff before permanently dead-lettering. Alternatively, move the Celery send_task call INSIDE the transaction so failures cause the entire \_dispatch() to rai…
- **Verificación (skeptic)**: The finding is confirmed. I verified the exact code flow: 1. **Unconditional ACK**: consumer.py line 133 executes `await self._redis.xack(...)` unconditionally after calling `_dispatch()` at line 132, regardless of whether the handler succeeds or fails. 2. \*\*H…

#### `workers-orchestrator-8` · ALTO · data-integrity → **task_06_14_05**

- **Qué**: Orchestrator dispatch sets task.assigned_agent_id but transaction may not commit
- **Dónde**: `apps/orchestrator/src/orchestrator/dispatch.py:98-140`
- **Problema**: The \_dispatch() method modifies task.assigned_agent_id (line 124) and task.status = 'in_progress' (line 123) within a transaction context that it enters (line 102) but never explicitly commits (the context manager auto-commits on exit at line 108, but only if no exception raised before Celery task enqueue). If the send_task() call on line 87 raises an exception, the transaction may be rolled back but the worker already considers the task dispatched. This creates a lost-update anomaly.
- **Fix**: Move send_task() inside the transaction context so that if it fails, the task status change is rolled back. Better approach: use a transactional outbox pattern - write a separate outbox row inside the transaction, then have a background job retry sending messages from the outbox. This decouples DB writes from broker reliability. Example: (1) Inside \_dispatch() transaction, write {task_id, agent_id, ...} to an outbox table with status='pending'. (2) Set task to in_progress only after outbox write succeeds (commit). (3) Add a periodic sweep task (e.g., every 30s) that queries outbox rows with status='pending' and retries send_task(). (4) Mark o…
- **Verificación (skeptic)**: The finding is accurate. The transaction in \_dispatch() commits before send_task() is called. Specifically: (1) Lines 102-139 in dispatch.py show the DB transaction wrapping only the task status update; the context manager auto-commits at line 139 when \_dispat…

#### `ws-sse-realtime-2` · ALTO · correctness → **task_06_14_01**

- **Qué**: JWT tenant_id claim decoded but immediately discarded in authentication
- **Dónde**: `apps/api-server/src/api_server/routers/ws.py:62-73`
- **Problema**: The \_authenticate function at lines 62-73 calls decode_jwt(token) but does not store or use the return value. The claims dict (containing 'tid', 'sub', 'sid') is computed but thrown away. This means the function has no access to the tenant_id even though it's embedded in the JWT and is the source of truth for the user's tenant scope.
- **Fix**: Change \_authenticate to extract and return claims (not just bool). Then in each endpoint, extract tenant_id from claims['tid'], and verify the resource (via DB query with RLS) belongs to that tenant before calling \_pump. Example: claims = decode_jwt(token); verify that Execution/Project/Conversation/Document row with the given ID belongs to the principal's tenant_id (either via RLS query or explicit WHERE clause), then proceed. If verification fails, close the socket with code 403. This mirrors the pattern in get_principal (line 112 in auth/deps.py) which properly validates all claims.
- **Verificación (skeptic)**: The finding is technically and materially correct. Line 69 in apps/api-server/src/api_server/routers/ws.py calls decode_jwt(token) but discards the return value, which contains the 'tid' (tenant_id) claim. All four WebSocket endpoints (/ws/executions/{id}, /ws…

#### `ws-sse-realtime-4` · ALTO · multi-tenancy → **task_06_14_01**

- **Qué**: Kanban stream lacks both tenant_id and project_id access control validation
- **Dónde**: `apps/api-server/src/api_server/routers/ws.py:136-148, 105-111`
- **Problema**: The kanban_stream endpoint streams from a global EVENTS_STREAM and filters by project_id. However: (1) the project_id is not validated to belong to the user's tenant, (2) events are filtered only by project_id in the \_pump function (line 109), never by tenant_id. An attacker knowing a project_id from any tenant can subscribe to watch all task transitions in that project, even if the project belongs to another tenant. Events in the stream include tenant_id field but it is never checked.
- **Fix**: Three parallel fixes: (1) Modify \_authenticate to return the decoded JWT claims containing tenant_id, not just a boolean. (2) In kanban_stream, after accepting the WebSocket, use get_tenant_session (or a WebSocket-adapted variant that doesn't require FastAPI's session context) to load the Project with tenant scoping via get(Project, UUID(project_id)); if None or if project.tenant_id != principal.tenant_id, close the socket with code 1008 and reason 'project not found or access denied'. (3) Modify \_pump to accept tenant_id parameter and add a second filter condition: if project_filter is not None and (event.get('project_id') != project_filter…
- **Verificación (skeptic)**: The kanban_stream endpoint at C:\laragon\python\agent-ai-multitenant\apps\api-server\src\api_server\routers\ws.py lines 136-148 accepts a project_id parameter but performs ONLY JWT token validation (decode_jwt on line 69), not project access control. The \_auth…

#### `ws-sse-realtime-5` · ALTO · multi-tenancy → **task_06_14_01**

- **Qué**: WebSocket session does not use PostgreSQL RLS for fine-grained access control
- **Dónde**: `apps/api-server/src/api_server/routers/ws.py:122-187`
- **Problema**: WebSocket endpoints do not use get_tenant_session dependency, which means they bypass PostgreSQL RLS policies entirely. While get_tenant_session (deps.py:185-227) sets app.tenant_id and app.user_id for the session, enabling RLS to filter all queries, WebSocket handlers never create a session. They read from Redis directly, which has no tenant awareness. This violates the platform's core multi-tenancy principle that all data access goes through RLS.
- **Fix**: 1. Add `get_tenant_session` dependency to each WebSocket endpoint to establish RLS context: `python async def execution_stream( ws: WebSocket, execution_id: str, token: str \| None = Query(default=None), principal: AuthPrincipal = Depends(get_principal), # Extract tenant_id session: AsyncSession = Depends(get_tenant_session), # Enable RLS redis: Redis = Depends(get_redis), ) -> None: ` 2. Before subscribing to the Redis stream, verify the resource exists and belongs to the user's tenant by doing a single RLS-filtered database query: `python execution = await session.get(Execution, UUID(execution_id)) if execution is None: await ws.close(code=…
- **Verificación (skeptic)**: The finding is CONFIRMED. I verified the following: 1. **WebSocket handlers bypass tenant authorization checks:** The code at /ws.py:122-187 has four WebSocket endpoints (execution_stream, kanban_stream, conversation_stream, document_stream) that: - Accept a J…

### MEDIO (19)

#### `api-routers-validation-3` · MEDIO · performance → **task_06_14_13**

- **Qué**: Unbounded list endpoints lack pagination limits
- **Dónde**: `apps/api-server/src/api_server/routers/agents.py:79-106`
- **Problema**: The GET /agents endpoint returns all matching agents without a limit parameter. If a tenant has thousands of agents, the response could be very large. Projects (line 56) and tasks have similar unbounded list endpoints.
- **Fix**: Add pagination support to the `list_agents` endpoint: `python @router.get("", response*model=list[AgentResponse]) async def list_agents( scope: AgentScope \| None = Query(default=None, description="Filter by scope"), project_id: UUID \| None = Query(default=None, description="Filter by project_id"), role: str \| None = Query(default=None, description="Filter by role"), agent_type: str \| None = Query(default=None, description="Filter by agent_type (ai\|human)"), limit: int = Query( default=100, ge=1, le=500, description="Max agents returned. Use smaller values for typeahead/comboboxes." ), *: AuthPrincipal = Depends(require_tenant_member), se…
- **Verificación (skeptic)**: The reported finding is accurate. I examined the code at C:\laragon\python\agent-ai-multitenant\apps\api-server\src\api_server\routers\agents.py lines 79-106 and confirmed: 1. The `list_agents` endpoint accepts optional filter parameters (scope, project_id, ro…

#### `container-isolation-1` · MEDIO · robustness → **task_06_14_11**

- **Qué**: Auxiliary services (postgres-test, redis-test) launched without security hardening
- **Dónde**: `apps/workers/src/workers/test_runtime.py:397-417`
- **Problema**: The \_start_aux_services method launches PostgreSQL and Redis test containers without any security hardening (cap_drop, read_only, security_opt, resource limits). While these are transient test services on an internal bridge, they run with elevated privileges by default and can exhaust host resources without bounds, violating the hardened isolation principle stated in CLAUDE.md §2.
- **Fix**: Extend AuxServiceSpec to include optional resource limits (mem_limit_mb, pids_limit, cpu_percent). In \_start_aux_services, apply cap_drop=['ALL'], security_opt=['no-new-privileges:true'], read_only=True (for aux-only writable paths via tmpfs or mounts if needed), and resource limits. Set reasonable defaults: mem_limit='256m', pids_limit=128, cpu=0.5 (matching or slightly below the expected test-runtime requirements). Note: read_only=True may need per-service tmpfs exemptions (e.g., postgresql requires /var/lib/postgresql/data writable). Consider a helper similar to \_build_test_kwargs for aux containers, or parameterize the launch call to reus…
- **Verificación (skeptic)**: Code inspection confirms the problem: \_start_aux_services (lines 405-413) launches PostgreSQL and Redis test containers with only detach, environment, network, hostname, and labels—no cap_drop, read_only, security_opt, mem_limit, pids_limit, or nano_cpus. By c…

#### `container-isolation-2` · MEDIO · robustness → **task_06_14_11**

- **Qué**: DinD proxy container launched without memory and process limits
- **Dónde**: `apps/workers/src/workers/test_runtime.py:441-472`
- **Problema**: The \_start_dind_proxy method launches the docker-socket-proxy sidecar with cap_drop and read_only set, but without mem_limit or pids_limit. This allows a misconfigured test or malicious testcontainer code to exhaust host memory/processes, potentially affecting other tasks on the same worker.
- **Fix**: Apply memory and pids limits to both \_start_dind_proxy and \_start_aux_services containers. For the proxy: use conservative defaults (e.g., mem_limit='128m', pids_limit=64) since it's a thin proxy not running user code. For aux services: add mem_limit and pids_limit fields to AuxServiceSpec with reasonable defaults (e.g., postgres: mem_limit='256m', redis: mem_limit='128m', both pids_limit=128), allowing project override via spec.aux_services. Propagate these from TestRuntimeSpec or Settings to both containers.run() calls.
- **Verificación (skeptic)**: The code analysis confirms the finding. The \_start_dind_proxy method (lines 454-472) launches the docker-socket-proxy sidecar without mem_limit or pids_limit, unlike the main test container which has both (lines 584-585). The main container uses settings-drive…

#### `container-isolation-3` · MEDIO · robustness → **task_06_14_11**

- **Qué**: MinIO service in docker-compose.yml uses unpinned :latest image tag
- **Dónde**: `docker/docker-compose.yml:88`
- **Problema**: The MinIO service uses image: minio/minio:latest in the production docker-compose, which violates the requirement to pin images for reproducibility and security. An unexpected image pull (e.g., due to Docker cache eviction or explicit pull) could run a different version or a compromised build.
- **Fix**: Pin MinIO to a specific version matching the release cadence of other services in the compose file. Recommended: use 'minio/minio:2024.11.17' or the current RELEASE.<date> format MinIO uses. This maintains consistency with postgres:16, redis:7-alpine, and hashicorp/vault:1.17 pinning strategy and prevents unexpected version pulls that could cause CI flakiness or compatibility issues during local development.
- **Verificación (skeptic)**: The finding is accurate: line 88 of docker/docker-compose.yml uses 'minio/minio:latest' while all other infrastructure services are pinned to specific versions (postgres:16, redis:7-alpine, hashicorp/vault:1.17, clamav/clamav:1.4). However, context is importan…

#### `error-obs-logging-3` · MEDIO · security → **task_06_14_10**

- **Qué**: SQLAlchemy internal error details exposed to client
- **Dónde**: `apps/api-server/src/api_server/routers/knowledge_bases.py:126-131`
- **Problema**: When an IntegrityError is caught on KB creation, line 130 returns detail=f'kb name already exists in tenant: {exc.orig}'. The exc.orig attribute is an internal SQLAlchemy detail (the original database error) that may contain implementation specifics about the constraint. While this specific case is relatively safe, it exposes SQLAlchemy internals to the client.
- **Fix**: Apply the pattern from kb_categories.py (lines 115-126): (1) Query the database proactively to check for duplicate KB name within the tenant before attempting insert. (2) If found, raise HTTPException with a human-friendly message like "kb name already exists in this tenant". (3) Only catch IntegrityError as a fallback for race conditions / unexpected constraint violations, and respond with a generic 409 message like "kb name already exists in this tenant" (no exc.orig). Additionally, log the full IntegrityError server-side for debugging. Apply the same pattern consistently across conversations.py, projects.py, plans.py, tasks.py, and knowled…
- **Verificación (skeptic)**: The finding is CONFIRMED as a real (though low-severity) issue. Analysis: WHAT I FOUND: 1. Line 130 in knowledge_bases.py exposes exc.orig: `detail=f'kb name already exists in tenant: {exc.orig}'` 2. In SQLAlchemy with asyncpg+PostgreSQL, exc.orig is the under…

#### `error-obs-logging-5` · MEDIO · robustness → **task_06_14_10**

- **Qué**: No global exception handler for unhandled route exceptions
- **Dónde**: `apps/api-server/src/api_server/main.py:73-233`
- **Problema**: The FastAPI application has no global exception handler registered (@app.exception_handler). This means any unhandled exception in a route (e.g., AttributeError, TypeError, database error not wrapped in HTTPException) will bubble up to FastAPI's default handler, which logs it via uvicorn. While this is somewhat safe (uvicorn logs to stderr), it bypasses the structured JSON logging pipeline and doesn't include request context (request_id, tenant_id, user_id) even if context binding were fixed, since the exception happens outside the logging midd…
- **Fix**: Implement a request-context-binding middleware that must run early in the stack (before other middleware). The middleware should: (1) Extract request_id from X-Request-ID header or generate one; (2) After calling get_principal in dependencies, call bind_request_context(request_id=..., user_id=..., tenant_id=...); (3) Ensure the binding happens for EVERY request before any endpoint code runs. Additionally OR alternatively: register @app.exception_handler(Exception) that logs via structlog (which will inherit the bound context if the middleware is in place). Without the middleware, the exception handler still won't have context. The middleware…
- **Verificación (skeptic)**: Code audit confirms the finding is technically accurate but nuanced. I verified: (1) No global @app.exception_handler(Exception) exists in apps/api-server/src/api_server/main.py - CONFIRMED by searching main.py lines 73-233 and entire api_server directory with…

#### `frontend-admin-panel-1` · MEDIO · security → **🚫 fuera de alcance → Plan 15 (httpOnly cookies)**

- **Qué**: JWT Token Stored in localStorage (XSS Vector)
- **Dónde**: `apps/admin-panel/lib/auth.ts:11-20`
- **Problema**: The JWT authentication token is stored in localStorage where it is vulnerable to XSS attacks. Any malicious script injected into the page can access localStorage and steal the token. This violates the principle that sensitive authentication tokens should never be accessible to JavaScript.
- **Fix**: Migrate JWT token storage to httpOnly cookies as documented in Phase 15 roadmap. httpOnly cookies are inaccessible to JavaScript, preventing XSS-based token theft. Implement this via API response headers (Set-Cookie) rather than client-side localStorage. Until then, implement Content Security Policy (CSP) to mitigate XSS risk and sanitize all user inputs.
- **Verificación (skeptic)**: The reported finding accurately describes the current implementation: JWTs are stored in localStorage in Phase 0, making them vulnerable to XSS attacks. The code comment explicitly acknowledges this is temporary ("Phase 0 keeps the JWT in localStorage. Phase 1…

#### `frontend-admin-panel-5` · MEDIO · security → **🚫 fuera de alcance → Plan 15 (parte authz va en task_06_14_01)**

- **Qué**: JWT Token Exposure in WebSocket URL Query Parameter
- **Dónde**: `apps/admin-panel/lib/ws.ts:17-24`
- **Problema**: The JWT token is appended to the WebSocket URL as a query parameter (`?token=<jwt>`). This exposes the token in browser history, server logs, and any network debugging tools. Query parameters are not as secure as Authorization headers (which cannot be transmitted over WebSocket due to protocol limitations, but could be handled via a connection upgrade flow or token negotiation).
- **Fix**: Phase 0 limitation: (1) Immediate mitigations: ensure JWT expiration is short (recommend 15min max); audit logging configuration to verify mask_pii_processor is active in all environments. (2) CRITICAL: implement resource ownership validation before shipping—add authorization checks in WebSocket handlers to verify the JWT's tenant_id matches the resource's tenant, and user has access to the specific execution/project/conversation/document. (3) Phase 15: migrate token from localStorage to httpOnly cookies and remove ?token from URL. The suggested token-negotiation fix is impractical (WebSocket protocol limitation); focus instead on authorizati…
- **Verificación (skeptic)**: The JWT token IS appended to WebSocket URLs as a query parameter (apps/admin-panel/lib/ws.ts:18-24), making it visible in browser dev tools, network tabs, and potentially server logs. This is acknowledged in ADR 0015 as an intentional trade-off due to RFC 6455…

#### `frontend-admin-panel-8` · MEDIO · robustness → **task_06_14_14**

- **Qué**: No Error Boundary for Admin Shell Error Recovery
- **Dónde**: `apps/admin-panel/app/admin/layout.tsx:1-44`
- **Problema**: The admin shell layout (auth gate, sidebar, header) lacks an Error Boundary component. If any child component throws an uncaught error, the entire admin UI will crash to a blank/white screen, leaving the user unable to navigate or recover. This is particularly problematic for multi-tenant systems where a single tenant's data loading error could take down the entire administrative interface.
- **Fix**: Implement a client-side Error Boundary component that catches rendering errors from child pages and displays a recoverable error state. For Next.js 14 App Router with "use client" in admin/layout.tsx, the pattern is: (1) Create ErrorBoundary component using class-based React.Component with componentDidCatch; (2) Wrap AdminShell with it: `&lt;ErrorBoundary&gt;&lt;AdminShell&gt;{children}&lt;/AdminShell&gt;&lt;/ErrorBoundary&gt;`; (3) The boundary should display 'Something went wrong' with a Refresh/Go Back button; (4) For TenantProvider query errors specifically, also wrap with QueryErrorResetBoundary from @tanstack/react-query. Note: error.ts…
- **Verificación (skeptic)**: The admin layout (apps/admin-panel/app/admin/layout.tsx) is marked "use client" and wraps children with TenantProvider, LanguageProvider, and AdminShell without any error boundary. When I examined the codebase: 1. No error.tsx files exist in app/ or app/admin/…

#### `guardrails-4` · MEDIO · correctness → **🚫 fuera de alcance → Plan 11**

- **Qué**: Approval gate covers only tool categories, not output content — post_tool hook missing
- **Dónde**: `docker/agent-runtimes/agent-runtime/agent_runtime/approval.py:45-64`
- **Problema**: The ApprovalGate currently gates TOOL NAMES (mapping shell_exec -> code_execution) in the plan node before the tool runs (pre_tool behavior). However, there is NO post_tool hook that inspects the tool's OUTPUT. A tool might return sensitive data (credit card numbers, API keys) that should be caught after execution, not just at decision time. The specification for Plan 11 (guardrails phase) includes post_tool guardrails (task 11_02) but they are not implemented.
- **Fix**: The corrected fix should acknowledge this is deferred to Plan 11 Phase A (task_11_01 + task_11_02). For now, the mitigation is: (1) pre_tool approval gates the most sensitive actions, (2) in Phase 2, tools return only placeholder/safe outputs (echo, noop), and (3) when real tools land (Phase D+), the post_tool guardrails engine must be wired into the observe node or a new node between act and observe. The hook should inspect ToolResult.output for sensitive patterns and route to redaction, escalation, or blocking based on the project's guardrail config (passed through task spec like approval_policy is now). Do NOT treat this as a critical hole…
- **Verificación (skeptic)**: Code review confirms the finding is accurate. The ApprovalGate class (approval.py lines 45-64) only implements pre_tool review, checking tool NAMES against categories. The act() node (graph.py lines 259-281) executes tools and records results without any post-…

#### `llm-providers-1` · MEDIO · correctness → **task_06_14_06**

- **Qué**: Protocol stream() signature mismatch — async implementation vs non-async definition
- **Dónde**: `packages/shared-llm/src/shared_llm/base.py:43-52`
- **Problema**: The LLMProvider Protocol defines stream() as a regular (non-async) method returning AsyncIterator[StreamChunk], but all four provider implementations (CopilotProvider, AzureFoundryAPIMProvider, OllamaProvider, ClaudeAgentProvider) define it as `async def stream()`. This violates the Protocol contract and creates type safety issues. While Python's runtime duck-typing allows this to work in practice, it defeats the purpose of the Protocol as a type-checkable interface and will cause type checking failures.
- **Fix**: Change the Protocol definition in base.py line 43 from `def stream(...)` to `async def stream(...)` to align with ADR 0021 and match all implementations. This is the correct fix because: (1) it matches the specification, (2) it matches all implementations, and (3) semantically, an async iterator generator that must be awaited-before-iteration doesn't exist—calling `async def` yields the generator directly with no intermediate coroutine layer, so this is the only sensible contract.
- **Verificación (skeptic)**: The finding is technically accurate: the Protocol definition in base.py line 43 defines `def stream(...)` as a non-async method, while all four provider implementations (copilot.py:245, azure_foundry.py:112, ollama.py:129, claude_agent.py:181) define `async de…

#### `llm-providers-2` · MEDIO · correctness → **task_06_14_06**

- **Qué**: CopilotProvider.stream() lacks JWT 401 retry logic present in complete()
- **Dónde**: `packages/shared-llm/src/shared_llm/providers/copilot.py:245-279`
- **Problema**: The CopilotProvider.complete() method (lines 207-243) includes logic to handle JWT expiration: if a 401 is received, it clears the cached JWT and retries once. However, the stream() method (lines 245-279) does not have this retry logic. If a JWT expires exactly between a stream() call's initialization and the first chunk, the stream will fail with a 401 AuthError instead of transparently reminting and retrying. This asymmetry violates the principle of consistent error handling between the two methods.
- **Fix**: Wrap the stream() call in a try-except to catch and retry on 401. Before entering the context manager, attempt the stream(); if AuthError with 401 occurs, clear self.\_jwt = None and retry once. Alternatively, check resp.status_code == 401 immediately after entering the context manager (before calling check_status or iterating lines) and retry the entire stream operation. The simpler approach: add a retry wrapper around the entire async context manager block to match complete()'s pattern. Example: `for attempt in range(2): try: async with self.\_client.stream(...) as resp: if resp.status_code == 401 and attempt == 0: self.\_jwt = None; continue;…
- **Verificación (skeptic)**: The code shows a clear asymmetry between complete() and stream() in JWT 401 retry handling. In complete() at lines 235-241, a 401 response triggers JWT cache clearing and automatic retry. In stream() at lines 245-279, check_status() is called at line 272 insid…

#### `llm-providers-3` · MEDIO · robustness → **task_06_14_06**

- **Qué**: No error handling during streaming iteration in three providers
- **Dónde**: `packages/shared-llm/src/shared_llm/providers/copilot.py, azure_foundry.py, ollama.py:copilot.py:272-279, azure_foundry.py:134-141, ollama.py:156-163`
- **Problema**: The stream() methods in CopilotProvider, AzureFoundryAPIMProvider, and OllamaProvider perform check_status() to validate the response before starting iteration, but have no try/except around the iteration loop itself (async for line in resp.aiter_lines()). Network interruptions, timeouts, or provider errors during streaming (e.g., the server closes the connection mid-stream) will raise uncaught exceptions that are not wrapped in the standard LLM error types (ProviderError, RateLimitError, AuthError). Only ClaudeAgentProvider has try/except arou…
- **Fix**: Wrap the async for loop in each of CopilotProvider.stream(), AzureFoundryAPIMProvider.stream(), and OllamaProvider.stream() with try/except, following the pattern in ClaudeAgentProvider.stream() (lines 200-222). For each provider, add try/except around the iteration block and convert exceptions to ProviderError: try: async for line in resp.aiter_lines(): ... except Exception as exc: raise ProviderError(str(exc)) from exc. This ensures all three providers consistently convert network/streaming errors to the typed LLMError hierarchy, matching ClaudeAgentProvider and maintaining the contract with callers.
- **Verificación (skeptic)**: After reading the actual code: (1) check_status() in all three providers (copilot.py:272, azure_foundry.py:134, ollama.py:156) validates the initial response status BEFORE entering the iteration loop. (2) The async for loop at lines 273, 135, 157 respectively…

#### `llm-providers-5` · MEDIO · correctness → **task_06_14_06**

- **Qué**: Missing error type discrimination in \_openai_compat.check_status()
- **Dónde**: `packages/shared-llm/src/shared_llm/providers/_openai_compat.py:38-49`
- **Problema**: The check_status() function checks for 401/403 (AuthError), 429 (RateLimitError), and >=400 (ProviderError) in that order. However, the logic on line 40 groups 401 and 403 together without distinction. HTTP 403 (Forbidden) is semantically different from 401 (Unauthorized) — 403 typically means the credential is valid but lacks permission, while 401 means the credential itself is invalid/expired. Treating both as AuthError conflates two different failure modes and may cause incorrect retry logic (e.g., a 403 should not trigger JWT remint in Copi…
- **Fix**: Split line 40 into two separate checks: (1) `if resp.status_code == 401: raise AuthError(f"{provider}: unauthorized ({resp.status_code}) {resp.text}")` to handle invalid/expired credentials, and (2) move `resp.status_code == 403` to after the 429 check, raising `ProviderError` instead (since 403 is a permission/authorization issue, not an authentication failure): `if resp.status_code == 403: raise ProviderError(f"{provider}: forbidden ({resp.status_code}) {resp.text}", status_code=resp.status_code, raw=resp.text)`. This preserves Copilot's correct retry behavior (401 intercepted before check_status) while fixing the semantic classification an…
- **Verificación (skeptic)**: The reported finding is technically correct: line 40 of _openai_compat.py groups HTTP 401 (Unauthorized) and 403 (Forbidden) together as AuthError without distinction. I verified this by reading the actual code at packages/shared-llm/src/shared_llm/providers/_…

#### `mcp-tools-1` · MEDIO · correctness → **task_06_14_12**

- **Qué**: No JSON schema validation of MCP tool arguments
- **Dónde**: `docker/agent-runtimes/agent-runtime/agent_runtime/graph.py:259-264`
- **Problema**: Tool arguments passed from the LLM to MCP tools are never validated against the tool's input_schema. The act() node extracts tool_args from the LLM's decision and calls registry.call(tool, args) without any schema checking. While the Tool table stores input_schema and MCP servers advertise inputSchema, the execution path has no defensive validation.
- **Fix**: Before calling tools.call(tool, args), validate args against the tool's declared input_schema using a JSON schema library (jsonschema). This protects against malformed arguments that could cause tool errors or unexpected behavior.
- **Verificación (skeptic)**: The finding is confirmed. Reading the actual code in graph.py line 264, the act() node extracts tool_args from the LLM decision and calls registry.call(tool, args) without any validation. I traced through the execution path: 1. graph.py line 264: `result = sel…

#### `mcp-tools-2` · MEDIO · robustness → **task_06_14_12**

- **Qué**: Tool result output not size-limited before returning to LLM
- **Dónde**: `docker/agent-runtimes/agent-runtime/agent_runtime/mcp_tools.py:283-301`
- **Problema**: When an MCP tool returns large or malicious output, it is concatenated and returned verbatim to the agent loop without size limits. The output is then folded into the observation dict and fed back to the LLM in the next planning cycle. No documented maximum output size exists, which could exhaust context windows or cause unexpected LLM behavior.
- **Fix**: Implement a size limit for MCP tool output (e.g. 64KB per tool result) with truncation and a marker. Apply this at the MCP tool wrapper layer (\_make_tool_fn in mcp_tools.py after line 298-300) before returning the ToolResult, similar to shell_exec's \_truncate function. Add configuration via MCPServerConfig.max_output_bytes (default 65536) so tool limits can be tuned per server. Truncated output should include '[output truncated at N bytes]' so the agent and model know data was omitted.
- **Verificación (skeptic)**: The finding is technically accurate. MCP tool output flows untruncated through: (1) mcp_tools.py line 301 returns full output in ToolResult, (2) graph.py line 278 stores it in last_observation, (3) observe node line 287 adds full observation to context, (4) pr…

#### `secrets-config-2` · MEDIO · security → **task_06_14_03**

- **Qué**: Review URL signing secret has weak dev default with no production validation
- **Dónde**: `apps/api-server/src/api_server/config.py:44-54`
- **Problema**: The review_url_signing_secret is documented to require '32+ byte secret' in production (line 48), but the default 'dev-only-review-url-secret-change-me' is only 28 bytes and there is no startup validation preventing production use. An attacker can forge review session URLs if this weak secret is used in production.
- **Fix**: The suggested fix has the right approach but wrong technical detail. Corrected fix: (1) Remove or correct the misleading comment (line 49-50 doesn't match implementation). (2) Add a @model_validator to Settings that raises ValueError if environment in ('prod', 'staging') and review_url_signing_secret.get_secret_value() == "dev-only-review-url-secret-change-me", with message requiring API_SERVER_REVIEW_URL_SIGNING_SECRET to be set. (3) Drop the "32+ byte" requirement check (36 bytes already exceeds it) unless there's a specific deployment policy requiring longer keys. Example: `@model_validator(mode='after') def validate_secrets(self) -> Setti…
- **Verificación (skeptic)**: Reading config.py lines 44-54, the comment at lines 48-50 explicitly states: "Production deployments MUST set this to a random 32+ byte secret; the default is rejected outside of `environment=dev`." However, examining the entire config.py file and all related…

#### `secrets-config-5` · MEDIO · security → **task_06_14_03**

- **Qué**: PostgreSQL connection URLs contain hardcoded dev passwords in defaults
- **Dónde**: `apps/api-server/src/api_server/config.py:20-34`
- **Problema**: Database URLs are hardcoded with dev passwords in default values: 'changeme-app-dev-only' and 'changeme-migrations-dev-only'. While these are only defaults and should be overridden via env vars, the presence in source code could leak if config.py is accidentally printed or logged.
- **Fix**: Add explicit startup validation in api_server/main.py and other services: check if environment="prod" and any database_url contains "changeme", then raise ValueError with clear message. Alternatively, make database_url and admin_database_url REQUIRED (no defaults) in production by using Pydantic validators that check settings.environment and raise if credentials are unset/default when environment != "dev".
- **Verificación (skeptic)**: The hardcoded dev passwords in defaults at C:\laragon\python\agent-ai-multitenant\apps\api-server\src\api_server\config.py lines 20-34 are REAL. However, the actual severity is lower than claimed because: (1) pydantic-settings will use environment variables pr…

#### `tests-quality-6` · MEDIO · correctness → **task_06_14_16**

- **Qué**: CI pipeline does not explicitly gate on cross-tenant isolation tests or RLS policy verification
- **Dónde**: `.github/workflows/ci.yml:201-213`
- **Problema**: The CI runs 'pytest tests/integration -v' which includes cross-tenant and RLS tests, but there is no explicit gating or marker (e.g., pytest.mark.cross_tenant) that forces CI to fail if those specific test categories are skipped or fail. If a developer accidentally disables cross-tenant tests (or they break), CI would not catch it unless the overall pytest run fails. Per CLAUDE.md, cross-tenant tests are mandatory.
- **Fix**: Add a separate CI step that runs 'pytest tests/integration -m cross_tenant -v' (after marking appropriate tests with @pytest.mark.cross_tenant). Make that step required to pass before merge. Alternatively, add a test that counts the number of cross_tenant tests found and asserts it >= 15 (current count is 17).
- **Verificación (skeptic)**: The finding is CONFIRMED. The CI pipeline at .github/workflows/ci.yml line 210 runs `pytest tests/integration -v` with no explicit marker filtering for cross-tenant tests. Under pytest 8.2+ (specified in pyproject.toml), skipped tests do not cause CI failure.…

### BAJO (26)

#### `api-routers-validation-2` · BAJO · correctness → **task_06_14_15**

- **Qué**: Unvalidated dict payload bypasses schema validation in grant_kb_to_agent
- **Dónde**: `apps/api-server/src/api_server/routers/agents.py:516-522`
- **Problema**: The POST /agents/{agent_id}/knowledge-bases endpoint accepts a raw `dict[str, str]` payload instead of a Pydantic schema. This allows: (1) mass-assignment of unexpected fields, (2) bypassing type validation, and (3) inconsistent error messages compared to the rest of the API.
- **Fix**: Create a Pydantic schema `class GrantKBRequest(BaseModel): kb_id: UUID` and use it as the parameter type. This enables automatic type coercion, standardized error responses, and OpenAPI documentation.
- **Verificación (skeptic)**: The endpoint at agents.py:516-522 does accept a raw dict[str, str] instead of a Pydantic schema like the analogous KB-project grant endpoint in knowledge_bases.py:264-300. However, upon code inspection and test validation: 1. MANUAL VALIDATION PRESENT: Lines 5…

#### `api-routers-validation-4` · BAJO · correctness → **task_06_14_15**

- **Qué**: Query parameter passed directly to database filter without type validation
- **Dónde**: `apps/api-server/src/api_server/routers/projects.py:58, 93-94`
- **Problema**: The `status_` query parameter (aliased from `status`) is a string without validation. It is then passed directly to `Project.status == status_` without checking if it is a valid ProjectStatus enum value. This allows invalid status values to reach the database query.
- **Fix**: Change line 58 from `status_: str \| None = Query(default=None, alias="status")` to `status_: ProjectStatus \| None = Query(default=None, alias="status")` and import ProjectStatus at the top if not already imported. FastAPI/Pydantic will then automatically validate the query parameter against the enum values and return a 422 error for invalid values. This makes the API consistent with the POST/PUT endpoints which already validate status as an enum. Alternatively, if lenient filtering is desired, add an explicit description to the Query parameter documenting that invalid values are silently ignored.
- **Verificación (skeptic)**: The finding is CONFIRMED but with important caveats. The code at lines 58 and 93-94 of /apps/api-server/src/api*server/routers/projects.py does indeed accept the `status*` query parameter as a raw string without type validation against the ProjectStatus enum.…

#### `api-routers-validation-6` · BAJO · correctness → **task_06_14_15**

- **Qué**: Unvalidated JSON payload fields without schema validation
- **Dónde**: `apps/api-server/src/api_server/schemas/projects.py:64-68, 98-102`
- **Problema**: Project creation and update accept `rag_knowledge_bases`, `worker_config`, `repository_config`, and `human_approval_policy` as arbitrary `dict[str, Any]` without shape validation. While `mcp_servers` has a validator, the others have no schema enforcement. This allows clients to send malformed JSON that may cause runtime errors later when orchestrators consume these configs.
- **Fix**: Add field validators for the other JSON fields to ensure they conform to expected schemas. For example: `@field_validator('worker_config', mode='after')` with a schema check, or document the expected structure and add runtime validation in the orchestrators.

#### `api-routers-validation-7` · BAJO · correctness → **task_06_14_15**

- **Qué**: Password validation too permissive in LoginRequest
- **Dónde**: `apps/api-server/src/api_server/schemas/auth.py:20-24`
- **Problema**: The LoginRequest password field allows `min_length=1`, which is much weaker than the RegisterRequest which enforces `min_length=8`. A user could theoretically log in with a 1-character password if they somehow registered with it (though registration blocks that). The inconsistency creates a validation gap.
- **Fix**: Change LoginRequest password to `Field(min_length=8, max_length=128)` for consistency, or document why the login endpoint allows weaker validation (e.g., for backward compat with legacy short passwords).

#### `auth-rbac-casbin-6` · BAJO · correctness → **task_06_14_15**

- **Qué**: Tenant admin promotion logic in registration has a potential TOCTOU race on first-user check
- **Dónde**: `apps/api-server/src/api_server/routers/auth.py:73-110`
- **Problema**: The register endpoint checks if the users table is empty (line 89) to auto-promote the first user to system_admin. Although the code comment (lines 82-85) claims the check and insert run inside the same transaction to prevent a race producing two superadmins, the check uses `User.id.limit(1)` (line 89) on an unordered SELECT, which is technically read-only and does not acquire a write lock. If two register requests arrive simultaneously and both see an empty table before either write, both could be promoted. PostgreSQL transaction isolation sho…
- **Fix**: Add explicit lock during the first-user check, e.g., `select(User.id).with_for_update(skip_locked=True).limit(1)` or restructure to rely solely on database constraints. Alternatively, document the assumption that PostgreSQL's default isolation level prevents this race.

#### `container-isolation-4` · BAJO · robustness → **task_06_14_15**

- **Qué**: Docling service in docker-compose.yml uses unpinned :latest image tag
- **Dónde**: `docker/docker-compose.yml:167`
- **Problema**: The docling-serve service uses image: ghcr.io/docling-project/docling-serve:latest. While docling is an optional document parser (Plan 04 task_04_10) and not on the hot path, unpinned images still pose a reproducibility and supply-chain risk.
- **Fix**: Pin docling-serve to a specific version, e.g., ghcr.io/docling-project/docling-serve:v1.0.0 or the latest released version. Update .env.example to document the expected tag.

#### `container-isolation-5` · BAJO · robustness → **task_06_14_15**

- **Qué**: Auxiliary services lack individual timeout enforcement
- **Dónde**: `apps/workers/src/workers/test_runtime.py:397-437`
- **Problema**: The auxiliary services (postgres-test, redis-test) have no timeout mechanism if they hang during startup. The \_wait_healthy method polls with a 30-second healthcheck_timeout_s default, but if a service fails to become healthy, the entire task may stall waiting indefinitely in edge cases (e.g., if healthcheck_cmd is None or the service never responds).
- **Fix**: Add an overall startup timeout per AuxServiceSpec (default e.g., 60 seconds) and kill the container if it is not healthy by then. Alternatively, document that projects must set a healthcheck_cmd for any aux service, and fail fast if one is missing.

#### `db-models-migrations-3` · BAJO · performance → **task_06_14_15**

- **Qué**: Missing Foreign Key Index on projects.team_id
- **Dónde**: `apps/api-server/migrations/versions/20260521_0002_domain_minimum.py:383-437`
- **Problema**: `projects` table has a foreign key on `team_id` (line 395-398) but no index on that column. When a team is deleted, PostgreSQL must scan the entire projects table to find children. Also, queries like 'find all projects for team X' will be slow. This is a standard FK indexing gap.
- **Fix**: Add `CREATE INDEX ix_projects_team_id ON projects(team_id);` in a follow-up migration.
- **Verificación (skeptic)**: The finding is confirmed: projects.team_id lacks a dedicated index, though the FK constraint exists (migration 0002, line 395-398). The ORM model (domain.py lines 538-554) defines only two indices, neither covering team_id. When a team is soft-deleted, Postgre…

#### `db-models-migrations-4` · BAJO · performance → **task_06_14_15**

- **Qué**: Missing Foreign Key Index on review_sessions.plan_id
- **Dónde**: `apps/api-server/migrations/versions/20260528_0024_review_sessions.py:44-160`
- **Problema**: `review_sessions` has a foreign key on `plan_id` (line 99-102) but only creates indexes on tenant_id, expires_at, last_activity_at, and plan_id separately (line 139). While plan_id is indexed, the pattern is inconsistent: other tables (e.g., documents in migration 0022) index the FK together with related columns for common query patterns.
- **Fix**: This is lower priority than the missing indexes above; consider a composite index `(plan_id, status)` for the common query pattern in a future migration.

#### `error-obs-logging-6` · BAJO · security → **task_06_14_15**

- **Qué**: Error details in health check exposed to clients
- **Dónde**: `apps/api-server/src/api_server/routers/admin.py:250-307`
- **Problema**: The /admin/system-health endpoint (accessible to system admins only) returns truncated error messages from failed service probes in the detail field. While this is appropriate for internal health checks, if error messages contain sensitive information (e.g., a postgres error mentioning a specific schema, vault error revealing the vault URL structure, clamav path), they could leak via logs or monitoring systems that consume this endpoint.
- **Fix**: For /admin/system-health, return generic messages like 'connection failed' or 'timeout' without the exception message. Log the full exception server-side for debugging. Alternatively, only return messages approved by a whitelist mapping (e.g., 'timeout' → HTTP 504, 'auth failed' → HTTP 401, etc.) to prevent information disclosure.

#### `frontend-admin-panel-2` · BAJO · correctness → **task_06_14_15**

- **Qué**: Broad Query Cache Invalidation on Tenant Switch
- **Dónde**: `apps/admin-panel/lib/tenant-context.tsx:80-89`
- **Problema**: When a user switches tenants via `setTenantId()`, the code calls `queryClient.invalidateQueries()` with no query key filter, invalidating ALL cached queries. This is overly aggressive and can cause unnecessary re-fetches of unrelated data (e.g., global settings, platform catalogs that do not depend on tenant). It also triggers a full cache wipe even for the auth/me query which itself contains tenant-aware data and should be refetched anyway.
- **Fix**: Replace `queryClient.invalidateQueries()` on line 86 with a filtered call: `queryClient.invalidateQueries({ predicate: (query) => !query.queryKey.some(key => ['auth', 'admin', 'system-health'].includes(key as string)) })` or more simply, replace with queries that directly target tenant-scoped queries: `queryClient.invalidateQueries({ predicate: (query) => query.queryKey.some(key => ['projects', 'teams', 'agents', 'tenant-settings', 'tenant-data'].includes(key as string)) })`. Alternatively, adopt a consistent query key namespace for tenant-scoped queries (e.g., all tenant queries use first key element 'tenant') and filter on that pattern. The…
- **Verificación (skeptic)**: The reported finding is technically correct: line 86 of tenant-context.tsx does call `queryClient.invalidateQueries()` without query key filters, which per TanStack Query documentation invalidates ALL cached queries. Analysis of actual queries in the codebase…

#### `frontend-admin-panel-4` · BAJO · correctness → **task_06_14_15**

- **Qué**: Stale Closure in Chat Composer Mention Parsing
- **Dónde**: `apps/admin-panel/app/admin/projects/[id]/chat/page.tsx:540-563`
- **Problema**: The `parsePendingMention()` function is called on every render with the current `value` state, but the suggestion list filtering and pick logic operate on stale closures. If `value` changes rapidly (user typing fast), the rendered suggestions may not match the current text in the input because React's rendering is async.
- **Fix**: Use `useRef` to store the current value and update it in the `onChange` handler, so `pickMention` always operates on fresh state. Alternatively, pass the current `value` as a parameter to the click handler instead of relying on closure: `onClick={() => pickMention(role, value)}`.

#### `frontend-admin-panel-7` · BAJO · correctness → **task_06_14_15**

- **Qué**: Unhandled Promise in Approval Resolution
- **Dónde**: `apps/admin-panel/app/admin/approvals/page.tsx:98-102`
- **Problema**: The `onSuccess` callback in the approval resolve mutation returns `queryClient.invalidateQueries(...)` without being awaited, which returns a Promise. While TanStack Query handles this gracefully, the pattern is inconsistent and could mask errors if the invalidation fails.
- **Fix**: Either await the invalidation or use `setQueryData` for immediate cache updates: `onSuccess: () => { setError(null); queryClient.setQueryData(['approvals'], (prev) => prev?.filter(r => r.id !== request.id)); }`. If keeping the invalidation, optionally await: `onSuccess: async () => { setError(null); await queryClient.invalidateQueries({ queryKey: ['approvals'] }); }`.

#### `guardrails-5` · BAJO · correctness → **🚫 fuera de alcance → Plan 11**

- **Qué**: Guardrail configuration layer (platform/tenant/project) declared but no implementation path exists
- **Dónde**: `docs/roadmap/11-guardrails-precios.md:51-52`
- **Problema**: The specification (Plan 11 task 11_02) requires 'Configuración por capas (plataforma → tenant → proyecto) con campos lockable', implying platform-level guardrails set defaults, tenant can override some, project can override further, with platform being able to lock fields. However, the current code has NO infrastructure to pass layered guardrail config from the API server (where platform/tenant/project settings live) to the agent runtime (sandboxed container). The ExecutionRequest dataclass only carries approval_policy, not a guardrails configu…
- **Fix**: This is not a fix needed for current code — it is a specification requirement for Plan 11 implementation. When implementing task_11_02 ("Configuración por capas"), the suggested fix in the finding is sound: (1) Add guardrails_config field to ExecutionRequest; (2) Before worker invocation, fetch platform_settings[guardrails], tenant_settings[guardrails], project.guardrails_config; (3) Merge respecting lock_status on each field; (4) Pass merged config in AGENT_TASK_SPEC; (5) Document merge precedence. The suggested fix does not have errors — it is simply not yet required to implement because the feature is blocked (status: pending_approval in t…
- **Verificación (skeptic)**: The finding correctly identifies that no guardrail configuration layer infrastructure exists in the production code path. Reading the actual code confirms: 1. ExecutionRequest (workers/execution.py lines 61-99) has NO guardrails_config field — only tenant_id,…

#### `llm-providers-4` · BAJO · security → **task_06_14_15**

- **Qué**: ClaudeAgentProvider stores API key in environment variable at runtime
- **Dónde**: `packages/shared-llm/src/shared_llm/providers/claude_agent.py:51-52`
- **Problema**: If an api_key is passed to ClaudeAgentProvider.**init**(), it is written directly to os.environ['ANTHROPIC_API_KEY'] at line 52. This stores the credential in the process environment, which is visible to child processes, logging systems, and debuggers. The design comment at lines 54-57 indicates support for subscription-based users (who do not set ANTHROPIC_API_KEY), but the code mutates the environment for explicit API key cases. This is a security anti-pattern when the key could come from a Vault or configuration system.
- **Fix**: Remove the api_key parameter from ClaudeAgentProvider.**init**() entirely, since it is never used in production and represents dead code with security risk. The SDK and its subscription auth (Pro/Max) work with environment pre-configuration only. If api_key support is genuinely needed in the future, redesign to follow the pattern used by OllamaProvider and AzureFoundryAPIMProvider: store it as a private instance variable and pass it directly to the SDK constructor (if supported) or use it in HTTP headers, never via os.environ mutation.
- **Verificación (skeptic)**: The finding is technically correct but needs important context. The code at line 51-52 in packages/shared-llm/src/shared_llm/providers/claude_agent.py does indeed mutate os.environ['ANTHROPIC_API_KEY'] when an api_key parameter is passed to **init**(). This is…

#### `llm-providers-6` · BAJO · robustness → **task_06_14_15**

- **Qué**: AzureFoundryAPIMProvider does not validate APIM URL format
- **Dónde**: `packages/shared-llm/src/shared_llm/providers/azure_foundry.py:54-56`
- **Problema**: The apim_base_url is accepted as a bare string and simply rstripped('/') at line 54. There is no validation that it is a valid URL, contains the expected APIM endpoint structure, or uses HTTPS. A typo in the configuration (e.g., 'http://wrong.com' or 'https://example.com/typo') will only fail at request time with a confusing HTTP error from the wrong endpoint, not at provider construction time with a clear configuration error.
- **Fix**: Add validation in **init**: parse apim_base_url with urllib.parse.urlparse(), check that scheme is 'https', and validate the host is not empty. Raise ValueError if validation fails. This catches configuration errors early.

#### `llm-providers-7` · BAJO · robustness → **task_06_14_15**

- **Qué**: CopilotProvider JWT refresh margin may not be sufficient for high-latency networks
- **Dónde**: `packages/shared-llm/src/shared_llm/providers/copilot.py:62, 170`
- **Problema**: The JWT refresh margin is hardcoded to 60 seconds (line 62: `_JWT_REFRESH_MARGIN_S = 60.0`). The \_ensure_jwt() method at line 170 checks if the JWT has less than 60 seconds of life left and remints if so. On high-latency networks (e.g., intercontinental, corporate proxies), the token mint request itself could take 10-30 seconds, and if the JWT has exactly 60 seconds when the call starts, there is still a race where the token expires in flight. For robustness, the margin should be larger (e.g., 120s) or configurable.
- **Fix**: Increase \_JWT_REFRESH_MARGIN_S to 120.0 or make it a constructor parameter (default 120.0). Alternatively, after receiving a new JWT with `expires_at`, validate that `expires_at - now() > _JWT_REFRESH_MARGIN_S` and raise AuthError if the token is too short-lived.

#### `mcp-tools-3` · BAJO · data-integrity → **task_06_14_15**

- **Qué**: Unused MCPToolResult.raw field may leak unintended data
- **Dónde**: `packages/shared-mcp/src/shared_mcp/client.py:111`
- **Problema**: The MCPToolResult dataclass stores the full raw MCP response in the 'raw' field (via response.model_dump()). This field is never accessed or used anywhere in the codebase but will be serialized if ToolResult is logged or stored, potentially exposing multimedia content blocks or internal server metadata.
- **Fix**: Either (1) Remove the 'raw' field from MCPToolResult if debugging doesn't require it, or (2) if kept for debugging, document that it contains untrusted server data and ensure it is never logged or persisted without explicit filtering of sensitive fields.

#### `multi-tenancy-rls-4` · BAJO · correctness → **ℹ️ confirmación positiva (sin acción)**

- **Qué**: Internal /internal/agent endpoints properly set tenant context via AgentPrincipal
- **Dónde**: `apps/api-server/src/api_server/auth/internal_agent.py:192-213`
- **Problema**: The agent-runtime sandbox uses get_agent_tenant_session() which correctly calls set_config('app.tenant_id', ...) on line 210 within a transaction. This is the correct pattern and properly isolates agent requests to their tenant. However, note that unlike get_agent_principal, this sets ONLY app.tenant_id without setting app.user_id (line 205 comment explains agents have no user), which is correct since sessions table RLS keys off user_id.
- **Fix**: No fix needed - this is correctly implemented. Use this as the template for fixing worker/orchestrator code.

#### `secrets-config-4` · BAJO · security → **task_06_14_14**

- **Qué**: CORS middleware allows all HTTP methods and headers with credentials enabled
- **Dónde**: `apps/api-server/src/api_server/main.py:82-88`
- **Problema**: The CORS middleware is configured with `allow_methods=['*']`, `allow_headers=['*']`, and `allow_credentials=True`. While CORS origins are restricted via config, the wildcard methods/headers combined with credentials enabled is unnecessarily broad and could allow CSRF-style attacks if origins validation is bypassed or misconfigured.
- **Fix**: Replace wildcards with explicit allow lists for defense-in-depth: allow_methods=['GET', 'POST', 'PUT', 'DELETE', 'PATCH', 'OPTIONS'] (OPTIONS needed for preflight) and allow_headers=['Authorization', 'X-Tenant-Id', 'Content-Type', 'Accept']. This applies least-privilege and prevents amplification if allow_origins is ever misconfigured, though the current origin restriction is the primary protection.
- **Verificación (skeptic)**: The CORS middleware in apps/api-server/src/api_server/main.py (lines 82-88) does use wildcard allow_methods=['*'] and allow_headers=['*'] with allow_credentials=True. However, this is not actually a CSRF vulnerability in the current configuration because: (1)…

#### `tests-quality-1` · BAJO · test-gap → **task_06_14_16**

- **Qué**: KB endpoint tests lack explicit cross-tenant denial verification
- **Dónde**: `tests/integration/test_kb_endpoints.py:1-519`
- **Problema**: The test_kb_endpoints.py file (519 lines) focuses on KB CRUD operations but does not contain explicit tests verifying that Tenant B is denied access to Tenant A's KBs. While cross-tenant KB isolation IS tested in test_agent_kb_grants.py (test_grant_cross_tenant_kb_is_404) and test_builtin_kb_visibility.py, the main KB endpoints test suite should include explicit negative tests for the core KB endpoints themselves (GET /knowledge-bases/{id}, DELETE /knowledge-bases/{id} with cross-tenant IDs).
- **Fix**: Add test_tenant_b_cannot_read_tenant_a_kb() and test_tenant_b_cannot_delete_tenant_a_kb() to test_kb_endpoints.py. Seed two tenants + two KBs, mint tokens for each, and verify that Tenant B's attempt to GET/DELETE Tenant A's KB returns 404 (RLS hiding, not 403).
- **Verificación (skeptic)**: The test_kb_endpoints.py file (verified by reading lines 1-519) indeed lacks explicit cross-tenant denial tests for KB GET and DELETE endpoints. All tests use a single seeded tenant with no attempts to access resources from another tenant's context. The claime…

#### `tests-quality-2` · BAJO · test-gap → **task_06_14_16**

- **Qué**: Migrations reversibility tests only verify structure, not data preservation integrity on round-trip
- **Dónde**: `tests/integration/test_migrations.py:109-160`
- **Problema**: The test_upgrade_downgrade_upgrade_is_idempotent test verifies that tables and RLS policies survive a downgrade→upgrade round-trip, but it doesn't verify that INSERT/UPDATE/DELETE operations in the mid-state (after downgrade) would work correctly if data were present. This is a minor gap: the test confirms schema structure is preserved, but doesn't exercise that the schema accepts the same write patterns after round-trip.
- **Fix**: Enhance test_upgrade_downgrade_upgrade_is_idempotent to INSERT a row after upgrade, verify it survives downgrade+upgrade round-trip by SELECT, and compare field values. This confirms the schema shape is not just present but also compatible with the same application code.

#### `tests-quality-4` · BAJO · robustness → **🚫 mejora de test no priorizada (CI no debe llamar a LLMs reales)**

- **Qué**: LLM provider unit tests use mocks; real HTTP/SDK client behavior under network conditions not tested
- **Dónde**: `tests/integration/test_model_clients.py:1-373`
- **Problema**: The test_model_clients.py integrations tests (372 lines) cover all four LLM providers (Claude SDK, Copilot, Azure, Ollama) but use mocked HTTP transports (httpx.MockTransport) for the three OpenAI-compat providers. These tests verify request/response parsing but do not test resilience to network errors (timeouts, partial reads, retry logic). The Claude SDK adapter is tested with a fake query function, not the real SDK.
- **Fix**: Add tests for timeout/retry paths: create a handler that returns 503 ServiceUnavailable for first N calls, then 200 success, verify the client either retries or fails gracefully. Test partial/corrupted JSON responses. This is lower urgency since the transports are stable in production, but high availability requires these paths.

#### `tests-quality-5` · BAJO · test-gap → **task_06_14_16**

- **Qué**: Unit tests for authorization helpers (auth_role_helpers) do not test edge cases for role matrix
- **Dónde**: `tests/integration/test_auth_role_helpers.py:1-50`
- **Problema**: The auth role helpers (tenant_admin vs tenant_user vs system_admin) are tested in isolation but edge cases in the role matrix (e.g., tenant_user trying to call an endpoint that requires tenant_admin, or system_admin calling a tenant-scoped endpoint without X-Tenant-Id header) are covered sparsely. Some role denial paths may exist in the code that tests don't exercise.
- **Fix**: Add a parametrized test matrix: for each endpoint marked with a role requirement (e.g., @require_role('tenant_admin')), assert that a tenant_user token gets 403 and a system_admin token gets 200 (or appropriate response). Use pytest.mark.parametrize to avoid duplication.

#### `workers-orchestrator-6` · BAJO · correctness → **task_06_14_15**

- **Qué**: Concurrent worktree operations lack synchronization or locking
- **Dónde**: `apps/workers/src/workers/git_repos.py:225-280, 350-401`
- **Problema**: The WorktreeManager class (which manages git worktree creation, sync, and pruning) is not thread-safe or process-safe. If two Celery tasks on the same worker process call wt_mgr.add() for the same task_id concurrently, or if prune_idle() runs while add() or sync_to_head() are executing, git commands will race. Git worktree operations (add, remove, prune) on a shared bare repo require external synchronization.
- **Fix**: The suggested fix is reasonable but should be deferred until Plan 06 integration with production orchestration. At that time, use file-based locking (fcntl.flock on bare_repo_path/.git/HEAD or a sentinel lock file) rather than threading.Lock, since: (1) different worker processes won't share the lock; (2) file-based locking persists across process boundaries and Celery task lifetimes; (3) it mirrors git's own lock file approach. Alternatively, coordinate through a dedicated Redis-backed distributed lock keyed by (repo_path, task_id) that prune_idle() must acquire before iterating worktrees. Add the synchronization BEFORE wiring WorktreeManage…
- **Verificación**: ⚠️ needs_context — The finding correctly identifies that WorktreeManager has no thread/process-level synchronization. However, this is not currently a practical problem in the codebase because: (1) WorktreeManager is NOT used in the production execution path (Plan 02), only in t…

#### `workers-orchestrator-9` · BAJO · robustness → **task_06_14_15**

- **Qué**: Memorizer task enqueue swallows broker errors but logs as warning, not alert
- **Dónde**: `apps/workers/src/workers/memorizer.py:245-266`
- **Problema**: The trigger_memorize() function (called after every execution finishes) catches broker errors and logs them as warnings (line 260-264). If the broker is down or the task queue is unreachable, the Memorizer simply won't run, and the warning may be missed by ops. Since Memorizer is a best-effort fire-and-forget job, this is tolerable, but the log level should be higher to ensure visibility.
- **Fix**: Change the log level from warning to error: '\_log.error("memorizer.enqueue_failed", ...)'. Add an alert annotation if your observability stack supports it (e.g., 'alert=true' in structlog). Document that Memorizer failures are non-fatal but should be investigated.

## Anexo B — Hallazgos refutados por verificación adversarial (30)

Reportados por los finders pero **descartados** tras leer el código real (no requieren acción). Documentados para no re-investigarlos.

- `multi-tenancy-rls-2` — Orchestrator dispatch queries bypass RLS without tenant context  
  ↳ _The finding misunderstands how the orchestrator handles tenant scoping. Critical facts: 1. **Event to \_dispatch flow**: The handle() method (line 78-96) receives the full event but passes ONLY task_id to \_dispatch() (line 82). The event's tenant_id claim is ne…_
- `auth-rbac-casbin-2` — System admin flag (is*system_admin) is cached in JWT and cannot be revoked until expiry  
  ↳ \_The reported finding assumes an endpoint or mechanism exists to change `is_system_admin` status in the running API, but no such endpoint is implemented. Reading `/admin/users` (admin.py:213-232) only returns a list—it does not provide any means to update user…*
- `auth-rbac-casbin-4` — JWT lacks audience (aud) and issuer (iss) claims for defense-in-depth  
  ↳ _The finding describes a risk applicable to multi-service architectures where multiple independent backends share the same JWT secret. This codebase is a single-service deployment: one FastAPI backend (api-server) handles all user authentication, and no other b…_
- `db-models-migrations-1` — RLS Policy Cast Inconsistency in Tenant-Scoped Tables  
  ↳ _The finding correctly identifies a pattern inconsistency between migrations 0023-0025 (which use `tenant_id::text = current_setting(...)`) and the canonical pattern from 0001-0022 (which use `tenant_id = NULLIF(current_setting(...), '')::uuid`). However, both…_
- `db-models-migrations-2` — Missing Foreign Key Indexes on Junction Tables  
  ↳ _The reported finding claims missing FK indexes on agent_skills, agent_tools, and team_members junction tables cause performance degradation when ON DELETE CASCADE executes. However, this problem does not exist in the actual codebase for three reasons: 1. \*\*ON…_
- `db-models-migrations-5` — Missing tenant*id Index on agent_knowledge_bases Junction  
  ↳ \_The reported finding mischaracterizes how the agent_knowledge_bases junction table is actually queried in this codebase. Analysis of the real query patterns in rag/visibility.py and routers/knowledge_bases.py shows: 1. Queries operate on (kb_id, agent_id) pair…*
- `db-models-migrations-6` — RLS Policy Name Collision Risk in Multiple Migrations  
  ↳ _The reported finding claims a "collision risk" where a failed migration upgrade/downgrade will fail with 'policy already exists'. However, this misunderstands PostgreSQL's policy namespacing. **What the code actually does:** - Migration 0023 creates `CREATE PO…_
- `async-correctness-1` — WebSocket task reuse without cancellation creates coroutine leak on exception  
  ↳ _The reported finding claims a "coroutine leak on exception" where the reader task is left in a pending state and never properly handled. However, analysis of the actual code shows this is false. The reader task created at line 92 is indeed reused across loop i…_
- `async-correctness-2` — xread task result() called without error handling after asyncio.wait()  
  ↳ _The auditor claims that xread.result() on line 104 raises an exception that bypasses the outer try-except at lines 114-117. This is incorrect. The entire block from line 93-111 is wrapped in a try statement (line 93: "try:"), with exception handlers at lines 1…_
- `async-correctness-3` — Unguarded shared task state mutation in WebSocket pump coroutine  
  ↳ _The reported concern about unguarded shared task state mutation is partially true in form (xread tasks can be left pending) but does not constitute the correctness problem claimed. Code analysis: Lines 91-104 implement a two-task race: `reader` (ws.receive() f…_
- `async-correctness-4` — Fire-and-forget Memorizer task without exception handling  
  ↳ _The finding mischaracterizes the async-correctness risk. While `trigger_memorize(execution_id, result.status)` on line 315 of execution.py is indeed called synchronously in an async function, Celery's `apply_async()` method (line 256 of memorizer.py) is not a…_
- `async-correctness-5` — Pending asyncio task with long-lived reference in orchestrator loop  
  ↳ _The reported finding conflates proper async cleanup patterns with bugs. Analysis of the actual code shows: 1. **Task lifecycle is correct** (app.py lines 66-81): The task is created, stored in app.state, and cancelled on shutdown using the standard Python asyn…_
- `async-correctness-6` — Database session created and disposed inside thread-spawned coroutine without session isolation  
  ↳ _The finding misunderstands SQLAlchemy 2.x async behavior and the actual code flow. All analysis of the code shows: (1) Every database session is created and closed via `async with sessionmaker() as session:` context managers, ensuring proper cleanup before the…_
- `async-correctness-7` — MinIO client lazy initialization without thread safety in concurrent requests  
  ↳ _The first claim (per-request instance creation defeats the lock) is factually false. The \_build_minio_storage() function is decorated with @lru_cache(maxsize=1) on line 28 of apps/api-server/src/api_server/storage/**init**.py, guaranteeing a single instance pe…_
- `async-correctness-8` — WebSocket reader task lifetime inconsistency creates potential for stuck event loop  
  ↳ _The reported finding claims a task lifetime inconsistency where the reader task remains in an exception state and causes the event loop to hang. After thorough analysis: 1. The `reader` task is created once at line 92 and reused across loop iterations, which i…_
- `guardrails-3` — No fail-closed error handling on guardrail evaluation — unimplemented hooks would silently skip  
  ↳ _This is a prospective finding about unimplemented guardrail hooks that don't yet exist in the codebase. Plan 11 (guardrails) is in status "pending_approval" and contains no implemented code. The auditor is correctly identifying a design risk for when guardrail…_
- `workers-orchestrator-3` — Missing visibility timeout and broker*transport_options for Redis broker  
  ↳ \_The finding mischaracterizes how Celery + Redis work together. The code correctly uses `task_acks_late=True` (line 72), which means tasks are NOT acknowledged (removed from the queue) until they complete. This fundamentally changes the behavior: if a worker cr…*
- `workers-orchestrator-5` — Task dispatch recheck only validates ready status, not tenant scoping  
  ↳ _The finding identifies a real gap: the orchestrator's \_dispatch() method does not validate that the event's tenant_id matches the fetched task's tenant_id. However, this is not a practical vulnerability in this codebase because: (1) Events are ONLY published b…_
- `workers-orchestrator-7` — Worktree cleanup race condition between add() and prune*idle()  
  ↳ \_WorktreeManager is only used in apps/orchestrator/src/orchestrator/plan_runner.py, which is explicitly documented (line 1) as "Synchronous plan runner — demos / human tests glue". The docstring (lines 14-16) states that "The production orchestrator (apps/orche…*
- `rag-memory-ingestion-1` — Unsanitized filename in storage key enables path traversal in MinIO  
  ↳ _The finding identifies an unsanitized filename being embedded in MinIO storage keys, but this does NOT constitute an exploitable path traversal vulnerability in this codebase. MinIO/S3 keys are opaque strings, not navigable paths. A key like `kb/tenant/kb/doc/…_
- `rag-memory-ingestion-3` — Chunk details loaded without re-validating visibility after RRF fusion  
  ↳ _The finding claims that chunk details loaded at lines 212-221 could return unauthorized chunks if IDs were "corrupted" after the visibility check. However, this analysis misses two critical layers of protection: (1) PostgreSQL RLS on the chunks table (enabled…_
- `rag-memory-ingestion-4` — Missing tenant*id check in final chunk load violates defense-in-depth  
  ↳ \_The chunk detail loading query on lines 212-221 of search.py does not include an explicit `AND chunks.tenant_id = :tenant_id` check. However, this is not a security gap or defense-in-depth violation: 1. **RLS provides primary defense**: The chunks table has RL…*
- `api-routers-validation-1` — Missing tenant*id filter in dep_cache project lookup  
  ↳ \_The reported finding claims the missing tenant_id filter violates multi-tenancy principles and is a security issue. However, detailed code inspection reveals this is fully mitigated by PostgreSQL Row-Level Security (RLS): 1. CONFIRMED RLS ENFORCEMENT: The proj…*
- `api-routers-validation-5` — IntegrityError details expose internal constraint names in error responses  
  ↳ _The reported finding claims that IntegrityError details expose constraint names in project creation (lines 159-161 of projects.py). However, critical analysis reveals: (1) There is NO unique constraint on the projects.name column in the database schema. The pr…_
- `secrets-config-6` — Workers database URL also contains hardcoded dev password  
  ↳ _The reported finding is technically accurate (the default password string is present in the code), but it misses critical context: (1) pydantic-settings with env_prefix="WORKERS_" automatically overrides the default from environment variables (e.g., WORKERS*DA…*
- `frontend-admin-panel-3` — Missing Dependency in WebSocket Message Handler (Board Kanban)  
  ↳ _The finding misses a critical detail: the `kanbanUrl` includes `effectiveSelected` directly in the URL string (`/ws/kanban/${effectiveSelected}`) and is memoized with `effectiveSelected` as a dependency. When `effectiveSelected` changes, `kanbanUrl` is recalcu…_
- `frontend-admin-panel-6` — Race Condition: Tenant ID Mismatch Between localStorage and React Context  
  ↳ _The finding describes a multi-tab race condition where React state and localStorage could diverge, causing UI confusion. However, this is NOT a security or data correctness issue because: 1. All API requests use `apiFetch`, which reads tenant ID from localStor…_
- `error-obs-logging-2` — Exception type and message leaked to client in MCP test endpoint  
  ↳ _The finding is based on a misunderstanding of the exception handling architecture. The code analysis reveals: 1. EXCEPTION HANDLING IS COMPREHENSIVE: All code paths in `MCPClient.connect()` (lines 158-211 in client.py) are wrapped in try/except blocks that con…_
- `error-obs-logging-4` — Bare except Exception blocks silently swallow errors without logging  
  ↳ _The finding claims "bare except Exception blocks silently swallow errors without logging" at lines 217, 386, and 406. While technically true that no \_log calls are present, this is not a real defect in this context: 1. **Intentional Graceful Fallback Design**:…_
- `tests-quality-3` — Container isolation tests mock Docker; real container execution is only checked once  
  ↳ _The reported finding claims test_container_isolation.py has "2 of ~2-3 tests use real containers" and that isolation guardrails aren't properly tested against real Docker. This is factually incorrect. Reading the actual code shows: (1) test_container_isolation…_
