---
name: tanda-inteligencia-2026-07-11
description: "2026-07-11/13 — tanda inteligencia COMPLETA + los 5 ADR gated cerrados (0080 navegador Playwright, 0100 marketplace, 0112-f2, 0077 consolidación, 0097 sesión SDK viva) + DESPLEGADO + e2e verde + PUSHEADO (7b52ba1a)"
metadata:
  node_type: memory
  type: project
  originSessionId: 573f43d4-c6ef-46c2-8bc9-7c8109851461
---

Mandatos del operador (2026-07-11, acumulados en una sesión): (1) implementar TODOS los hallazgos del informe `investigacion-inteligencia-agentes-2026-07-11.md`; (2) córtex «lo más parecido a una simulación humana» + mejorar asistentes + auditoría profesionalidad (informe `investigacion-cortex-asistente-profesionalidad-2026-07-11.md` ENTREGADO, commit 223e48fe); (3) mejorar canales de notificación + comprobar invocación + que lleguen alertas + añadir neonize para WhatsApp; (4) propuesta de simplificación de la KB sin perder funcionalidad; (5) «implementa todos los hallazgos de forma autónoma, hasta el final, resolviendo errores por el camino».

**HECHO — Tanda P0 agentes (7/7, TDD+commit atómico, rama plan/runs-visor-trabajo):**

- P0-1 persona+rol al run (b2d43f71): `api_server/agent_persona.py` (es→en→plano, cap 8k) → `agent_persona` en payload/spec → `build_persona_preamble` PRIMER bloque; ensamblado extraído a `assemble_system_preamble` (orden: persona→comments→feedback→failure→review→skills).
- P0-3 rag_search capacidad de sistema (793f69a6): en SYSTEM_FAMILY_TOOL_NAMES + register_system_families + SYSTEM_TOOL_NAMES worker (solo la búsqueda; document_convert/promote_to_kb siguen catálogo).
- P0-4 es_unaccent (6dd7910d): migración **0107** reconstruye ix_chunks_content_fts con public.es_unaccent (¡el preview iba SIN índice!); `rag/search.py` unifica \_TS_CONFIG en las 3 rutas.
- P0-5 planning RAG completo (593f59dd): build_project_context usa rag_search con OllamaEmbedder real (best-effort, aclose) + agent_id del PM (KBs de rol).
- P0-2 auto-RAG al boot (61acc26a): `AgentDeps.knowledge` + `_build_auto_rag` (3×700), nodo recall inyecta role=knowledge + guardrail scan.
- P0-7 fracasos no-review realimentados: `_read_prior_failure` (última ejecución failed/aborted; done posterior apaga) → `prior_failure` → `build_prior_failure_preamble` (tail 1500 fenced).
- P0-6 brief worktree: `worktree_file_list` (60 paths, filtro compartido `_collect_rel_paths`) sembrado en perceive («N existing file(s)»).

**HECHO — Tanda NOTIF (4/4):** NOTIF-1 (6392a5f0) body+event*type+severity en structured + fallback body en whatsapp; NOTIF-2 (b5e45fe7) `/internal/alerts/ingest` (router internal_alerts, Bearer fail-closed API_SERVER_ALERTS_INGEST_TOKEN, dedup fingerprint+status Redis TTL 3300, evento `infra_alert` platform-scoped in_app+telegram, token dev cableado + GeneratedSecrets del installer); NOTIF-3 (6b73f24c) emisiones reales (plan_approved en approve_plan, plan_rejected en submit_verdict, execution_failed/finished en worker `_notify_execution_outcome`, review_requested en orchestrator; task_failed RETIRADO por imposible; execution_finished/review_requested opt-in sin default) + `GET /notifications/event-catalog` con candado anti-drift (test_notification_event_catalog) + UI prefs consume el catálogo; NOTIF-4 (78ea4780) **ADR 0109 accepted** neonize: rama config.provider en whatsapp.py (cloud default intacto), settings whatsapp_neonize*\*, sidecar EXPERIMENTAL docker/whatsapp-neonize (profile compose apagado), runbook pairing QR.

**HECHO — Tanda CÓRTEX COMPLETA (C1/C2/C3/C4/C9/C12):** C9 (fd3b20a8) aburrimiento en tone_guidance (arousal<TONE_AROUSAL_LOW ∧ drives≤TONE_DRIVE_BORED=0.15); C12 (fd3b20a8) chip mood en el chat (cortexFetch /mind, 30s); C4 (871a3f6b) diario: GET /owner/cortex/journal (narrativas versionadas dedup + memorias reflection/learning) + JournalPanel en mind/page.tsx. Detalle previo: C3 (3cb5e7dd) conciencia temporal: `temporal_context_lines` ES/EN + SelfContext.now/last_turn_at (último turno role=cortex a propósito) en la guía fuera de DATOS; C2 (06b5d5b0) pulso de plataforma: beat `workers.cortex_platform_pulse` 15min gated + `cortex/platform_affect.pulse_appraisal` (determinista, sin LLM, asimetría negativa, techos, checkpoint Redis 4 ventanas) → snapshot+caché+telemetría; C1 (0fb9d2cf) iniciativa proactiva: beat `workers.cortex_initiative` 30min gated + `cortex/initiative.py` (should_reach_out: pendientes+silencio≥20h+sin iniciativa sin respuesta+nunca primer contacto; compose determinista) → turno role=cortex metadata.initiative=true en conversación nueva + mark_pursuits_surfaced + evento `cortex_message` (registry+templates+catálogo). PENDIENTE córtex: C4 diario (endpoint sobre cortex_identity_history + memorias kind=reflection + vista Panel de Mente), C9 aburrimiento (arousal bajo ∧ drives bajos → tone_guidance), C12 mood visible en chat (metadata.self_context ya lo lleva).

**HECHO — ASISTENTE:** A1 (95739dce) hilos persistentes (migración **0108** assistant_conversations/turns RLS, conversation_id en /assistant/chat, últimos 20 turnos al prompt, endpoints GET conversations/turns, picker UI; cross-tenant 404); A4 (518cdbcd) tool `search_knowledge` (BM25 es_unaccent cross-proyecto bajo RLS, catálogo+default+label UI). A6 → ADR pendiente (usage/coste de consumidores no-run: asistente+córtex+planning SIN contabilizar — decisión de modelo de datos). A2 streaming pendiente.

**HECHO — P1 agentes (parcial):** P1-8 (e33ca0e0) temperature a los kinds HTTP (\_temperature_kwargs; claude_sdk la ignora documentado); P1-4 (a239b1e4) distill_stack_logs (señales de fallo antepuestas al tail 8000); P1-11a (3eb34443) comentarios de FASE al agente (\_task_phase_index sobre Plan.specification.phases); P1-1 (a8957833) fracasos memorizables por defecto (failed/aborted/needs_human_review en \_DEFAULT_ELIGIBLE_STATUSES + prompt de LECCIÓN) + memoria semántica project_shared determinista del rechazo del reviewer (\_persist_rejection_memory en apply_reviewer_verdict); P1-2+P1-3 (b2f81ecd) dedup por contenido normalizado en persist_memory_candidates (+intra-lote) y recall pre-run con rol+criterios (cap 900).

**GOTCHA de sesión**: el transporte del heredoc Bash procesa escapes (`\\n`→newline real, `\\b`→\\x08) — para strings con backslashes editar con Edit tool o `bytes([...])`, JAMÁS heredoc python con escapes.

**HECHO — P1 resto:** P1-7 (037b5949) reviewer ve últimos 3 outputs etiquetados (\_format_prior_outputs, tail 4000 los anteriores); P1-5 (38abbfe9) \_condense_evicted → bloque EARLIER (condensed) (15 líneas×160); P1-6 (5677475d) scratchpad `update_plan` (interceptada en act, sticky agent_plan cap 1500, render YOUR PLAN, schema en SYSTEM_TOOL_NAMES+\_RUNTIME_ONLY_SCHEMAS); P1-11b (d0d11771) beat `workers.backfill_chunk_embeddings` (espejo memory_backfill, 5min).

**HECHO — docs finales:** propuesta KB (f126a8ba, docs/roadmap/propuesta-simplificacion-kb-2026-07-12.md: Q1 auto-grant desde proyecto, Q2 añadir-conocimiento, Q3 built-ins toggle, Q4 panel único de acceso, Q5 avanzado plegado, Q6 estados humanos; recomendado Q1+Q3+Q5+Q6); **ADRs 0110-0116 proposed** (8edd3a90): 0110 threading/caching, 0111 batch read-only, 0112 reflexión periódica, 0113 budgets ampliables, 0114 ask_human, 0115 skill_match, 0116 usage no-run.

**TODO IMPLEMENTADO — cierre 2026-07-12:** P1-9 (7b227a69) pista de adopción en equipos built-in (la edición ya existía para adoptados; los built-in son read-only correctamente); A2 fase 1 (a11f61b7) `POST /assistant/chat/stream` SSE (frames progress por paso del grafo vía on_progress/astream + answer final que persiste el hilo; frontend streamAssistantChat + «Pensando… (ronda N — tool)»; token-a-token queda en ADR 0073 F2); P1-10 = toggle del operador documentado en el informe. Informes actualizados con estado (20042a5c). **Validación final: unit 2206 passed (cobertura 32.15%, ratchet 31), runtime 317+, admin-panel typecheck+vitest 214+build, integración de cada pieza en verde.** Rama plan/runs-visor-trabajo ~+65 commits SIN push. NO desplegado (rebuild de imágenes pendiente si el operador lo pide). **Decisiones del operador abiertas**: ADR 0108 A/B/C + ADRs 0110-0116 + encender cortex.autonomy_enabled + aprobar planes prod-\* (prod-08 primero) + elegir Q1-Q6 de KB + asistente para members. Rama plan/runs-visor-trabajo ~~+60 commits sin push (merge = operador). (memoria fracasos/review+dedup+recall rico, destilar stack_exec, resumen evictado, scratchpad, reviewer informado, temperature, teams.model_config UI, reranker medir, comentarios fase+backfill chunks); CÓRTEX-1 (C3 temporal trivial en self_context.py:227-238 → C2 eventos→afecto → C1 proactividad gated → C4 diario → C9/C12); ASISTENTE-1 (A1 persistencia+hilos patrón conversations, A4 KB tool cross-proyecto, A6 coste, A2 streaming ADR0073 F2); propuesta KB (auto-grant al crear desde proyecto, built-ins 1-click, grants unificados, embeddings a «avanzado», estados simples, «añadir conocimiento» directo); 6 ADRs P2 agentes (threading/caching~~ADR0097, batch tools, reflexión semántica, budgets ampliables, ask_human, skill_match).

**CIERRE FINAL 2026-07-12 (FASES 2 y 3 HECHAS):** FASE 1 robustez commiteada antes (ADR 0108→accepted C, ADR 0115 f1 skill_match, ADR 0113 budget_ceiling_multiplier, ADR 0116 f1 migración **0109** llm_usage_events + record_llm_usage en asistente/córtex, KB Q1/Q3/Q6, prod-08 núcleo app_alerts+DLQ). **FASE 2 deploy**: 6 imágenes rebuild, stack healthy, migraciones dev al head **0109** (había que correr `docker exec -e DATABASE_URL=... api-server alembic upgrade head` — estaba en 0106), e2e ciclo autónomo 2/2 post-deploy, reranker ya ON. **FASE 2b monitorización (encargo nuevo, commit e014d644)**: BUG REAL — ningún compose montaba /host/textfile en el worker de la cola default → agentic_celery_queue_depth/tasks_by_status/dlq_depth NUNCA llegaron a Prometheus en ningún entorno; fix = mount del volumen node_exporter_textfile en `workers` (docker-compose.monitoring.yml, capa correcta) + one-shot `textfile-init` chmod 1777 (drop-dir multi-escritor: workers uid1000 EACCES vs workers-backup root) + métrica nueva `agentic_executions_24h{status}` + dashboard Grafana provisionado «Plataforma Agéntica» (uid agentic-platform, /d/agentic-platform). Verificado VIVO: .prom escrito, 3 familias en Prometheus, reglas agentic-app cargadas, cadena de alertas e2e real (ingest 401/401/200 → infra_alert → dispatcher → in_app **sent**; ojo: notification_channels estaba VACÍA en dev — sembré canal in_app platform-scope por SQL, sin canal TODO acaba no_op «no enabled channels in scope»). **FASE 3**: push HECHO — 70 commits, cd0ea48a..e014d644 → origin/plan/runs-visor-trabajo.

**TANDA «valor añadido» 2026-07-12 (mandato: implementa lo que consideres):** ADR **0111 IMPLEMENTADO→accepted** (a19e0133): batch read-only por turno en el agent-runtime — prefijo consecutivo read-only cap 4 en `batch_calls` de ModelDecision, `act` los ejecuta en UNA iteración (observación agregada, error por elemento), presupuesto+post_tool+novedad POR ELEMENTO, fingerprint del detector incluye lote, gate de aprobación expulsa elementos sensibles, system prompt anuncia la excepción; helper `_readonly_batch_prefix` (PLR0912). ADR **0112 fase 1 IMPLEMENTADO→accepted** (26060f85): self-check semántico cada K=10 iteraciones vía sticky `self_check_nudge` (reflect lo puebla/limpia, `_decide_messages` lo renderiza, instrumentado nudge:self_check) — COSTE CERO, sin llamada LLM extra; fase 2 (mini-turno dedicado) gated a telemetría. ADR **0110 DIFERIDO** con nota de planificación (tanda dedicada + ADR 0097). Deuda documental córtex (affb048e): 0075/0078→accepted con nota de estado; 0076 queda proposed honesto (se implementó el camino degradado anti-SSRF, no WebSearch del SDK). Suite runtime 334 passed.

**TANDA «implementa todo en orden» 2026-07-12 (mandato sobre el análisis de diferidos docs/roadmap/analisis-diferidos-2026-07-12.md, cccf5a27):** #1 **ADR 0101→accepted** (5a3a43be): import MCP re-descubre server-side y PERSISTE input_schema/description (fail-closed 502; upsert refresca; antes toda tool MCP con args era inservible). #2 ratificación documental **0063/0073/0077→accepted** (5a938576). #3 **ADR 0103→accepted** (490afefc): G10 firma de símbolos en digest (\_SYMBOL_LINE_RE) + safeguard_stats en la cabecera del visor de ejecuciones + **G8-B ratificado por el operador** (mandato explícito). #4 **ADR 0098→accepted** (968c037e): beat sweep_project_git_remotes gated por setting git_fetch_sweep_enabled (OFF) + WORKERS_GIT_FETCH_CRON (\*/30). #5 **ADR 0114→accepted** (db1eb920): ask_human no terminal — capacidad del loop interceptada en plan (\_maybe_park_ask_human), park por maquinaria de aprobaciones (category human_question SIEMPRE humana), inbox responde (botón Responder, respuesta en reason), re-dispatch con preámbulo human_answers (dispatcher \_read_prior_human_answers cap 3 → run_spec → build_human_answers_preamble fenced). #6+#7 **KB Q4+Q2** (f3e459b4): edición unificada de grants en kb-assignments-dialog (alta proyecto + agente bajo Avanzado) + «Añadir conocimiento» (KB implícita lazy «Documentos de {proyecto}» + upload multi-fichero) — propuesta KB Q1-Q6 COMPLETA. #8 **token-a-token A2f2/0073-F2** (81c708ec): stream() YA existía en los 4 providers; decide_stream en LLMAssistantModel + finish streamea el camino FINISH_NUDGE con on_delta + frames SSE answer_delta + burbuja draft en la página. #9 **anticipo 0099** (67160f2f): contexto estructurado del conflicto de rebase {plan_branch,files,shas} capturado antes del abort → step en steps_log + ficheros en la nota. Suites: unit 2227, runtime 342, vitest 216, typecheck OK. PENDIENTE AL CIERRE: rebuild 4 imágenes+deploy+e2e (en curso), push final. Quedan proposed deliberados: 0110+0097 (tanda dedicada), 0102 resto (enforce por fases), 0099 visor completo, 0100 (gated, 0 installs), 0080 (bloqueado), 0112-f2 (telemetría), 0077-consolidación (tras datos).

**TANDA «proposed beneficiosos» 2026-07-13 (mandato: continúa con los proposed que veas bien):** ADR **0102 COMPLETO→accepted** (98255317+22c39f40): motor shared-guardrails con to_dict D3 + on_error D5 (warn=fail-open/block=fail-closed) + truncado 50k D6; runtime `_screened_tool_call` envuelve cada tool call (principal+lote) entre pre_tool/post_tool con ENFORCE de block (pre=rechaza llamada, post=sustituye output; baseline warn/LOG intacto); capa PLATAFORMA en platform_settings.guardrails_config (tipo nuevo en registry+validación+editor JSON en panel, categoría Seguridad) + capa PROYECTO projects.guardrails_config (**migración 0111... no: 0110**) + fusión `_resolve_effective_guardrails` (worker, junto a approval_policy; locked gana, cap 64KB) → spec["guardrails"]. ADR **0099 COMPLETO→accepted** (f9ff96c7): servicio `api_server/code_diff.py::plan_code_diff` (bare real vía worktree_coordinates, merge-base..rama, numstat completo + cuerpo capped 400k, refs \_safe_git_ref) + endpoint GET /projects/{pid}/plans/{plan_id}/code-diff + sección FE plegable «Diff de código de la rama» (DocDiffRenderer reutilizado). ADR **0110 mitad HTTP flag-OFF** (7dcecc9a, sigue proposed con nota): hilo en memoria por run en \_ProviderModelClient (turn update compacto + compactación EARLIER TURNS cap 20), gated WORKERS_RUNTIME_CONVERSATION_THREAD→spec.model.conversation_thread→_with_thread_flag (solo HTTP; claude_sdk intacto); ratificar tras e2e coste/convergencia. **NO implementados con justificación**: 0080 (4 preguntas seguridad), 0100 (0 installs=valor nulo), 0112-f2/0077-consolidación (gates de datos no cumplidos), 0097 (spike SDK vivo). Suites: unit 2238, runtime 349, vitest 216, guardrails 38.

**TANDA «abordamos los 5 puntos gated» 2026-07-13 (mandato: 0080/0100/0112-f2/0077/0097):** los CINCO cerrados.

- **ADR 0080 FIRMADO por el operador** (942aac2a) vía AskUserQuestion: (1) sí aprobado, (2) **self-host**, (3) **validación humana POR SESIÓN**, (4) **interacción completa** (login/formularios/clicks).
- **ADR 0100 COMPLETO→accepted** (822b6adf): materialización real del marketplace (skill→Skill, mcp/http→Tool sandboxed; python_function/docker_command DIFERIDOS hasta sandbox out-of-process ADR 0081 B/C), provenance en tools+skills (**migración 0111**, FK SET NULL + índice parcial), idempotente por source_installation_id, dematerialize en uninstall/consent-revoke. Integración 4/4 sobre BD real. Gotcha: `requested_permissions` son DESCRIPTORES `{"type": <allowed_domains|allowed_paths|network_policy>, "value":...}` — otro type = 422.
- **ADR 0112 fase 2 flag-OFF** (fdf02a79): assess_progress (tool_choice forzado a submit_progress {score,stuck,reason}) cada K + escalado DETERMINISTA tras 2 «stuck» seguidos (abort_code `reflection_stalled`); solo HTTP (claude_sdk no tiene tool_choice); flag WORKERS_RUNTIME_REFLECTION_ASSESS.
- **ADR 0077 consolidación** (1752efaa): merge-into de la episódica repetida — `cortex/consolidation.py` PURO (coseno greedy ≥0.90, min 3, contenido fusionado determinista que CITA los originales, sin LLM: no puede alucinar) + `_consolidate_similar` en cortex_maintenance (episódicas >14d con embedding, excluye protegidas y ya-consolidadas; centroide + consolidated_from/consolidated_into; soft-delete reversible), gated cortex.autonomy_enabled.
- **ADR 0097 SPIKE PASA + IMPLEMENTADO** (687de5bb): spike con **credencial VIVA** dentro del contenedor api-server (CLI real, claude-agent-sdk 0.2.116) → el deny **SIN interrupt** cierra el turno LIMPIO (1 deny, ResultMessage y usage íntegros: **el interrupt era la causa raíz del usage frágil F1.4**), la sesión RECUERDA turnos previos y reusa KV-cache (~45k cache_read en turnos 2+). Implementada la variante «deny-espera» (el host ejecuta la tool fuera de la sesión) porque preserva el contrato del grafo. **Apunte del operador: ha de servir a CUALQUIER provider, no solo al SDK** → el hilo conversacional es UNA capacidad con DOS transportes: HTTP re-envía el hilo (0110) y claude_sdk mantiene sesión viva (0097), MISMA flag `WORKERS_RUNTIME_CONVERSATION_THREAD`, mismo contrato, default OFF. `ClaudeAgentSessionProvider` (shared-llm) vive en un **event loop de fondo** (el runtime puentea con asyncio.run: un loop nuevo por turno mataría el transporte del SDK); turno fallido ⇒ sesión destruida y reabierta con el historial completo; review/assess siguen one-shot; el run cierra el proveedor al acabar. **ADR 0110 pasa a COMPLETO.**
- **ADR 0080 FASE 1 IMPLEMENTADA** (7b52ba1a): imagen `browser-runtime:v1` (base oficial playwright/python, 2.8GB) con lógica PURA testeable sin Chromium — catálogo CERRADO de pasos (goto/click/fill/wait_for/extract, **no hay eval_js**), anti-SSRF, presupuestos duros, salida como DATO saneado, lo tecleado en un `fill` NO vuelve. Lanzamiento efímero por el worker con el MISMO perfil hardened del agent-runtime (cap-drop ALL, read-only, sin socket Docker, red interna → solo egress-proxy). Aprobación humana POR SESIÓN: **migración 0112** `browse_sessions` + máquina de estados sin atajo a `running`; endpoints owner `browse-sessions/{id}/approve|reject`. Host tools `browse_request`/`browse_result` con DOBLE gate (kill-switch `cortex.browser_enabled` default OFF + aprobación) → **funcionan con los 4 providers**. Verificado contra la imagen real: sin proxy no arranca, rechaza loopback, rechaza eval_js.

**CIERRE de la tanda de los 5 gated (2026-07-13)**: 4 imágenes reconstruidas (api-server:manuals → workers:ci → agent-runtime:v1 → **browser-runtime:v1** 2.8GB; admin-panel = cache, no tocaba), stack desplegado y healthy, **migraciones dev al head 0112** (0111 marketplace-provenance + 0112 browse*sessions; venía de 0110). Verificado VIVO en los contenedores: tools browse*\* aparecen/desaparecen con el kill-switch, task `workers.browse_session` registrada con imagen browser-runtime:v1, `ClaudeAgentSessionProvider` seleccionado por el spec con deny-sin-interrupt, tabla browse_sessions con RLS. Contra la imagen real del navegador: **sin proxy no arranca**, rechaza loopback (anti-SSRF), rechaza `eval_js` (catálogo cerrado). **E2E post-deploy VERDE**: ciclo autónomo 2/2 + marketplace 4/4. **PUSH HECHO**: 6 commits 7dcecc9a..7b52ba1a → origin/plan/runs-visor-trabajo.

**AUDITORÍA DE CIERRE + REMEDIACIÓN 2026-07-13 (mandato: «¿seguro que no queda nada?»)**: NO me fié de memoria — lancé una auditoría multi-agente (workflow, 6 lentes + refutación adversarial doble) y corrí TODAS las suites yo mismo. Veredicto: **10 hallazgos confirmados** (2 críticos, 6 importantes, 2 menores) + 2 descartados con criterio. **Los 10 remediados** (TDD; los tests nuevos destaparon los bugs). Commits **44bfbc22** (backend/CI) + **ebee9680** (UI), pusheados.

- **4 BUGS REALES en código ya enviado** (no eran "solo tests"): (1) ADR 0097 `CancelledError` hereda de BaseException → `except Exception` no lo capturaba → un timeout de turno (wait_for 900s) dejaba la sesión SDK envenenada y el reintento lanzaba query concurrente sobre el mismo CLI; fix `except BaseException` + re-propagar cancelación/KeyboardInterrupt/SystemExit tal cual. (2) ADR 0080 `browse_task` reventaba al re-chequear el kill-switch porque `approved→failed` era transición ILEGAL → task Celery petaba, sesión colgada en `approved` en bucle; fix permitir approved→failed + blindar ejecución. (3) ADR 0080 `BrowseSession` escribía datetimes aware en columnas sin timezone (asyncpg DataError) → `DateTime(timezone=True)`. (4) ADR 0077 `_consolidate_similar` fallaba SIEMPRE en silencio (best-effort): pgvector da el embedding como numpy.ndarray y `arr or []` es ambiguo → conversión explícita a lista.
- **2 CRÍTICOS de UI (la feature 0080 era INERTE)**: no había toggle de `cortex.browser_enabled` ni inbox de aprobación en el admin-panel → el owner no podía encender el navegador NI aprobar sesiones. Implementado `BrowseInboxPanel` (lista pending + guion legible browseStepSummary + aprobar/rechazar) + toggle en el panel de mente. El estado documental del ADR 0080 MENTÍA (daba el toggle por hecho como "fase 1" y el inbox como "fase 2 QA"); corregido.
- **Huecos CI/metadata**: BrowseSession ahora en Base.metadata (evita drop_table espurio en autogenerate); CI construye browser-runtime:v1 + corre sus 19 tests de seguridad + suite shared-llm (el 0097 caía en ese hueco); scripts/dev/build-browser-runtime.sh.
- **Descartados con criterio** (no requieren código): el navegador comparte el egress-proxy deny-by-default del córtex (servicios sensibles postgres/redis/vault/minio INALCANZABLES; solo ollama/searxng en allowlist — documentado como límite conocido en el ADR); provenance del marketplace invisible = fase futura ya documentada en ADR 0100.
- Tests nuevos: sesión SDK cancelada, browse_task e2e (4), consolidación e2e (3 similares→1+centroide+reversible), approved→failed, browseStepSummary. Suites: unit 2267, runtime 358, shared-llm 99, browser-runtime 19, integración 13, vitest 218.
- **REDEPLOY 2026-07-13**: 4 imágenes reconstruidas + stack recreado (25 healthy, head 0112) + e2e ciclo autónomo 2/2 post-redeploy. **GOTCHA de build cazado**: el primer `workers:ci` salió con la CAPA BASE api-server CACHEADA antigua (BuildKit `FROM <tag>` resolvió un digest viejo pese a que api-server:manuals ya tenía el fix) → `approved→failed` NO estaba en el worker desplegado; se detectó verificando el \_ALLOWED dentro del contenedor y se corrigió reconstruyendo workers una 2ª vez (base ya confirmada) + recreando el contenedor. **Lección**: tras reconstruir la base, VERIFICAR el fix dentro del contenedor derivado, no fiarse del tag. Todos los fixes confirmados vivos en los contenedores.

**Decisiones del operador aún abiertas**: aprobar planes prod-\* (recomendado prod-08 primero), encender cortex.autonomy_enabled / git_fetch_sweep_enabled / WORKERS_RUNTIME_CONVERSATION_THREAD (4 providers) / WORKERS_RUNTIME_REFLECTION_ASSESS / **cortex.browser_enabled** (navegador, arranca OFF; el inbox de aprobación YA existe en el panel de mente), asistente para members, merge de la rama (PR), canales reales de notificación, ratificar 0110/0097 tras e2e de coste-convergencia. Pendiente fase 2 del 0080 (no bloqueante): QA visual del inbox + navegación real contra un sitio de prueba con el kill-switch ON.

**How to apply:** continuar TDD+commit atómico por ítem; los informes fuente están en docs/roadmap/ con fichero:línea de cada hueco; auditoría notificaciones completa en el resultado del explorador (sección 5 = fixes priorizados). Relacionado: [[investigacion-inteligencia-agentes]], [[hallazgos-pendientes-implementados-2026-07-09]].
