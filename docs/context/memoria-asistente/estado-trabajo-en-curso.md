---
name: estado-trabajo-en-curso
description: Checkpoint — 2026-06-21; rama feat/builtin-customization. Tras lo built-in se añadió: memoria provider-agnóstica (asistente+agentes, aprender-de-errores), modo voz/videoconferencia (ADR 0073, STT/TTS/avatar lip-sync), sweep markdown en textareas, y CABLEADO del chat de proyecto (planning/discussion/execution) — todo desplegado en api-server:manuals. Pendiente: estrategia de merge + QA visual + aprobar B0.2 (egress web) + voz F2.
metadata:
  node_type: memory
  type: project
  originSessionId: f7e54214-9978-4552-9197-70ecc3f15b3d
---

Checkpoint actualizado el **2026-06-18** (prod-01 A-F pusheado; **EJECUCIÓN REAL verificada**; + 2ª tanda autónoma: Claude SDK, mem0 nativo, deprecated, deps, dockers).

## ✅ Sesión 2026-06-20/21 (autónoma) — memoria provider-agnóstica + voz + markdown + chat de proyecto (rama `feat/builtin-customization`, desplegado en `api-server:manuals`)

Stack vivo en dev = **contenedores docker `*:manuals`** (NO uvicorn local; el PID file `.dev/api-server.pid` es stale). Build/recreate: `docker build -t agentic-platform/api-server:manuals --build-arg WITH_CLAUDE=1 -f apps/api-server/Dockerfile .` (contexto=raíz, **desde PowerShell** por el bug MSYS de build-args) + `docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml -f docker/docker-compose.manuals.yml up -d api-server`. **App tras Caddy en `http://localhost:8080` (API bajo `/api`).** stt/tts NO levantados (voz audio requiere `docker compose pull/up -d stt tts` del operador).

- **Memoria provider-agnóstica (asistente + agentes).** Independiente del provider LLM (ADR 0021, futuros como OpenAI funcionan igual). claude_sdk.complete() ahora honra `tools` y emite `tool_calls` como los OpenAI-compat: anuncia tools como MCP in-process (`create_sdk_mcp_server`+`@tool`), intercepta con `can_use_tool→PermissionResultDeny(interrupt=True)` y cosecha `ToolUseBlock→CompletionResponse.tool_calls` (quita prefijo `mcp__server__`). Bucle de memoria de agentes: worker inyecta schemas de tools en model_config (#2), runtime claude_sdk emite tool_calls (#2b), trigger memorizer gateado por status terminal (#3). Setting plataforma `memory.memorizable_statuses` (default `{"done"}`) gatea aprender-de-errores. Asistente verificado en vivo.
- **Modo voz/videoconferencia (ADR 0073).** STT=`faster-whisper-server` (`latest-cpu`, OpenAI-compat `/v1/audio/transcriptions`); TTS=`Kokoro-FastAPI` (`/v1/audio/speech`, voces M/F seleccionables); avatar=lip-sync por amplitud en navegador (Web Audio AnalyserNode sobre SVG) — NO TalkingHead 3D. WS `/ws/assistant/voice` (auth `?token=` + `require_assistant_access`). Provider-agnóstico. **F1+F3 hechos+desplegados; F2 (streaming/barge-in) PENDIENTE** tras validar con modelos reales.
- **Sweep markdown en textareas.** `components/ui/markdown-textarea.tsx` (tabs Editar/Vista previa, `renderPlanDraft`) aplicado al chat + 9 textareas. **persona-section DIFERIDO** (muy enredado con e2e `agent-persona.spec` por múltiples `idPrefix`/testids).
- **CABLEADO del chat de proyecto + AUDITORÍA + STREAMING + modelo de chat (commits `a94faf7`→`e7cfb71`).** El chat no respondía: el sub-grafo `planning_graph.py` solo tenía `ScriptedPlanningModel`, sin caller real. Recorrido completo:
  1. **`chat/planning_llm.py`** (`LLMPlanningModel`, commit 9be62a5): adapta cualquier `LLMProvider` al seam `PlanningModelClient`; sync↔async vía `asyncio.run` dentro de `asyncio.to_thread`.
  2. **`chat/responder.py` + disparo** (a94faf7→e7cfb71): `respond_to_conversation` para los 3 modos; `post_message` lo dispara SOLO ante mensaje USER.
  3. **Error de diseño v1 corregido**: cablé la resolución al **asistente personal** (`resolve_assistant_model`) → el usuario: "no entiendo qué tiene que ver el asistente". CORRECTO = modelo del **proyecto/equipo** vía `resolve_model_config_chain` (cadena ADR 0065 plataforma→proyecto→equipo, SIN nivel agente) + provider **por kind, fila activa más nueva** (`resolve_provider_config`, ADR 0028 — idéntico al dispatch de agentes). El operador CONFIRMÓ "por tipo". Memoria relacionada: [[provider-resolution-two-paths]].
  4. **AUDITORÍA adversarial (workflow ultracode, 54 agentes, 37/47 hallazgos reales)**. Hallazgo CRÍTICO (C1, mi sospecha confirmada): el disparo con **`BackgroundTasks` corre ANTES del commit de la dep `yield` en FastAPI 0.136** → el responder no veía el mensaje USER recién enviado. FIX (commit 94ddbd1): **`schedule_after_commit` + tarea detached** (`schedule_reply`) — post-commit garantizado SIN bloquear el POST. Mismo patrón que tasks.py. + C2 (validar tenant en sesión BYPASSRLS), A1 (propagar temperatura), A3 (mensaje system en error/timeout/vacío), A4 (timeout), M2/M4/M5/M6. Test de integración `test_conversation_responder_after_commit.py` fija el orden.
  5. **STREAMING por agente** (e7cfb71): en planning el responder orquesta PM decide→cada especialista→síntesis y **publica CADA paso como su propio mensaje de agente** en tiempo real (etiqueta de rol 🧭/⚙️/🎨…), timeout+error POR PASO. Petición del usuario: "ver lo que trabajan los agentes".
  6. **`chat_model_config`** (e7cfb71, migración **0089** reversible, dev en head 0089): columna JSONB en teams+projects SEPARADA del `model_config` de ejecución. `resolve_chat_model_config` prioriza override de chat (proyecto→equipo) y si no cae a la cadena de ejecución. PUT/GET (alias JSON `chat_model_config`) + UI (2ª `DefaultModelSection` "Modelo del chat" en ficha proyecto/equipo). **MOTIVO (verificado en vivo): claude_sdk (Claude Code, AGÉNTICO) + opus + reasoning `max` tarda >150s en UN solo paso → inviable para chat interactivo** (también puede dar "Reached maximum number of turns (8)"). El override permite un modelo más ligero/rápido para el chat. Para chat snappy conviene un provider NO agéntico (ollama/azure) — en el stack manuals solo corre claude_sdk, así que la demo end-to-end rápida queda pendiente de que el operador fije un modelo de chat ligero (vía la nueva UI) o levante ollama.
  - Desplegado: api-server:manuals + admin-panel:manuals reconstruidos y healthy; chat_model_config confirmado en el OpenAPI vivo.
  7. **Bugs de runtime descubiertos al probar en vivo (commits `4a87485`, `842a149`, `dd39c3c`):**
     - **IntegrityError** (4a87485): los mensajes `agent` fallaban — la tabla `messages` tiene CHECK `ck_messages_author_kind_consistency` que exige `author_agent_id NOT NULL` para `author_kind='agent'`. El responder los creaba sin agente. Fix: `team_role_agents` (rol→agent_id del equipo) atribuye cada mensaje al agente real (PM/framing/síntesis→PM, especialista→su agente; fallback al 1er agente; si no hay equipo con agentes→aviso system). Verificado con INSERT real + rollback.
     - **claude_sdk + reasoning → "Reached maximum number of turns (8)"** (842a149): claude_sdk es AGÉNTICO (Claude Code); con `reasoning_effort` high/max el loop agota turnos en las llamadas estructuradas (pm_decide JSON) → ProviderError. MEDIDO: pm_decide tarda **~8s SIN razonamiento** vs falla con high. Fix: el chat DESCARTA el reasoning SOLO para claude_sdk; los providers no-agénticos (ollama/azure/copilot) conservan el razonamiento. ⚠️ **Para chat rápido conviene un provider NO agéntico**; en el stack manuals solo corre claude_sdk (ollama configurado pero su servicio NO levantado).
     - **plan-only** (dd39c3c): el PM respondía "voy a crearte la app" (tono ejecución). Decisión operador: planning SOLO planifica (lectura/análisis) y devuelve un PLAN insertable, NUNCA implementa. Inyectada `_PLAN_ONLY_RULE` en los 3 prompts.
  8. **Feature A2 — plan estructurado insertable (commit `67b41db`, desplegado+tests):** en finish_planning, `LLMPlanningModel.pm_plan_draft` formaliza un plan estructurado (title/summary/tasks con id+depends_on, saneado por `_normalise_plan_draft`) y el responder lo adjunta como `{kind: planning_directive, intent: finish_planning, specification}`. **Reusa el botón "Generar Plan" que la UI YA tenía** (isFinishPlanningReady): `create_plan` (POST /projects/{id}/plans) con solo conversation_id ahora LEVANTA esa spec del adjunto y crea el Plan con sus tareas DAG (materialización chat→plan, task_03_14, que estaba sin cablear). Tests: unit `_normalise_plan_draft` + integración chat→plan. **Plan-only intacto**: el equipo planifica, el plan se inserta, los agentes ejecutan.
  9. **Feature B — proveedor LLM concreto por nombre para el chat (commit `c49cb8d`, desplegado+verificado):** `GET /agents/provider-options` (tenant) lista proveedores ACTIVOS {id, kind, display_name, slug, models, reasoning_options} — distingue filas del mismo kind (verificado en vivo: Claude SDK 22 modelos, Ollama local 2, Ollama cloud 41). `chat_model_config` admite `{provider_id, model, temperature?, reasoning_effort?}`; `resolve_chat_model_config` lo prioriza y `_resolve_chat_provider` construye por ESA fila (`build_llm_provider`), no por kind; `validate_chat_model_config` valida provider_id+model. UI: nuevo `ChatModelSection` (selector por nombre → carga modelos del provider elegido) reemplaza al DefaultModelSection de chat en proyecto/equipo. La ejecución de agentes sigue por kind (sin cambios). **Resuelve la confusión "solo aparece Ollama local".**
  10. **Contexto del proyecto en el planning (commit `ef883c6`, desplegado):** un 2º plan debe saber "cómo está montado el proyecto". Decisión de diseño (tras debate con el operador sobre no romper la uniformidad de providers): NO un agente que navega el repo (solo encajaría con claude_sdk nativo → rompe "todos igual"), sino **ensamblar contexto e inyectarlo en el prompt** (provider-agnóstico). `build_project_context` reúne best-effort: planes previos (título/estado/resumen) + memorias `project_shared` + docs/código vía RAG (`recall_chunks`, BM25 sin embedder) → lo mete en `project_context` (que ya llegaba a los 3 prompts vía `_context_note`). El último mensaje USER guía el RAG. Reusa memoria+RAG+planes; cero arquitectura nueva. **Veredicto del operador validado:** el agente-que-navega era over-engineering; la inyección de contexto es lo correcto y uniforme.
  11. **Unificación de desplegables de modelo a PROVEEDOR CONCRETO (commits `7fbe642`, `748525c`, desplegado):** el operador detectó que el picker de "Modelo del chat" (proveedores concretos) y el de "Modelo del proyecto" (4 tipos/kind) eran distintos. Decisión (AskUserQuestion): TODO a proveedores concretos por nombre. **Diseño de bajo riesgo backward-compatible:** los pickers guardan `{provider: <kind>, provider_id, model, …}` — el kind se conserva (cadena de herencia + validate_model_config intactas) y provider_id fija la fila exacta. **Layer 1 (workers/model_resolver):** si el spec trae provider_id, resuelve ESA fila (endpoint+credencial) en vez de newest-active-of-kind; si no, kind (fallback). import_module para api_server.db.llm_providers (quirk mypy worker→api_server). **Layer 2 (UI):** ChatModelSection generalizado (emite provider kind + title/description) sustituye al DefaultModelSection de ejecución en proyecto y equipo. **Estado:** los 4 pickers principales (asistente, chat, proyecto, equipo) ya son concretos+uniformes. **PENDIENTE:** el picker del AGENTE (PersonaModelFields — entangled con e2e/draft machinery) y el DEFAULT de plataforma siguen por tipo. **OJO ollama-cloud:** da 401 (sin API key de ollama.com en Vault, config de System Admin); ollama-local solo tiene `llama3.2:1b` cargado. El chat del proyecto Prueba CI4 quedó fijado a ollama-cloud/qwen3-coder:480b (resolución verificada) pero no responde hasta poner la key.
  - **TODO el chat de proyecto: ENTREGADO** (commits a94faf7→748525c). Pendientes menores: auditoría A2 (modos custom→discussion), max_turns por proyecto (ofrecido, no pedido al final; el cuello era el reasoning, ya mitigado), gotcha del flujo de despliegue (rebuild api-server:manuals + admin-panel:manuals desde PowerShell + migración manual al head). Voz F2 sigue diferida.

## ✅ Sesión 2026-06-19 — Personalización de equipos built-in ENTREGADA (rama `feat/builtin-customization`, pusheada hasta 8849e7e)

Diseño aprobado (brainstorming HARD-GATE) → plan TDD → ejecutado en olas. **10 commits míos** (43b5d63→8849e7e). **OJO: la rama está APILADA sobre `plan/prod-01` sin mergear** → 82 commits ahead de `master` pero **solo los 9-10 de arriba son de esta entrega**; el resto es prod-01/Claude SDK/mem0. Un PR a master arrastraría todo → **la estrategia de merge es decisión del operador** (no abrí PR).

- **B0.1** (43b5d63): 18 skills nuevas en `builtin_skills` (PHP/CI4, security, data, LLM), 6 categorías cerradas.
- **B** (14083d5, d5bef09): equipos built-in **completos**. Dos huecos simétricos cerrados — CI4 sin skills + `builtin_agents` sin tools. Nuevo `builtin_role_capabilities` (rol→{tools,skills}) + `seed_ci4_agent_skills` + `seed_builtin_agent_tools` + **guardia** integración (todo agente de equipo built-in ≥1 tool y ≥1 skill).
- **A herencia de modelo** (115b46d, **ADR 0065**): migración **0085** (teams/projects.model_config); `resolve_model_config_chain` puro (cadena plataforma→proyecto→equipo→agente, gana el más específico que pinea provider+model); dispatch `_route_ai` la resuelve cargando el equipo; worker (ADR 0057) sin cambios.
- **C adopción** (8b5d9be, **ADR 0066**): migración **0086** (teams.forked_from_team_id/version); **POST /teams/{id}/adopt** (copia editable, forkea N miembros con KBs/tools/skills, scope project_local|global_tenant_template, recrea TeamMember; built-in intacto).
- **A-UI/D backend** (2d8a455): model_config en PUT /teams y /projects (alias JSON, validado); **aviso de Capacidad reescrito accionable** (mantiene code + ADR 0054).
- **Frontend** (39a8260): `DefaultModelSection` (heredar vs pinear, reusa `PersonaModelFields`) en detalle Equipo/Proyecto; `AdoptTeamDialog` (tenant/proyecto+nombre+modelo opc) con botones Adoptar (detalle built-in + cards lista). **PersonaModelFields vive en components/capability/persona-section.tsx**, helpers en lib/persona/persona.ts. `to_*_response` con alias model_config van por `model_validate` (mypy no expone el kwarg field-name con alias).
- **D-2** (2844183): `resolve_model_config_origin` + `CapabilitySer.model_origin`; el Hub muestra el nivel del modelo efectivo. `_resolve_model_origin` en agents.py (cuidado: meter el helper ENTRE `@router.get` y el endpoint rompe FastAPI; va ANTES del decorador).
- **B0.2** (6ebea4e, **ADR 0067 `proposed` — NO implementado**): run-tests/format-code/fetch-url **ya existen** (run-pytest/run-lint/run-build/http-get); lo nuevo = `web-search` + `web-fetch` anti-SSRF → abre **egress a Internet** (Principio 2) → GATED. Preguntas abiertas: ¿aprobar egress? ¿Brave API y/o SearXNG self-host? ¿web-fetch además de web-search?
- **Verificado:** backend regresión consolidada 57 verde + migraciones 0085/0086 reversibles (head dev = **0086**); frontend `tsc`/`next lint`/87 vitest verdes; mypy + pre-commit limpios. Changelog en `docs/07-changelog/builtin-customization.md`.
- **PENDIENTE (decisión operador):** estrategia de merge (rama apilada), QA visual del admin-panel, aprobar+implementar B0.2.

## ✅ Sesión 2026-06-18 (tanda 2, autónoma) — Claude SDK + mem0 + deprecated + deps + dockers (pusheado 97a99b8..7eb7726)

- **Claude SDK habilitado en el sandbox (ADR 0064, e0806ce + 7eb7726).** Bug: la credencial nunca llegaba al contenedor (rama claude_sdk del model_resolver era no-op). Cableado TDD por capas: `model_resolver._overlay_provider_fields` traslada api_key/oauth_token al spec; agent-runtime (`build_provider_client`/`ClaudeSDKModelClient`/`_overlay_resolved`) lo pasa a `ClaudeAgentProvider`; shared-llm añade `oauth_token`→`CLAUDE_CODE_OAUTH_TOKEN` (api_key→ANTHROPIC_API_KEY ya estaba); schema acepta api_key O oauth_token; UI con selector de modo. **Dos modos, mismo kind** (sin 5º proveedor). La credencial viaja en `AGENT_TASK_SPEC['model']` (sin montar ~/.claude). **Imagen Claude-capable = build-arg `WITH_CLAUDE=1`** (instala shared-llm[claude] + Node + CLI `@anthropic-ai/claude-code`; verificado: claude_agent_sdk + `claude` en PATH). **PENDIENTE para correr Claude de verdad:** (1) imagen agent-runtime con WITH_CLAUDE=1, (2) `API_SERVER_VAULT_TOKEN` válido + mount KV `platform` (el token de `vault-init-output/root-token.txt` está STALE/inválido en el Vault dev → 403; por eso el provider claude_sdk no se pudo crear en dev), (3) la credencial real del usuario (API key + suscripción). Verificado fail-loud: agente→claude_sdk sin provider activo → "no active llm_providers row of kind 'claude_sdk'".
- **mem0 nativo (ADR 0059 Opción A, f531f45).** Sin la librería: `memory_entries.entities` (JSONB+GIN, **migración 0084** reversible), distilación extrae entidades (4ª clave del prompt) normalizadas con el mismo tokenizador que la query, recall fusiona una **3ª señal entity-match** (entities ?| query_terms) en el RRF (fuse_rankings→4-tupla; MemoryRecallHit.entity_rank). Verificado: memoria hallada solo por entidad emerge.
- **Deprecated (bd4f43a):** sweep `HTTP_422_UNPROCESSABLE_ENTITY`→`_CONTENT` (67 usos); nuestro código ya no emite DeprecationWarning. Los restantes (otel pkg_resources, langgraph allowed_objects) son de librerías → en el plan de deps.
- **Deps (e0b703c):** informe `docs/06-runbooks/auditoria-dependencias-2026-06.md`. Casi todo son majors con breaking changes (langgraph 1.x, redis 8, pytest 9, mypy 2, ruff 0.15, React 19, Next 16, Tailwind 4, TS 6). **Decisión: NO upgrade masivo** (rompería sistema+CI); recomendado plan dedicado por olas, gateado por CI. Nada de versiones mutado.
- **Dockers:** reconstruidas las 5 (api-server/orchestrator/workers:ci/agent-runtime:v1/admin-panel:manuals) con el código nuevo, recreadas y healthy; migración **0084 aplicada al DB dev** (head=0084). Próximo nº ADR libre = 0065.

## ✅ Sesión 2026-06-18 — Ejecución real verificada + manual con timeline (pusheado, commits 326b884 + 1c5b272)

**Cableado de ejecución real probado de punta a punta en el stack de manuales (dev)**: tarea `ready` asignada a un agente con `model_config.provider=ollama` → orchestrator dispatcha → worker `run_execution` → `agent-runtime` arranca vía `docker-socket-proxy` → llama a `llama3.2:1b` por el egress → tarea **`done`** (timeline real: perceive/recall/plan/finalize/self_review, 630 tokens). El panel `/admin/executions/{id}` lo muestra.

**3 bugs reales corregidos para llegar ahí (eran por qué "no estaba cableado" en dev):**

1. **Imagen `agent-runtime:v1` obsoleta** (anterior al provider ollama) → `ValueError: unknown provider kind: 'ollama'`. Fix: rebuild `docker build -t agent-runtime:v1 -f docker/agent-runtimes/agent-runtime/Dockerfile .` (contexto=raíz). El SOURCE ya soportaba ollama; era la imagen.
2. **egress-proxy bloqueaba el `ollama` in-stack**: el agent-runtime proxifica TODO el HTTP (HTTP_PROXY) y tinyproxy (FilterDefaultDeny) no tenía `ollama` en el allowlist → `ReadError`. Fix: `^ollama(:[0-9]+)?$` en `docker/egress-proxy/filter.txt` + rebuild. Gotcha: `agent-runtime-egress-blocks-in-stack-llm.md`.
3. **Generador (prod-01)**: `celery -A workers` no resuelve (app vive en `workers.celery_app:app`) → worker muerto + healthcheck nunca pasa. Fix con TDD en `compose_generator.py` (command+healthcheck, ambas lanes) + test; worker dev salía `unhealthy` por lo mismo.

**Los dos gaps — RESUELTOS (commits 2eb6531 + 97a99b8, pusheados; investigación con workflow multi-agente + verificación adversarial):**

- ✅ **Consumer atascado = CAUSA RAÍZ + FIX.** NO era envenenamiento del pool (la verificación adversarial lo refutó: cada `_dispatch` abre sesión nueva con `session.begin()`, READ COMMITTED → sin snapshot stale; un restart no curaría eso). Era **publish-before-commit**: el api-server publicaba `task.created`/`task.status_changed` ANTES del commit de `open_tenant_session` (commitea al retornar); el orchestrator leía la fila no comiteada → `task is None` → `return None` silencioso (dispatch.py:285). El "restart cura" era timing espurio. **Fix:** `schedule_after_commit(session, factory)` en auth/deps.py + `open_tenant_session` corre los callbacks POST-commit; create_task/update_task publican ahí. (`BackgroundTasks` NO sirve: en FastAPI 0.136 corren antes del commit de la dep yield.) **Verificado en vivo:** 2 tareas seguidas dispatchan sin reiniciar el orchestrator.
- ✅ **Gancho live plan-completion (ADR 0063 Parte A).** `TaskDispatcher.handle` reacciona a `task.status_changed`/`done` (`_on_task_done`): recomputa el plan desde la BD (reusa `transition_to_pending_human_validation`), transición ATÓMICA idempotente (`UPDATE...WHERE status='in_progress' RETURNING`), emite `orchestrator.plan_ready_for_review`. Antes la transición estaba HUÉRFANA (solo en `plan_runner` in-memory). **Parte B (spawn real del contenedor) en ADR 0063 `proposed`:** decisiones ABIERTAS (procedencia de `main_image` — no hay pipeline de build; worktree a nivel de plan) + necesita modelo capaz (Claude SDK; el 1b solo responde texto). Gotcha DooD del worktree fijada con test del generador (bind same-path, no volumen nombrado).

**Agente demo creado** (dev DB): `Demo Ollama Dev` (role backend_dev, scope project_local en proyecto Hello World PHP `019ed81a-...`, max_concurrent subido a 5), model_config ollama/llama3.2:1b. **Provider dev:** `ollama-local` reactivado (base_url `http://ollama:11434/v1`), `ollama-cloud` desactivado.

**Manual 12** ahora abre la timeline de la ejecución real más reciente (`/tenant-stats/runs?window_days=730` ⚠️ máx 730, no 3650 → `/admin/executions/{id}`). PDFs regenerados (manual-completo 127 págs). **PRÓXIMO que pidió el usuario:** configurar **Claude SDK** como provider (requiere credencial Anthropic del usuario) — mucho más potente, haría el trabajo real (escribir+commit+tests).

---

Checkpoint previo **2026-06-17** (prod-01: Fases A-D completas y pusheadas; CI de integración 20→0 verde; siguiente = Fase E TLS/reverse proxy).

## 🔍 Sesión 2026-06-10/11 — Auditoría integral de producción (COMPLETADA, nada implementado)

**Auditoría multi-agente** (~300 agentes, 16 dimensiones + 5 gaps, verificación adversarial multi-lente): **178 hallazgos vigentes** (9 critical, 52 high — 100 % verificados; 76 medium, 41 low; 1 refutado gap4-4, 1 disputado gap4-1/SSRF). Veredicto: **código de aplicación de alta calidad** (RLS FORCE fail-closed, Argon2id, 84 migraciones reversibles, ruff/mypy strict a 0) pero **plataforma NO desplegable hoy**: instalador = simulacro con stubs, imágenes de apps inexistentes (sin Dockerfiles), **CI muerto** (workflows disparan sobre `main` pero la rama es `master`; en rojo desde 2026-05-29 y se mergeó igual), **guardrails sin invocar** en producción (Principios 10-11 incumplidos; plantillas de validación humana con vocabulario incompatible = no gatean nada), **backup imposible** (tar sin `--create`, oculto por FakeRunner en tests; clave de descifrado circular), **rotación de claves no-op** (FakeVaultRotationClient audita SUCCEEDED), costes LLM a $0 para 3 de 4 providers, cero retries ante 429, Alertmanager → endpoint inexistente, SCA=0.

**Entregables auditoría (en PR #50, rama `docs/auditoria-produccion-2026-06`):** informe + serie **`docs/roadmap/prod-01…prod-16`** (16 planes). PR #50 sin mergear (auto-merge bloqueado por el clasificador → lo mergea el humano). Detalle por hallazgo en **`C:\tmp\auditoria\`**.

## IMPLEMENTACIÓN EN CURSO (2026-06-11, usuario: "continua hasta que acabes con todo")

Ejecutando los 16 planes en orden de olas, protocolo CLAUDE.md (un plan in_progress a la vez; cada `[x]` con su test verde local). **Mecánica clave:** ramas `plan/prod-NN` salen de `docs/auditoria-produccion-2026-06` (los planes solo viven ahí, no en master). **Tests SIEMPRE con `.venv/Scripts/python.exe`** (el `python` del PATH es Laragon, swithout deps). Stack dev arriba (postgres:15432, redis:6379, vault, minio). Los hooks pre-commit reformatean (ruff-format/prettier) → suele hacer falta `git add -A` + recommit.

- ✅ **prod-02 (CI) COMPLETO** → `pending_human_validation`, **PR #51** (base = rama docs; al mergear #50 re-apunta a master). 12/12 tareas verificadas verde en local (meta-tests 6/6, cross_tenant 189, unit 1173, cov 19.4%≥19 floor, security+docs 215+2xfail, vitest 82, Playwright mockeado 245, e2e_smoke 1, pool/rate 8). Cierra tests-1..8, frontend-7, quality-6. De paso arreglado ollama-bootstrap sin baseline. **ADR 0058** (proposed) protección de master + medida puente en conventions.md. Falta: 4 tests humanos + merge + su CI (⚠️ job Playwright ~44min con next dev → follow-up next build/start).
- **Decisiones DIFERIDAS a su plan dueño (NO decididas por mí):** postura AppArmor de cAdvisor privilegiado (contradicción committeada tests/unit vs tests/security → 2 `xfail`) → **prod-08/prod-12** (sandbox-8); extracción `localhost:8001`→constante + 11 specs Playwright con backend → **prod-09**.
- 🔧 **prod-01 (despliegue) EN CURSO** → `in_progress`, rama `plan/prod-01-despliegue-ejecutable` (local, sin pushear; commits durables en disco). **FASE A COMPLETA (tasks 01-04), verificada:**
  - task*01 (6130fee): Dockerfiles de las 4 apps backend. `apps/api-server/Dockerfile` = image PESADO (multi-stage, compila xmlsec/python3-saml, runtime slim no-root uid 1000, los 5 shared-*, CMD uvicorn factory, **contexto=raíz**). workers/orchestrator/notification-dispatcher **comparten api-server como base** (`ARG BASE_IMAGE`, FROM) porque importan api*server. `.dockerignore` excluye `vault-init-output/`+`.env*`+`\*.log`. Los 4 build+import OK.
  - task_02 (e2da33b): `apps/admin-panel/Dockerfile` Next.js standalone (337MB, sirve / →200). OJO `node:20-slim` ya trae usuario `node` uid/gid 1000 (NO groupadd 1000).
  - task_03/04 (8eae859): `.github/workflows/release-images.yml` (tag v\*+dispatch, publica las 5 a ghcr.io/agentic-platform; api-server base primero, resto con BASE_IMAGE) + `tests/unit/test_release_images_workflow.py` + `test_compose_images_contract.py` (5 passed). deploy-2/quality-2 cerrados de forma sostenible.
  - **CONFLICTO conocido:** prod-01 y prod-02 editan ci.yml en ramas distintas → reconciliar al integrar (el "Build each app" con contexto=raíz va ahí).
  - **PENDIENTE prod-01:** Fase B (task*05-08: envs prefijadas API_SERVER*\_/WORKERS\_\_, workers funcional, healthchecks, retro-port hardening — todo en `compose_generator.py`, verificable con tests Python del installer SIN Docker), Fase C (09-11 sandbox: socket Docker/red agentic-agents/perfiles/API interna), Fase D (12-13 migraciones+upgrade), Fase E (14-15 TLS/reverse proxy), Fase F (16-20 bindings reales del instalador + e2e), cierre.
- **CI REAL de prod-01/prod-02 (PR #52, rama rebasada sobre prod-02) — iteración en curso.** La 1ª corrida real reveló que el CI de prod-02 NO estaba runner-green (solo verificado en local). Estado tras varios rounds de fixes (commits hasta 66ca505):
  - ✅ **Build Docker images** verde (fix: `docker build` no `docker buildx`, el driver docker-container no ve la base local). Confirma toda la Fase A en runner.
  - ✅ **Unit / Lint TS / Lint Markdown** verdes.
  - ✅ **Lint Python (prettier)** arreglado: `mirrors-prettier@v4.0.0-alpha.8` **bundlea prettier 3.8.3** (no 4.0-alpha); reformateados prod-03/12/14 + sso-global con el hook real (3.8.3=CI). OJO: `additional_dependencies` es ineficaz (el hook ignora y usa su bundled); NO volver a pinear ahí. NO usar `npx prettier@4.0.0-alpha.8` (versión equivocada). El `pre-commit run` local puede dar falso "Passed" por un crash libuv en Windows.
  - ✅ **Integration: el stack ARRANCA** (fix apparmor: `apparmor_parser` best-effort + overlay `docker-compose.ci.yml` unconfined; los runners hosted de GitHub no dejan cargar perfiles).
  - 🔧 **Integration full-suite (2026-06-16): 20 fallos pre-existentes desglosados en 3 causas independientes** (los destapó prod-02 al resucitar la suite tras 12 días de CI muerto; NO los introdujo prod-01/prod-02). Commit **c25a4a0** pusheado en `plan/prod-01-despliegue-ejecutable`, run CI **27630847997** (se quedó in_progress al cerrar el 16/06):
    1. ✅ **copilot (6) — `test_copilot_device_flow`**: `_create_copilot_provider` posteaba sin `slug`, obligatorio desde la migración 0083 (schema CreateLLMProvider, `schemas/llm_providers.py:129`). Añadido `slug` a los 2 payloads. **Verificado local: 10 passed.**
    2. ✅ **sdk (10) — `test_sdk_python`**: `ModuleNotFoundError: agentic_platform_sdk` — el job de integración nunca instalaba `packages/sdk-python` (aterrizó tras morir el CI). Añadido `pip install -e packages/sdk-python` al step de install del job integration en ci.yml.
    3. ✅ **auth (3) — `test_auth` (login/me/logout)**: `relation "user_mfa_totp" does not exist`, **CI-only — CAUSA RAÍZ ENCONTRADA Y ARREGLADA (commit a64cc1a).** El `configured_app` **DUPLICADO** en `test_auth.py` (y `test_isolation.py`) seteaba `API_SERVER_DATABASE_URL` pero **NO** `API_SERVER_ADMIN_DATABASE_URL`. La probe de MFA del login (`auth/mfa/store.py::user_mfa_methods`) corre sobre el **ADMIN engine** (BYPASSRLS, sin tenant); sin override cae al DSN admin por **defecto**. En LOCAL ese default es la BD **dev** (`agentic_platform`) que SÍ está migrada → pasaba consultando la BD equivocada; en CI esa BD por defecto está **vacía** (solo se migra `agentic_platform_test`) → "does not exist". Confirmado con una **sonda fresca migrations_user** que devolvió `relation "alembic_version" does not exist` (BD vacía). Solo fallan los 3 que llegan al MFA check (wrong-password/unknown-email dan 401 antes). **Fix:** ambos fixtures setean `API_SERVER_ADMIN_DATABASE_URL = admin_database_url`, igual que conftest. Verificado local: test_auth+test_isolation 13 passed.
       - **Forense (descartado con datos, ya retirado):** no era orden, ni la 0037, ni la cadena de migraciones (lineal, single head), ni los reversibility tests (en CI el round-trip upgrade→downgrade 0011→upgrade RESTAURA bien, admin Y app_user). El hook teardown solo marcó test_migrations\* (tardíos, por diseño). El diag definitivo fue la sonda en `user_mfa_methods`.
       - **Diagnósticos TEMPORALES retirados en a64cc1a:** `test_aaa_mfa_diag.py` (borrado), `pytest_runtest_teardown`+infra en conftest, sonda en store.py, steps/env FORENSICS+MFA_FORENSICS en ci.yml. **MANTENIDO:** `timeout-minutes: 45` (era 30; el job rozaba los 30 y un runner lento lo canceló). Commits forenses (historial): c25a4a0, a6d3c54, d583a51, dca4f82, dc38ae1.
    - **GOTCHA a documentar** (pendiente, tras confirmar CI verde): fixture de test que omite `API_SERVER_ADMIN_DATABASE_URL` → el admin engine consulta la BD por defecto; verde en local (BD dev migrada) pero rojo solo-en-CI (BD por defecto vacía). Síntoma: `relation X does not exist` solo en CI para queries del admin engine.
    - **✅ CONFIRMADO VERDE en CI (run 27683709538, sha 4a6a630):** job Integration `success` con **2922 passed, 7 skipped, 0 failed** (+ migraciones 33 passed, cross-tenant 189). Build images + Unit + Lint Python/TS/Markdown todos verdes. **20→0.** Documentado: ADR 0059 (entity-linking, proposed, commit 4a6a630) + gotcha `test-fixture-admin-db-url-override.md` (commit 12e196f).
    - **OJO concurrency:** el workflow tiene `cancel-in-progress: true` → cada push mata el run anterior. La suite tarda ~22 min; NO pushear mientras un run que quieras observar esté en vuelo (varios runs r4-r6 salieron `cancelled` por esto, no por bug).
    - **Pendiente de push (local, sin pushear para no cancelar el run en vuelo):** commit **12e196f** (gotcha). HEAD remoto = 4a6a630. Pushear 12e196f tras cerrar el run actual.
    - **Playwright: DIFERIDO a prod-09 (decisión del operador 2026-06-17).** El subset mockeado está rojo por **deuda pre-existente reproducible en local** (no CI-only): (1) **colisión de glob** `page.route("**/X")` intercepta `page.goto(".../X")` en PW 1.60 (~15 specs: projects/agents/${id}/teams/${id}/human-agents/memories/plans/${id}; fix=predicado por pathname) + (2) otros modos (sidebar-complete, sso-oidc). Documentado: gotchas `playwright-route-glob-intercepts-navigation.md` + nota en prod-09 (task_prod09_08 ya reescribe specs e2e → se arregla allí). El job tiene timeout 60min y llega al timeout. **NO toqué los specs** (dominio prod-09). Commits docs pusheados: 4a6a630 (ADR 0059), 12e196f (gotcha admin-db), b66ca97 (gotcha glob), 8e073d1 (nota prod-09). HEAD remoto = 8e073d1.
    - **✅ prod-01 Fase B task_05 DONE (commit 0679154, pusheado).** `_app_environment` reescrito por-servicio: cada app emite SOLO claves prefijadas (`API_SERVER_`/`ORCHESTRATOR_`/`WORKERS_`/`NOTIFY_`) que mapean a campos reales de su Settings; URLs in-stack (redis DBs 0/1/2/3, vault:8200, minio:9000) como literales; secretos referencian su var prefijada del `.env`. Name-drift corregido (MINIO_ENDPOINT→MINIO_URL, VAULT_ADDR→VAULT_URL, fuera PLATFORM_DOMAIN); VAULT_TOKEN no se emite (opcional, lo inyecta el bootstrap). **Gap real arreglado:** `config_generators.build_env_vars` ahora escribe `ORCHESTRATOR_DATABASE_URL` (orchestrator arrancaba sin DSN). Test de contrato `tests/unit/test_compose_env_contract.py` (4 checks incl. cross-check compose↔.env). Verde: 38 (comando del plan) + 91 instalador, ruff/mypy limpios.
    - **✅ prod-01 Fase B task_06 DONE (commit 924f3e1, pusheado).** `_workers_service`: command celery explícito (pool genérico drena default,heavy,gpu,ingestion,test,review — mirror de `workers.celery_app.QUEUE_NAMES`, el test lo cruza); **servicio nuevo `workers-privileged`** drena SOLO `privileged` (singleton replicas=1, --concurrency=1; perfil estricto = task*10) registrado en CORE_SERVICES+\_BUILDERS; volúmenes en ambos lanes (bind data_root + seccomp ro); envs `WORKERS_BACKUP*\*`prefijadas (valores correctos = prod-04, TODO). Tests`-k workers` = 5. Verde 70 + 96 instalador.
    - **✅ prod-01 Fase B COMPLETA 4/4** (task_05 0679154, task_06 924f3e1, task_07 4a95f3a, task_08 0d9a25d — todos pusheados, TDD, ruff/mypy limpios). task_07: healthchecks de los 4 de fondo (orchestrator wget /healthz:8002; workers±privileged `celery -A workers inspect ping`; notify `celery -A notification_dispatcher inspect ping`). task_08: `cap_drop:[ALL]` (anchor x-seccomp) + `deploy.limits` (anchor x-limits 2g; ollama 8g/docling 4g override; bootstrap exento) en el canónico `docker/docker-compose.yml` + unificado el vault del generador (cap_drop:[ALL]+cap_add:[IPC_LOCK]); test nuevo `tests/unit/test_canonical_compose_hardening.py`.
    - **✅ prod-01 Fase C COMPLETA 3/3** (task_09 24a3b8b, task_10 b33f7b9, task_11 b8c2553 — pusheados, TDD, ruff/mypy limpios). task_09: `docker-socket-proxy` (tecnativa 0.3.0, ACL mínima) en red interna dedicada `agentic-docker`; workers±privileged via `DOCKER_HOST=tcp://docker-socket-proxy:2375`, redes [agentic-net,agentic-agents,agentic-docker], `WORKERS_EGRESS_PROXY_URL`; **ADR 0060 proposed** (acceso daemon + ruta API interna, Parte B=B1). task_10: `WORKERS_SECCOMP_PROFILE_PATH`=/etc/agentic/seccomp/agent-runtime.json + `WORKERS_APPARMOR_PROFILE`=agent-runtime; prereq `check_apparmor` (WARN/degrada). task_11: `internal_api` httpx `trust_env=False` + `ensure_reachable()` fail-loud (token→producción) + api-server en `agentic-agents` (ADR 0060 B1); `InternalAPIUnreachableError` nueva. **(d) rebuild imagen agent-runtime = pipeline release-images al taggear.**
    - **✅ prod-01 Fase D COMPLETA 2/2** (commit 7914211, pusheado, TDD, hooks verdes). task_12: servicio one-shot `migrations` en el compose generado (imagen api-server, `alembic upgrade head`, `ADMIN_DATABASE_URL`) registrado en CORE_SERVICES+\_BUILDERS antes de api-server; las apps (api-server/orchestrator/workers±privileged/notify) reciben `depends_on: migrations: condition: service_completed_successfully` vía `generate_compose`; `restart:"no"` (one-shot, exento en test_hardening junto a ollama-bootstrap); `pg_advisory_xact_lock(0x4147454E54)` en `do_run_migrations` de `apps/api-server/migrations/env.py`; paso `RUN_MIGRATIONS` en `INSTALL_STEP_ORDER` (tras START_STACK, antes de SEED_TENANT) + títulos ES/EN. task_13: `docs/06-runbooks/03-system-upgrade.md` reescrito → `docker compose run --rm migrations` (no `python -m alembic`/`apps/api-server`); orden backup→pull→migrate→up + verificación `alembic current`==head. Tests nuevos: `tests/unit/installer/test_install_steps.py`, `tests/docs/test_runbooks_consistency.py`. Gates: 39 passed (install_steps+migrations+runbooks) + 80 (compose+contract+hardening+cli_install). **Patrón builders sin usar cfg/prod:** `# noqa: ARG001` inline (cabe <100 si la firma es corta; si no, multi-línea).
    - **✅ prod-01 Fase E COMPLETA 2/2** (commit 9d646b6, pusheado, TDD + 2 workflows adversariales). task_14: **ADR 0061** (proposed) reverse proxy TLS = **Caddy**. task_15: servicio `caddy` (caddy:2.8-alpine) ÚNICA superficie publicada (80/443) en CORE_SERVICES/\_BUILDERS; `_hardening`+`cap_add [NET_BIND_SERVICE]`; depends_on api-server+admin-panel healthy; healthcheck plano :80/healthz. Retirados los `ports` de api-server+admin-panel. **Enrutado single-origin** (lo destapó el workflow de diseño): SPA en /, backend bajo `/api/*` con strip + regla `/api/v1`/`/api/v1/*` SIN strip ANTES (la colisión `/admin` frontend-vs-backend hace irresoluble el path puro; `/api/v1` ya nace con /api). `generate_caddyfile` en módulo nuevo `proxy_generator.py`. Campos TLS en SystemConfig (internal/provided/acme + validador: provided→cert+key, acme→email, acme+IP rechazado). SSO base→`https://{domain}/api`. Data dirs caddy/\*, regla AppArmor /config, `NEXT_PUBLIC_API_URL=/api` horneado en release-images.yml. Gate `-k 'proxy or tls or ports'`=14; suite instalador 119. **DIFERIDO a prod-09 (en el ADR):** fixes JS del frontend (wsUrl base relativa, review page cookie-vs-Bearer, fetches inline, preview wizard puerto 8000). El REST tras el proxy funciona sin ellos.
    - **✅ prod-01 Fase F COMPLETA 5/5 → PLAN a `pending_human_validation`** (commits c3561ce/eb1cc22/cf24c6e/087a5bf/f6c40fb/09f7290/0930672, pusheados, TDD + 2 workflows adversariales). task_16: `RealStepExecutor` (módulo nuevo) + seam `command_runner.py` (CommandRunner/SubprocessRunner/FakeCommandRunner) + `api_server/seeds/init_tenant.py` (NUEVO entrypoint idempotente org+admin+membership). SEED_TENANT corre `python -m api_server.seeds` (catálogo built-in) + init_tenant (password CSPRNG por env pass-through). task_17: `check_ports` (80/443) + `RealCredentialBuilder`. task_18: `real_teardown.py` (RealStackTeardown+RealDataPurger+FileSystem seam). task_19: `real_bindings.py` (host-only: RealEnvFileWriter/RealDataTreeProvisioner/hvac adapter lazy) + `build_default_installer` cablea REAL por defecto + guard `_assert_real_install/uninstall_seams` + `--dry-run` + runbook 01 sincerado. task_20: e2e skip-guarded (E2E_INSTALL=1 + docker) + pytest-timeout. **Decisiones de alcance:** BOOTSTRAP_VAULT solo orquesta (KV content + tokens = prod-10; apps leen del .env 0600); wizard HTTP sigue simulación (cableado real = prod-09); frontend same-origin + Playwright = prod-09. **MIN_COMPOSE_VERSION→2.21** (up --wait + one-shot). Falta para `completed`: 3 tests humanos (VM Linux) + merge PR. **➡️ SIGUIENTE: ADR pendientes (ver [[adr-pendientes-implementar-autonomo]]): 0060/0061 ratificar, 0058/0059 implementar.**
  - ⚠️ **Playwright: cancelled** (veredicto incierto; el fix next build/start está puesto, falta una corrida que lo valide).
- **CONSOLIDACIÓN:** los fixes de CI viven en la rama prod-01 (rebasada sobre prod-02); 3 son de dominio prod-02 (prettier, apparmor/integration, playwright) → reflejarlos en prod-02/PR #51 o consolidar PRs al integrar.
- **SIGUIENTE tras cerrar el CI de prod-01:** **prod-03** (guardrails), **prod-05** (rotación), Ola 2 (prod-04/06/07/08/09/10/11/12/13), Ola 3 (prod-14/15/16).

**Why:** evita re-auditar y fija prioridades: Ola 1 = prod-02 (CI, barato y des-riesga todo), prod-01 (deploy), prod-03 (guardrails), prod-05 (rotación); gate de producción = Ola 1 + prod-04 (drill DR) + prod-10 (Vault operable).

**How to apply:** si el usuario pide implementar, activar SOLO el plan que apruebe (uno `in_progress` a la vez, protocolo CLAUDE.md); sugerir prod-02 primero. Acción inmediata pendiente de OK humano: retirar `vault-init-output/` (root token de Vault en claro en el working tree) y re-init de Vault. Los 34 medium sin lote de verificación se re-confirman al iniciar su plan. Roadmap README/EXECUTION-SEQUENCE deliberadamente NO tocados (lo regulariza prod-15).

## Estado del repo y la dev DB (sesiones 2026-06-08/09, todo mergeado)

- master limpio, 0 PRs abiertos; head de migraciones = `0083` (slug único en llm_providers). PRs #32–#49 mergeados: 06.17/06.18, asistente personal (ADR 0053/0054), equipo CI4 (reworkeado), Ollama-por-Docker (ADR 0056), platform-defaults UI (`/admin/settings/platform-defaults`), ADR 0057 F1 (modelo real resuelto en worker, fin del scripted silencioso). **Próximo nº ADR libre = 0058** (0057 ya usado).
- **Dev DB runtime (no en git):** `model.default_config` = ollama/qwen3-coder:480b; `ollama-cloud` ACTIVO, `ollama-local` DESACTIVADO (dispatch resuelve a cloud); embeddings nomic-embed-text 768, 355 chunks con vector; equipo CI4 ejecutable (hereda default por #43).
- Stack dev en Windows: compose base + dev + monitoring + monitoring.dev + windows; Grafana en 3001; postgres dev en puerto 15432.

**Reglas vigentes:** workflows que tocan migraciones/seeds usan SOLO la DB de test, nunca dev. Runtime de agentes aborta bucles (REPETITIVE_LOOP); el asistente converge (dedup) — deliberado. Ver [[workflow-review-paralelo-contamina-fuente]], [[provider-resolution-two-paths]], [[codeigniter4-builtin-team]].
