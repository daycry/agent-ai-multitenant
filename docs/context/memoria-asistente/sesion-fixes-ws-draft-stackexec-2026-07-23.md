---
name: sesion-fixes-ws-draft-stackexec-2026-07-23
description: "2026-07-23 sesión larga — WS acting-as tenant (?tenant_id), draft planning reintento+aviso, stack_exec al equipo CI4, catálogo MCP HTTP-only (ADR 0117a)+auth_kind/ADR 0127, Context7 configurado en hello-world; COMMITEADO (4 commits en plan/runs-visor-trabajo), desplegado en dev."
metadata:
  node_type: memory
  type: project
  originSessionId: ed356da1-3ffb-49dc-a846-642abace2f05
  modified: 2026-07-24T16:19:45.320Z
---

Sesión 2026-07-23 (rama `plan/runs-visor-trabajo`, dev). Diagnóstico + 2 fixes de plataforma (TDD) + 1 config, todo DESPLEGADO en dev pero **SIN commitear** (working tree).

**Fix A — WS acting-as tenant (bug real).** La API WebSocket del navegador no puede mandar cabeceras, así que el WS solo llevaba el JWT; para un System Admin/Owner operando "como" otro tenant (REST lo hace vía cabecera `X-Tenant-Id`), el `tid` del JWT es su tenant "de casa" → `ws.py::_resolve_principal` scopeaba mal → RLS ocultaba el recurso → cierre 1008 → **tormenta de reconexión** → sin updates en vivo (chat, kanban, ejecuciones, documentos). Fix: `_resolve_principal` acepta `?tenant_id=` que sobreescribe el `tid` SOLO para `is_system_admin` (espejo de `X-Tenant-Id`), cableado en los 4 endpoints `/ws/*`; `admin-panel/lib/ws.ts::wsUrl` lo añade desde `getTenantId()`. Tests: `tests/unit/test_ws_principal.py` (4) + `tests/integration/test_ws_tenant_isolation.py` (3 nuevos acting-as, sin romper los 10 de denegación cross-tenant) + `lib/ws.test.ts` (2). **Requiere hard-reload del navegador** (el bundle viejo no manda `tenant_id`).

**Fix B — draft de planning robusto.** El botón "Generar Plan" faltaba porque `pm_plan_draft` (2ª llamada, JSON estricto) volvía vacío en un turno (intermitencia de gpt-oss / timeout de step 150s) → mensaje mudo. Fix en `chat/responder.py`: helper `_resolve_plan_attachment` con **reintento** (`_DRAFT_ATTEMPTS=2`) + `_finish_planning_attachment`; si aun así vacío, `_system_notice` avisa al usuario. Tests en `tests/unit/test_chat_responder.py` (2 nuevos). El mensaje viejo NO recupera el botón: hay que mandar uno nuevo (el proyecto hello-world ya usa claude-sonnet-5, que genera el draft fiable).

**Deploy:** rebuild `agentic-platform/{api-server,admin-panel}:manuals` (recetas en docs/03-guides/gotchas: api-server `--build-arg WITH_CLAUDE=1` contexto raíz; admin-panel `--build-arg NEXT_PUBLIC_API_URL=/api` contexto `apps/admin-panel`, PowerShell por el gotcha MSYS) + `docker compose -p agentic-platform up -d api-server admin-panel`. Ambos healthy. `workers:ci` NO se reconstruyó (no ejecuta WS ni responder).

**Config — stack_exec al equipo CI4.** Runs de la tarea "Configurar entorno base CI4" (tenant Mediapro `019f8e26`, proyecto hello-world `123b2f2c`) morían: el agente intentaba `composer`/`php` vía `shell_exec` (corre en agent-runtime, SIN PHP) → `find / -iname composer*` en bucle → `repetitive_loop_detected` → escalado a humano → tarea `blocked`. Raíz: los 10 agentes del equipo CI4 NO tenían la tool `stack_exec` (la que corre el toolchain en el runtime-template `php-phpunit` del proyecto, ADR 0093). Asignada `stack_exec` (tool_id `11c92c57-5546-5285-a56a-519f3603547f`) a los 10 (INSERT en `agent_tools`, sin tenant_id — scoping por FK). Pendiente/watch: verificar que la imagen `php-phpunit` trae composer; la tarea `019f8e48-979c` sigue `blocked` — NO relanzada (ver [[no-desbloquear-sin-verificacion]]). Relacionado: [[ux-tool-assignment-friendly]], [[stack-exec-feature]], [[supervision-runs-autofix-plataforma]].

**Catálogo MCP — ADR 0117(a) resuelto (Opción 3, híbrida HTTP-only).** Descubierto que las 24 plantillas de `shared_mcp/catalog.py` eran TODAS `stdio` con binarios no empaquetados en el runtime → ninguna arrancaba (un run CI4 real daba vueltas y escalaba). Decisión del operador: OCULTAR (no borrar) las stdio y ofrecer solo HTTP. Implementado: `routers/mcp_catalog.py::offered_catalog()` filtra `transport != "stdio"` (+ deny-list `_UNAVAILABLE_TEMPLATE_IDS`); `GET /mcp-catalog` solo HTTP. +3 plantillas HTTP en el catálogo: `context7` (remoto `https://mcp.context7.com/mcp`), `atlassian` (sidecar `ghcr.io/sooperset/mcp-atlassian` streamable-http, auth en env del sidecar), `github-remote` (`https://api.githubcopilot.com/mcp/`, PAT en cabecera Authorization vía Vault). CATALOG total 24→27 (24 stdio ocultas + 3 ofrecidas). Tests: test_mcp_catalog_availability (nuevos), test_github_mcp (familia scm +github-remote), test_mcp_integrations (recuento 27) — 89 verdes, ruff+mypy limpios. ADR 0117 sección (a) con «Resolución 2026-07-23» ((b)/(c) siguen proposed); doc `04-reference/mcp-servers.md` actualizado. **api-server:manuals reconstruida+recreada+verificada** (picker sirve solo las 3 HTTP). Las plantillas stdio NO se borran → sin romper conduct-time validation ni tests SCM/postgres. Ojo egress: context7/github-remote exigen abrir su dominio en `projects.allowed_domains`; atlassian es hostname interno (sin egress). Los stdio-binarios (Opción 1 empaquetar) descartados salvo caso que lo pague. Pendiente/watch: si un agente va a USAR uno de estos MCP en un run, conviene rebuild de agent-runtime (validación conduct-time contra el catálogo baked).

**Cierre de sesión (implementa-en-orden):** (1) conector OAuth ADR 0127 — solo el CIMIENTO verificable hecho: campo `auth_kind` (none|static|oauth) en `McpServerTemplate`+DTO (derivado de secret_keys; oauth explícito); el MOTOR OAuth (VaultTokenStorage sobre el `OAuthClientProvider` del SDK mcp 1.28.1 + flujo connect/callback + wiring) queda para SESIÓN INTERACTIVA (no verificable headless: requiere registrar app OAuth + consentir en navegador). (2) **Context7 configurado en hello-world**: `mcp.context7.com` en `allowed_domains`, server declarado en `mcp_servers`, conexión verificada (tools reales: `resolve-library-id`, `query-docs`), y **tools importadas por la API real** → `context7.resolve_library_id`, `context7.query_docs` (falta asignarlas a agentes en UI). Atlassian NO configurable headless (necesita token del operador + desplegar sidecar). (3) **COMMITEADO**: 4 commits en `plan/runs-visor-trabajo` (958a59af ws acting-as, 7a42da99 chat draft, 719f42a9 mcp catálogo+ADRs, 6c5b4e27 manual14); pre-commit verde (black/ruff/mypy árbol completo/prettier). Nota importante: `stack_exec` a los agentes CI4 y la config de Context7 son cambios en **BD/dev, NO en git**. La rama plan/runs-visor-trabajo NO se ha pusheado.

**CONTINUACIÓN (misma sesión, tras compactar) — 0128 fases 3+4 + 0127 núcleo, TODO EMPUJADO.** (a) **ADR 0128 fase 3** (`f46116b2`): eliminado el gate MCP por-agente de `routers/agents.py` PUT `/agents/{id}/tools` (rechazaba 422 una tool MCP salvo server declarado) + `compute_effective_tools` acepta `project_mcp_tool_names` y `capabilities.hacer_for_agent`+`get_agent_effective_tools` lo inyectan (`role=agent.role`) → set efectivo honesto; test 422→200 + 3 tests effective. **Fase 4** (`08f9402d`): UI `McpToolRolePolicySection` (edita `project.mcp_tool_roles` vía PUT) + la ficha del agente ya NO ofrece tools MCP como asignables (nota ADR 0128); vitest nuevos. Implementadas por **workflow** (w4m59l53y, 3/3 verde) y re-verificadas por mí (mypy + 20 tests backend). ADR 0128 FASES 1-4 COMPLETAS. (b) **ADR 0127 núcleo** (`4260d816`, `status: accepted`): conector OAuth genérico para MCP remotos, parte VERIFICABLE headless — `shared_mcp.auth.VaultResolver.write()` (Static+Hvac KV-v2; era el hueco que impedía refrescar) + `shared_mcp/oauth.py` (`VaultTokenStorage` implementa el `TokenStorage` del SDK sobre Vault clave `(tenant,project,server)`, refresh conserva DCR; `build_oauth_provider` con handlers que fallan-ruidoso en runtime; `build_client_metadata`; `oauth_vault_path`) + `MCPClient.connect(auth=)` reenvía `httpx.Auth` a sse/streamable_http + catálogo: `auth_kind` gana `sidecar` (atlassian deja de mentir `none`) + plantilla `atlassian-remote` (`oauth`, `mcp.atlassian.com` sse) catalogada pero RETENIDA del picker (`_UNAVAILABLE_TEMPLATE_IDS`). Tests `test_shared_mcp_oauth` (20) + recuento 27→28. **DIFERIDO a sesión INTERACTIVA** (no headless): endpoints connect/callback + botón «Conectar» + handshake real Atlassian/GitHub + cablear Vault en api-server (`get_vault_resolver()` hoy devuelve None sin `API_SERVER_VAULT_TOKEN`). SDK mcp 1.28.1: `OAuthClientProvider` es `httpx.Auth`; `TokenStorage`=4 métodos async; `OAuthToken` no persiste `expires_at`. **Rama empujada** (`269edb9d..f0c60da3`). **DESPLEGADO en dev 2026-07-23**: rebuild `agentic-platform/{api-server,orchestrator,admin-panel}:manuals` (orchestrator `FROM ${BASE_IMAGE}` → construir con `--build-arg BASE_IMAGE=agentic-platform/api-server:manuals`; admin-panel `--build-arg NEXT_PUBLIC_API_URL=/api` contexto `apps/admin-panel` en PowerShell) + recreados vía `docker compose --project-directory docker -p agentic-platform` (6 ficheros -f) `up -d --no-deps --force-recreate`; los 3 healthy. Migraciones aplicadas dev **0115→0116→0117** (0116 mcp_tool_roles NO estaba aplicada antes; dev estaba en 0115) vía `docker exec -e DATABASE_URL=<admin> api-server python -m alembic upgrade head`. Skills resembradas quirúrgicamente (`docker exec -i api-server python -` con `ensure_platform_tenant`+`seed_builtin_skills`; 55 skills; NO la CLI completa que dispara ingesta Ollama). Verificado: 4 skills atlassian en BD, `ck_skills_category` con 7 valores, catálogo (atlassian=sidecar, atlassian-remote=oauth NO ofrecido, ofrecidos=context7/atlassian/github-remote), healthz 200. workers `:ci` y agent-runtime NO rebuild (no afectados por 0128/skills/0127-runtime). Para Atlassian HOY: usar el SIDECAR (funciona ya); OAuth es el futuro limpio. Relacionado: [[prueba-mcp-tools-skills-2026-07-18]]. (c) **Skills builtin de Atlassian + categoría `atlassian`** (`f0c60da3`, empujado): `SkillCategory` gana un 7º valor `atlassian` (bucket de INTEGRACIÓN, no dominio de trabajo) — sync en 4 puntos (enum `domain.py`, **migración 0117** `skills_atlassian_category` que reconstruye `ck_skills_category`, reversible verificada head↔base con test_migrations; mapas frontend label/icon `Ticket`/orden en agent-skills-section.tsx; seed `builtin_skills.py` 51→55). 4 skills builtin (is_builtin → visibles cross-tenant por policy `skills_builtin_read` de migr 0005): `atlassian-jira-task-tracking`, `atlassian-jira-review-notes`, `atlassian-confluence-docs`, `atlassian-jira-planning-context`. Prompts redactados por **workflow judge-panel** (3 lentes→síntesis); NO cablean tool namespaced (server lo elige el operador) ni IDs (llegan por el plan), idempotentes, degradan con gracia si el MCP no está (anotan en PROGRESS, no fallan). `required_tools` vacío (tools = capacidad del proyecto, ADR 0128). Tests `test_builtin_skills_atlassian` (guardas anti-namespaced + degradación) + `test_seed_skills`. Nota ADR 0050 «Actualización 2026-07-23» + tabla en `04-reference/mcp-servers.md`. **OJO**: NO se siembran en el arranque (startup.py solo garantiza el catálogo KB, no skills; el seed de skills es CLI-manual). Para verlas hay que aplicar migración 0117 + correr `seed_builtin_skills` — **ya HECHO en dev** (ver bloque de deploy arriba). El operador las asigna al agente desde su ficha (Skills).

**(d) Frontend del botón «Conectar» OAuth (ADR 0127, `954fb1df`, empujado, SIN desplegar).** El operador intentó añadir Atlassian pero solo veía el sidecar (`http://mcp-atlassian:9000/mcp`) sin OAuth — porque `atlassian-remote` (el oauth) está en `_UNAVAILABLE_TEMPLATE_IDS` y no hay botón. Implementado el FRONTEND: componente `mcp-oauth-connect.tsx` `McpOAuthConnect` (GET `/oauth/status` → Conectado/No conectado; botón «Conectar»→POST `/oauth/connect`→redirige a `authorization_url`, `onAuthorize` inyectable; errores con gracia), integrado en `McpServerCard` cuando `authKind==='oauth'` (page.tsx resuelve por url contra el catálogo, `authKindByUrl`), nota OAuth en el diálogo de creación, banner de retorno del callback en page.tsx (`?oauth_result=connected|error&server=&reason=`), `auth_kind` añadido a `McpCatalogEntry`. Tests: `mcp-oauth-connect.test.tsx` (5) + fix mock `useSearchParams` en `page.test.tsx`; 14 verdes en el dir; tsc 0. **PENDIENTE backend** (contraparte del contrato): endpoints `/oauth/{status,connect,callback}` en api-server usando `build_oauth_provider`/`VaultTokenStorage` (ya existen del núcleo) + cablear Vault en api-server (`API_SERVER_VAULT_TOKEN`) + quitar `atlassian-remote` de la deny-list + **fix url catálogo a `https://mcp.atlassian.com/v1/mcp` (streamable_http)** — hoy la puse `/v1/sse` pero la INVESTIGACIÓN (agente, live probes) confirma que `/v1/sse` está DEPRECADO (jun-2026) y el endpoint es `/v1/mcp`. **Hallazgo clave de la investigación**: Atlassian remoto **soporta discovery RFC 8414 + DCR RFC 7591 + PKCE + refresh** (probado con `POST /v1/register`→201, sin app pre-registrada; `well-known/oauth-authorization-server`→200); el `OAuthClientProvider` del SDK maneja todo el flujo → el backend será relativamente directo. El front solo se «enciende» cuando aterrice el backend (hoy no hay server oauth seleccionable).

**(e) OAuth «Conectar» BACKEND + DESPLEGADO end-to-end (ADR 0127, commit `52d8d5b5`).** `apps/api-server/src/api_server/mcp_oauth_flow.py` (motor auth-code+PKCE web-native: discovery RFC8414 + DCR RFC7591 register-once + exchange; estado pendiente en Redis single-use TTL10min; tokens+client_info en Vault vía `VaultTokenStorage`; reusa `PKCEParameters` del SDK) + `routers/mcp_oauth.py` (POST `/oauth/connect` tenant-admin→authorization_url, GET `/oauth/callback` **PÚBLICO** validado por state→exchange→302 a `/admin/.../mcp-servers?oauth_result=`, GET `/oauth/status`), registrado en main. Catálogo: `atlassian-remote`→`https://mcp.atlassian.com/v1/mcp` (streamable_http; `/v1/sse` DEPRECADO) y **OFRECIDO** (fuera de deny-list). Investigación (agente, live probes) confirmó Atlassian soporta discovery+DCR+PKCE+refresh SIN app pre-registrada (POST `/v1/register`→201). **DESPLEGADO**: rebuild api-server+admin-panel:manuals, recreados healthy; rutas verificadas (status→401, connect→405 POST-only, callback→302). **Override base pública fijado en dev** (platform_settings: `app.public_base_url=http://localhost:8080` + `app.api_path_prefix=/api`) → `_effective_redirect_base()=http://localhost:8080/api` (antes localhost:8001, mal para el stack Caddy) → el `redirect_uri` del callback resuelve. FALTA solo: el operador prueba «Conectar» en el navegador (consentimiento Atlassian real) navegando a `http://localhost:8080`. Vault YA cableado en api-server (`API_SERVER_VAULT_TOKEN=dev-root-token`). Tests `test_mcp_oauth_flow` (7). Commits empujados hasta `52d8d5b5`.

**(f) FIX git «no history in common» (commit `0f765f02`, empujado, DESPLEGADO).** Encargo del operador: entender el aviso del proyecto `123b2f2c` + asegurar que el git funciona al configurar. DIAGNÓSTICO (workflow `wb8w5cvzi` + live): el bare `hello-world.git` tenía `master` local en una raíz SINTÉTICA `c5cddccc "Initial commit"` mientras `origin/master`=`9d480c28 "Add initial README"` (el repo GitHub `daycry/test-hello-world` se creó CON README) → merge-base VACÍO → PR «no history in common». CAUSA RAÍZ: `execution.py::_provision_worktree` sembraba raíz sintética (`seed_initial_commit_if_empty`) sin hacer `fetch`+`align` cuando el bare estaba vacío y el sync no había corrido (la secuencia buena solo vivía en `repo_clone.py`). FIX: `_provision_worktree` gana `project_id`; nuevo `_align_base_from_remote_if_empty` que, si el bare no tiene commits, ejecuta `_clone_project_repo_async` (fetch+align, ruta probada) ANTES del seed → base desciende de `origin/<default>`; acotado por `has_commits()` (solo 1ª provisión toca red); best-effort. Endurecido `seed_initial_commit_if_empty(default_branch=)` (mata master-vs-main). +3 tests en `test_git_base_alignment` (11 verdes). El aviso EN SÍ es correcto (guarda merge-base de `plan_git.py` + `align_default_branch`, defensas post-incidente CI4 2026-07-09). DESPLEGADO: rebuild `workers:ci` (base api-server:manuals) + recreados workers/aux/backup. **VERIFICADO en vivo**: wipe del bare + mi ruta de fix → `master` local=`9d480c28`=origin/master (raíz sintética ELIMINADA), rama de plan sobre master COMPARTE historia con origin (PR viable). Plan `019f8e47` reseteado (10 tareas→backlog, plan→in_progress) + relanzado. **VERIFICADO E2E EN RUN REAL**: el worker (app) creó el bare, mi fix alineó `master`=`origin/master`, creó la rama de plan `plan/019f8e47-…` que **comparte historia con origin** (merge-base=9d480c28 → PR VIABLE); el plan corrió 6 tareas done + commiteadas sobre la base origin-rooted, y la tarea `97ab` (prueba E2E de la home) escaló a `needs_human_review` (`abort=max_iterations_exceeded`, 50 iter, parcial) → plan `blocked` en el **gate de review humano** (= el hito que pedía el operador; el escalado es convergencia del agente, NO git). Fix git 100% probado de punta a punta. OJO OPERACIONAL: al testear NO alinees el bare por `docker exec` (corre como ROOT → «dubious ownership» para el worker `app`); y si wipeas el bare, wipea TAMBIÉN `worktrees/` (los huérfanos rompen `git worktree add`).

**(g) FIX stack_exec `cwd` (A) + higiene de layout CI4 (B) — commit `5ffd9e79`, empujado, DESPLEGADO.** Diagnóstico del run 97ab (test E2E que agotó 50 iter sin converger, revisando su `steps_log`): el proyecto CI4 quedó en el subdir `ci4build/` (decisión del agente; `ci4build` NO es convención — no está en seeds/prompts) y `stack_exec` NO tenía forma de fijar el working dir (solo `command`+`timeout_s`; y bloquea `sh -c "cd …"`) → `vendor/bin/phpunit` daba 127 desde `/workspace`, el agente probó 25 formas frágiles (solo `composer --working-dir=ci4build exec -- phpunit` funcionó una vez, #18) y giró. Las OTRAS 7 tareas funcionaron porque eran de **código** (read/write, 0 `stack_exec`) o **setup ligero** (979c: 3× `php spark config:check`); 97ab es la única que EJECUTA phpunit (arranca CI4, `require ./app/Config/Paths.php` relativo al cwd). **FIX A**: `stack_exec` gana `cwd` OPCIONAL (relativo al worktree; **retro-compatible** — sin cwd corre desde la raíz como hoy, así que los proyectos SIN subdir no se ven afectados), hilado por 8 capas (schema `builtin_tools` → `stack_exec_tool` → `internal_api` → endpoint `internal_agent` `RunStackRequest.cwd` → `celery_client.run_stack_command_and_wait` → celery `stack_exec_task` → `TestRuntimeRunner.run_command` → `_exec` con helper puro `_apply_cwd` que valida sin `..`/absoluto/chars-inseguros y prefija `cd <cwd> &&`). **FIX B**: guía de higiene de stack compartida por TODOS los agentes CI4 (`ci4_team.to_model_config` es/en): proyecto en la RAÍZ del worktree; si en subdir, usar `cwd`; nunca `cd`/encadenar. Tests `test_stack_exec_cwd` (6). DESPLEGADO: rebuild api-server+workers+agent-runtime (`agent-runtime:v1`, WITH_CLAUDE=1, contexto raíz) + reseed `seed_builtin_tools`(17)+`seed_ci4_agents`(10) → `cwd` en el schema de la tool + guía B en los 10 prompts CI4 (verificado en BD). RETRY de 97ab **VERIFICADO CON ÉXITO**: la ejecución del implementador (`019f931b`) hizo **1 sola** llamada `stack_exec` con **`cwd="ci4build"` → `ok=True`** (`vendor/bin/phpunit -c phpunit.dist.xml tests/E2E/…` PASÓ), convergió en **22 iteraciones** (vs 50 max-out sin fix), `finish=success`, `done` → `in_review`. A+B probados de punta a punta: antes 25 stack_exec/50 iter/needs_human_review; después 1 stack_exec+cwd/22 iter/done. El plan corrió 9/10 done; `97af` (Revisión de seguridad) escaló a review humano (`research_exhausted`, convergencia del agente, NO git/stack_exec). PR viable (9 commits sobre origin/master).

**(h) FEATURE servicios + imagen de runtime por proyecto (ADR 0129, commit `dc1d99ae`, empujado, SIN desplegar).** Análisis (workflow `w9upyg1zl`, síntesis del journal): 14 runtime-templates = catálogo CERRADO `agent-runtime-<id>:v1` (build manual `scripts/dev/build-runtime-templates.sh`, sin publish; bug latente: CI las tagea sin prefijo `agent-runtime-`); NO hay UI de plataforma que liste imágenes; el tenant solo SELECCIONA (`default_runtime_template`). Hallazgo: infra de **servicios auxiliares** (`AuxServiceSpec`+`_start_aux_services`, sidecars endurecidos + modo testcontainers DinD) EXISTE pero DORMIDA (`aux_services` default `()`, nadie la puebla; sin inyección de connection-string; el review NO monta servicios). `review_image` = precedente de imagen aportada por tenant. IMPLEMENTADO TDD: `workers/runtime_services.py` (`build_project_runtime_services(repository_config)` → aux_services + main_env con `DATABASE_URL/REDIS_URL` derivadas + `runtime_image` override; allowlist mysql/mariadb/postgres/redis/beanstalkd + imagen arbitraria; validado) + `TestRuntimeSpec.main_env` inyectado en `_build_test_kwargs` + cableado en stack_exec_task+test_runtime_task (+ execution threadea repository_config). Config vía `repository_config.services`/`env`/`runtime_image` (sin migración). Tests `test_runtime_services` (18). **PENDIENTE fase 2**: aux en review/preview, UI, escaneo imagen custom, tope recursos, deploy+e2e. Relacionado: [[stack-exec-feature]]. Relacionado: [[data-root-volumen-durable]], [[supervision-runs-autofix-plataforma]].

**ADR 0128 (proposed, commit 8e1be4f9) — tools MCP aportadas por el proyecto en runtime.** Discusión de diseño con el operador: las tools MCP deben ser capacidad del PROYECTO aplicada dinámicamente al run (allowlist MCP = servers declarados del proyecto en el que corre), NO grants estáticos por-agente. Motivo: los agentes CI4 son `global_tenant_template` compartidos; hoy una tool MCP solo se asigna a un agente `project_local` cuyo proyecto declara el server (agents.py:1064-1081) → los template no pueden usar MCP del proyecto. Verifiqué que el flujo project-local SÍ existe (adopt_team `target=project` forkea a project_local; dispatch pool incluye project_local, dispatch.py:1648-1657) PERO forkear prolifera copias. El operador prefirió: agentes compartidos + proyecto aporta tools MCP dinámicamente + granularidad por rol OPCIONAL a nivel de proyecto (jira→PM, confluence→writer); builtins/rol siguen por-agente; forkear (ADR 0066) queda como personalización, no requisito MCP. Corrección propia importante: mi objeción de "no relajar el scope-check por fuga cross-tenant" solo aplica a grants ESTÁTICOS; el modelo CONTEXTUAL (agente en proyecto P usa tools de P) NO filtra. **Decidido NO forkear hello-world de momento** (queda con template agents; Context7 declarado+importado a nivel proyecto pero los template no lo usan hasta que aterrice 0128 o se forkee). ADR 0128 **Fase 1 (9b82e49a) + Fase 2 (13db2714) IMPLEMENTADAS y commiteadas** (rama plan/runs-visor-trabajo, sin push, SIN desplegar): Fase 1 = `resolve_project_mcp_tool_names`+`extend_allowlist_with_project_mcp` en `agent_tools_enforcement`, cableado en `dispatch._build_request` (unión aditiva). Fase 2 = columna `projects.mcp_tool_roles` (migración 0116 reversible, verificada up/down contra dev; dev queda en 0115) + `filter_mcp_tools_by_role_policy` + `role=agent.role` en dispatch + schema project (create/update/response + `to_project_response`). Camino de fix incidental: `fix(db)` 51f3f3e3 completó el agregador `db/models.py` con los modelos de marketplace (FK `Tool.source_installation_id`→`marketplace_installations`; el aislamiento de tests fallaba con NoReferencedTableError — pre-existente, no funcional). Y el test de contrato `test_project_settings_contract` cazó que faltaba `mcp_tool_roles` en el builder `to_project_response` (arreglado). PENDIENTE 0128: Fase 3 (deprecar gate MCP por-agente agents.py:1064-1081 + migración de agent_tools MCP), Fase 4 (UI), verificación e2e (naming runtime, no headless). Todo verde (unit + contrato + dispatch/enforcement); pre-commit (mypy árbol completo) verde.
