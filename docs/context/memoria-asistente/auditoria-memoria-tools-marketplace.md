---
name: auditoria-memoria-tools-marketplace
description: "Auditoría 2026-06-24 de memoria/tools/marketplace: hallazgos confirmados pendientes de fix (sin fuga cross-tenant)."
metadata:
  node_type: memory
  type: project
  originSessionId: cc6008fc-23fa-4218-be2b-123a3f5cd8cc
---

2026-06-24: auditoría (workflow wp31ton5t, 4 auditores + verificación adversaria) de **memoria de agentes, tools, e inyección de marketplace**. Informe: `docs/roadmap/auditoria-2026-06-memoria-tools-marketplace.md`. **Núcleo sólido, NINGUNA fuga cross-tenant.** Hallazgos confirmados pendientes:

- **HIGH H0+H3 — ARREGLADO (commit 3419b2b, rama `fix/agent-memory-tools-runtime-injection`, SIN desplegar)** — era: las tools `memory_recall`/`memory_store` (y orquestación kanban_update/task_comment/agent_invoke) NUNCA se anunciaban al LLM en el path orchestrator-dispatch→agent-runtime: runtime-only (sin fila Tool → no asignables → nunca en allowlist), y `build_model_tool_schemas` solo emitía schema para nombres del allowlist. Memoria INERTE en ejecución pese a memory_scope bien configurado. Fix TDD: worker `build_model_tool_schemas(..., include_system_tools=True)` desde execution.\_agent_spec anuncia memoria+orquestación; runtime `register_system_families` las cablea SIEMPRE (cierra L5) + `_effective_allowlist` las exime del allowlist por-agente (block-all de discussion intacto). Tests verdes (worker unit + runtime + integration chat-modes). **Desplegar = rebuild agent-runtime image + workers + recreate.** Relacionado [[memoria-tool-calling-fix]] ("agentes #2/#3").
- **HIGH H1/H2/M3 — ARREGLADO (commit 7b2ddae)**: `memory_entries` es tabla COMPARTIDA por agentes (team/project/global) + asistente (private, user_id=humano) + córtex (private, user_id=owner). Fix: en `/memories` (list/delete/similar/merge) `private` → estricto al dueño (otro usuario = 404); shared/global sin cambios (modelo operador; merge ya guardado por owner-pointer match). NO se inventó pertenencia humana a equipos (team_members son AGENTES). Córtex/asistente usan su vía interna, no este router.
- **HIGH H3 — ARREGLADO** (parte del commit 3419b2b, unificado con H0).
- **HIGH H4 — BLOQUEADO POR INFRA, NO cablear naïvemente**: el endpoint `POST /marketplace/installations` no corre los gates. PERO cablear `orchestrator.install()` es una REGRESIÓN: `_gate_sandbox` construye `MarketplaceSandbox()` (Docker) y falla cerrado; el api-server NO tiene socket Docker (Principio 2) → community/experimental → 422; verified necesita `MARKETPLACE_SIGNING_PUBLIC_KEY` + artefacto en disco → 422; rompe ~5 test files (test_marketplace_endpoints/consent/cross_tenant_sharing/revocation/versioning). Es Fase B/C diferida (= M1 "live path pending registry runtime"). **Fix correcto = ADR + sandbox runner fuera del api-server (overlapa con infra agent-runtime).** Acción segura inmediata (fallback del informe): corregir docstrings engañosos (InstallationStatus.ENABLED "usable by agents" es falso) + documentar el diferimiento. PENDIENTE decisión del operador.
- **MEDIUM M1** — instalar marketplace NO materializa nada en catálogo nativo (solo fila contable); docstring `InstallationStatus.ENABLED` miente ("usable by agents"). Diferido a "registry runtime". Fix: materialización + provenance, o ADR + corregir docstring.
- **MEDIUM M2 — ARREGLADO (commit 73e7add)**: `memory-store` del agente ahora usa el proyecto EFECTIVO (`_resolve_effective_project`, ADR 0054) en vez de `agent.project_id` → simetría store=recall; agente global escribe project_shared en el proyecto de la tarea. `_resolve_store_owner` usa `project.id`. Flag OFF → 400 (simétrico con recall vacío).
- **H4 — DIFERIDO con honestidad (commit 2a65821, ADR 0081)**: no se cablea (regresaría); copy de InstallationStatus.ENABLED corregido + plan Fase B/C en ADR 0081 (sandbox fuera del api-server + registry + claves + materialización M1).
- **MARKETPLACE Fase B/C (H4 + M1/L2/L3) — DECISIÓN DEL OPERADOR 2026-06-25: ANOTADO, NO IMPLEMENTAR de momento.** Queda en ADR 0081 (`accepted`) + informe. NO retomar autónomamente (ADR 0081 es accepted, no proposed, así que [[adr-pendientes-implementar-autonomo]] no aplica). El marketplace hoy solo hace contabilidad de instalación (no materializa la capacidad ni corre gates de seguridad en install fresco); bloqueado por infra (sandbox necesita Docker que el api-server no tiene, Principio 2). Sin fuga; la adopción de equipos va por otra vía funcional (`/teams/adopt`, ADR 0066).

- **BUG EXTRA hallado al "resolver los flakys" (no era flaky, era real) — ARREGLADO (commit 9571739)**: `rag/search.py` desempaquetaba 3 de la 4-tupla de `fuse_rankings` (que devuelve `(rrf_score, bm25_rank, vector_rank, entity_rank)` desde ADR 0059) → `ValueError "too many values to unpack"` → **rag-search/semantic_search daban 500 en CUALQUIER búsqueda con resultados**. El consumidor de memoria (recall.py) se actualizó al añadir entity-match; el de rag/search.py se quedó atrás. Fix: desempacar 4 en ambos sitios. Lección: "falla en baseline" ≠ "está bien"; investigar el fallo real. El 3er flaky (`has_embedding` en test_memory_store) era test acoplado a Ollama → fixture fija embedder a None (determinista).

**ESTADO FINAL (2026-06-24): todo el código de "implementa todo" HECHO en rama `fix/agent-memory-tools-runtime-injection`** (commits 3419b2b H0/H3, 7b2ddae H1/H2/M3, 73e7add M2, 2a65821 H4-diferido). Tests verdes (flakes ambientales pre-existentes: has_embedding + 2 rag-search HashEmbedder, confirmados por stash). **SIN DESPLEGAR** — requiere rebuild agent-runtime+workers (H0/H3) + api-server (memorias) + recreate; sin migraciones. Pendiente: confirmar deploy + estrategia de merge de la rama.

- **LOW — TODAS ARREGLADAS**: **L1** (commit bd69c8e) `required_tools` de skill validado contra tools vivas del tenant (422); **L4** (commit 3e60848 + **migración 0096**, reversible, dev DB en 0096) índice `COALESCE(project_id, zero-uuid)` para dedupe tenant-wide; **doc** "15 vs 19 tools" (bd69c8e); **L5** cerrada por H0/H3. Solo queda **M1/L2/L3** (materialización marketplace) ligada a H4/ADR 0081 (no independientes). dev DB head ahora = **0096**.

NO empezar a arreglar sin que el operador elija prioridad y resuelva el fork de `/memories`. Relacionado con [[prioridad-codigo-limpio-mantenible]] (TDD) y [[deliverables-en-docs-roadmap]].
