"""agent_runtime — the package that runs inside the agent-runtime
container.

Plan 02 Fase B ships the skeleton (a dependency self-check). The
LangGraph agent loop — perceive → recall → plan → act → observe →
reflect → finalize → self_review — arrives in Fase C (task_02_10).
"""

__version__ = "1.0.0"
