# agent-runtime:v1 dependencies (task_02_05).
#
# Loose upper bounds: the image is rebuilt per release and we want the
# current patch line, but never a surprise major. LangGraph drives the
# agent loop wired up in Fase C (task_02_10); the rest are the libs the
# builtin tools (Fase D) lean on.
langgraph>=0.2,<1.0
langchain-core>=0.3,<1.0
httpx>=0.27,<1.0
pydantic>=2.5,<3.0
structlog>=24.1,<26.0
tenacity>=8.2,<10.0
