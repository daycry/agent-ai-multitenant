"""Shared domain constants for the agentic platform.

Currently hosts the **canonical tool-name source of truth** (ADR 0048),
importable by api-server, orchestrator and the agent-runtime so the catalog,
chat-modes, runtime and approval gate all speak one namespace.
"""
